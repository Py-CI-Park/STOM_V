# V3K analyzer/data contract spec

???: 2026-05-09 KST
?? ??: `docs/update_log/2026-05-09_v3k_design_2_analyzer_data_contract.md`
?? ?? lane: `STOM_Version_2U_C`
??: implementation ? contract ??

## 1. ??

? spec? `STOM_Version_2U_C`? V3 ??/?? ??? ??? ? analyzer? ???? data contract? ????. ??? V3? LS API ??? ????, Kiwoom ?? 2U_C runtime ??? V3 ???? ???? ????? ???.

## 2. ?? runtime contract

### 2.1 ?? ?? object

?? analyzer adapter? ??? ?? ?? ???.

```text
market_gubun: int            # 1 stock, 2 future, else coin ?? ?? 2U_C ?? ??
market_type: str             # stock/future/coin
market_info: dict            # V3 analyzer ?? ?? view
is_tick: bool
dict_findex: dict[str, int]  # 2U_C factor index
code: str
backtest_date: int | str | None
code_data: ndarray           # ?? window
current_price: float | int | None
feature_flags: dict[str, bool]
```

### 2.2 ?? ?? object

adapter? analyzer? raw tuple? ?? bundle? ?? runtime?? ???.

```text
V3KAnalyzerOutput:
  pattern_score: float | None
  pattern_confidence: float | None
  volume_score: float | None
  volume_confidence: float | None
  volume_profile_score: float | None
  volume_profile_confidence: float | None
  volatility_score: float | None
  volatility_confidence: float | None
  expected_return: float | None
  take_profit_return: float | None
  stop_loss_return: float | None
  stop_take_confidence: float | None
  risk_score: float | None
  microstructure_signal: object | None
  diagnostics: list[str]
```

`None`? feature OFF, field missing, learning data missing, insufficient window? ??? ? ??. ??/?? logic? ? ?? ?? ???? ?? ?? ??? formula/global facade? ???? ????.

## 3. ?? adapter ??

| ?? | ?? |
| --- | --- |
| `resolve_field(dict_findex, *names, required=False)` | market? ?? field name ?? |
| `build_market_info(market_gubun, is_tick, dict_findex)` | V3 analyzer? ???? `market_info` compatibility view ?? |
| `slice_window(arry_code, index, tick_count)` | ?? 2U_C slicing ?? ?? |
| `build_candle_window(code_data, dict_findex, is_tick)` | candle pattern? OHLC mapping |
| `load_learning_before(code, backtest_date, analyzer_kind)` | `last_update < backtest_date` ?? ?? ?? |
| `no_signal(reason)` | analyzer ??? ?? ??? ?? diagnostics? ?? |

## 4. analyzer? minimal fixture

| analyzer | ?? fixture |
| --- | --- |
| CandlePattern | min window 60 rows, `index/current/open/high/low` ?? |
| VolumeSpike | tick/min window 60 rows, price + volume/trade-value ?? |
| VolumeProfile | learned node 1? ?? ?? no-node case |
| VolatilityPattern | price window 60 rows |
| VolatilityStopTake | price window 60 rows + stop/take setting |
| Microstructure | tick orderbook 5-level + buy/sell volume |
| AnalyzerRisk | stock/coin tick/min ? 1? window |
| FormulaAdapter | analyzer output bundle? formula globals? ?? ?? OFF case, ?? ON case |
| StgGlobalsFacade | ?? global helper name ?? ?? facade case |

## 5. acceptance criteria

### DESIGN-2 acceptance

- [x] analyzer? input/output contract? ????? ??.
- [x] 2U_C Kiwoom factor list ?? field mapping? ????? ??.
- [x] adapter boundary? feature flag ?? OFF ??? ????? ??.
- [x] AnalyzerRisk dormant ?? ??? ????? ??.
- [x] runtime code? DB ??? ???? ???.

### IMPL-2A acceptance ??

- [ ] 2U_C? adapter skeleton ?? equivalent compatibility module? ????.
- [ ] ?? flag OFF ???? ?? backtest/realtime path import ??? ??? ???.
- [ ] AnalyzerRisk dormant smoke fixture? ????.
- [ ] `python -m py_compile` ??.
- [ ] forbidden artifact guard? ????.
- [ ] root docs? 2U_C implementation docs/registry? ????.

## 6. ?? ??

```text
- LS API ?? ??? ?? ??
- Kiwoom receiver/order path ?? ??
- core DB ?? ??
- `_database`, `_database_v3k_shadow`, `_log`, `*.db`, `backtest/graph` commit ??
- feature flag ??? ? ON ?? ??
- analyzer output?? ??/?? ??? ?? ?? ??
```

## 7. rollback ??

?? ? ???? ???? ?? ?? ??? rollback ?? feature flag OFF? ?? ????.

1. OFF ???? ?? 2U_C test/smoke ?? ??.
2. analyzer import? runtime import chain? ???.
3. DB write ?? runtime artifact ??.
4. Kiwoom broker path? LS ?? ??? ??.
5. analyzer output? ?? ?? ??/?? ??? ???.
