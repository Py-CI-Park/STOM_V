# V4 타입 피드백 실 A/B 파일럿 #1 결과 (2026-07-16)

사전등록: `2026-07-16_v4_typed_feedback_real_ab_preregistration.md`
판정은 사전등록된 지표·규칙만 사용했다.

## 1. 실행 기록

| 팔 | run_id | 세대 | 생성 성공 | 백테스트 실행 | 소요 |
|---|---|---|---|---|---|
| L (자유문) | `ab20260716_legacy_min` | 6 | **0/6** | 0회 | 370s |
| T (타입) | `ab20260716_typed_min` | 6 | **0/6** | 0회 | 381s |

양팔 모두 전 세대(재시도 3회 포함, 팔당 18회 시도)가 동일 사유로 차단됐다:
`human DB pattern-card anti-copy guard blocked generation: threshold_copy`

## 2. 사전등록 규칙 판정

- **R1 (붕괴 금지)**: 판정 불가 — 양팔 성공률 0으로 분모가 무의미. 붕괴의 원인은
  타입 피드백이 아니라 **생성 단계 상류의 가드 결함**이다.
- **R2 (오염 0)**: 관측 불가 — 피드백 소비 단계에 도달한 세대가 없다.
- **R3 (방향 기록)**: HOLDOUT 도달 세대 없음.
- **R4 (확대 조건)**: 미충족 — 파일럿 재실행이 선행되어야 한다.

**결론: 본 파일럿은 피드백 비교에 관해 무정보(uninformative)이며,
대신 본선 연구 프리셋을 완전히 마비시키는 결함 1건을 확정했다.**

## 3. 확정된 결함과 수정

- **결함**: `validate_pattern_card_usage`의 임계값 항목 해시가 문맥 없는
  **맨숫자(bare number)** 단위였다. few-shot 패턴카드와 생성식이 0, 1, 100,
  세션 시각(90000 등) 같은 보편 상수 하나만 공유해도 `threshold_copy`로 차단.
  특히 no-op 시간창 가드가 시간 리터럴 사용을 **강제**하므로, 두 가드의 결합으로
  `few_shot_enabled=True`인 연구 프리셋의 생성 성공률이 구조적으로 0이 된다.
- **수정** (`ee7cd8a1`): 항목 판정을 변수+연산자+정규화숫자 삼중항으로 교체,
  시간축(시분초·시간) 비교는 항목 판정에서 제외. 전체식 복사, 전체 숫자집합
  동일, 성과 수치 유입 차단은 유지.
- **회귀 테스트**: 시간 게이트 공유 허용, 타 변수 동일 숫자 허용,
  고유 임계값 삼중항 공유 차단, 뒤집힌 비교 차단 — 10 passed.

## 4. 후속

- 파일럿 #2를 동일 사전등록 설계로 재실행한다 (run_id 접미사 `b`).
- 사전등록 문서의 지표·규칙은 수정하지 않는다.
