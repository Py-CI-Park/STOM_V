# 진입 연장(09:20~25) + min 전 세션(~15:00) 발굴 로드맵 (2026-06-13 사전선언)

> 사용자 지시: ① 매수를 09:05가 아닌 09:20~25까지 ② min 데이터 15:00까지
> 전부 사용한 조건식 발굴 개발.

## 0. 실측 기반 (이 로드맵의 출발 증거)

W1 스윕 window_end 응답곡선(시드 기본 θ): 90700 **+9,660,666(+12%, MDD
16.21)** > 90500 +8.63M > 91000 +6.92M > 92000 +2.17M. 한계 버킷 부검
(window_end=92000 CSV): 09:00~05 +28.1k/건 · 09:05~10 −6.1k/건(보합권) ·
09:10~15 −16.2k/건 · 09:15~20 −21.8k/건. **결론: 연장 유효, 단 09:07
이후는 같은 임계로는 음(−) — 후반 전용 차등 임계가 본체.**

## T-트랙: tick 진입 연장 (목표 09:20~25)

| 단계 | 내용 | 게이트 |
|---|---|---|
| T1 (진행 중) | THETA θ × window_end {90700,91000,91500} 결합 배치 — 미시도 결합 | 같은 자 비교 |
| T2 | **3분기 차등 임계 구조**: 시드에 90500~{ext_end} 분기 추가, 후반 전용 축(거래대금폭발↑·회전율↑·매수공격↑·시가대비등락율 상향 — R1 알파 운반자만) | 신규 구조 → 2-분기 스모크 |
| T3 | 한계 거래 승자 부검: 92000 CSV의 09:05+ 승자(2,000여건 중 흑자 거래) 특성 분리(시총·등락율·폭발배수 분포) → T2 임계 설계 입력 | 분석 |
| T4 | T2 스윕(지도) → 선택·동결 → **v2 판정**(비열등·무붕괴) → 고정 OOS+V4 | n_trials 누적·OOS 횟수 공시 |

## M-트랙: min 전 세션 (진입 09:00~14:50, 강제청산 15:00)

기존 min 실패(니치 2종·LLM 2종)는 전부 '완성 구조'를 던진 것 — **프리미티브
단위의 시간대별 기대값 지도는 미시도**. 시드가 없는 공간은 지도부터.

| 단계 | 내용 | 게이트 |
|---|---|---|
| M1 | **진단 프리미티브 지도**: 6 프리미티브(신고가 돌파·이평 재탈환·거래대금 급증·체결강도 급등·전일동시간비 고활성·연속 양봉) × 6 시간밴드(09·10·11·13·14시·14:30+) = 36셀 단순 조건 배치 — "몇 시에 무엇이 작동하나" | train 2025-04~12만 사용(프로토콜) |
| M2 | 생존 셀(흑자 프리미티브×시간) 조합 → 미니 구조 2~3종 빌드 → 2-개월 스모크(2025-05+2025-09) | 스모크 규약 |
| M3 | 스윕 → P-A 동결(min 임계: 연 1+월 일관성) → min OOS(2026-01~02)+V4 롤링 | v2 판정 |
| M4 | LLM min 세대 재개 — M1 지도를 컨텍스트로 주입 | 교훈 자동 누적 |

청산 규약: 전 구조 공통 `force_exit 145900` + entry_end ≤ 144500.

## 다음 해야 할 것 (오늘 실행 순서)

1. T1 판독(~15분 후) → T3 승자 부검 → T2 빌드·스모크·스윕 (오늘 중)
2. M1 진단 지도 빌드+배치 (min은 빠름 — 즉시 병행)
3. ★V6(사용자) · LLM 2차(T/M 컨텍스트 탑재) · v2 게이트 코드화 후속
