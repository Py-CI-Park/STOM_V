# 알파 랩 v2 설계·사전등록 — 챔피언 조건부 개선 채굴 (2026-07-06)

> **한 줄**: v2 = "채굴이 찾은 규칙으로 **실전 챔피언의 수익을 개선**하고, 상위 리프를 힐클라임 시드로 환류"하는 사이클. v1이 반증한 두 가정(고정지평 라벨·단독 매수식)을 교체하고, v1이 검증한 두 자산(ρ=0.669 나침반·재현게이트 0.0pp 리플레이)을 재료로 쓴다.
> **원 목적 정합**: 이 프로그램의 존재 이유는 "기존 자율 루프(콜드 생성 0/88)가 시드 공급 병목으로 성과가 없어, **STOM으로 실수익을 내는 좋은 조건식**을 데이터-우선으로 공급하는 것"(연구 프로그램 §1). 저장소에서 유일하게 검증된 개선 경로는 **검증된 챔피언 + 힐클라임**(rr8_12 계보 4/4 OOS)이며, §7은 처음부터 "산출물 = 힐클라임 시드 환류"를 명시했다. v2는 그 경로에 직결한다.
> **브랜치**: `research/alpha-lab-v2-20260706` (v1 브랜치에서 분기, 같은 워크트리). 산출물: `research_runs/alpha_lab_v2_20260706/`.

---

## 1. v1 → v2에서 바뀌는 것 / 유지되는 것

| 요소 | v1 | v2 | 근거 |
|---|---|---|---|
| **라벨** | 고정지평 net_rate(60/180/300초) | **L3_replay: 챔피언 현직 매도식 전체 절을 리플레이한 실현 net_rate** | v1 교훈 2(포지션 동학 갭). 신뢰 근거 = P5 재현게이트(오차 중앙 0.0pp·1틱내 99.7%) |
| **산출 형태** | 리프 = 단독 매수식 | **(a) 챔피언 매수식 AND 리프절 = 필터형** + **(b) 상위 리프 = 힐클라임 시드 등재** | v1 교훈 1(단독 전멸·순위 나침반 유효), §7 원 취지 |
| **엔진 검증** | ρ 순위 상관 | **챔피언 A/B 개선 판정**(대조군 = 기존 실측 재사용) | 목표가 "순위"가 아니라 "실수익 개선"이므로 |
| 표본·그리드 | 60일·moneytop·stride5·t0≤09:25 | **동일**(v1 windows.mvp 일자 그대로) | 비교 가능성 |
| 채굴기·통계·번역·봉인·CLI | — | **전량 재사용** | v1 인프라 자산 |

## 2. 라벨 L3_replay 조작 정의 (봉인)

- entry = (t0+1초) 매도호가1 + 2틱 불리 (v1 비용 모형 동일)
- 경로: t0+1 이후 관측 초들에 대해 **챔피언 현직 매도식 원문**(GATE_rr8_12_turnover_min_902_1_5_S, sha256 `8ef01e0e…f911c07`) 전체 절 평가 — alpha_lab.distill.replay 재사용. 매도 발화 시 exit = 그 초 매수호가1 − 2틱.
- 상한: 09:30:00 도달 시 강제 청산(그 초 매수호가1 − 2틱).
- 라벨: `L3_net`(연속) + `L3_pos = (L3_net ≥ +0.01)` — 채굴은 L3_pos.
- 결측: entry 초 결측/호가≤0 → 표본 제외. 경로 결측 초는 건너뜀(관측 초만 평가 — 리플레이 게이트와 동일 컨벤션).

## 3. 채굴 (v1 mining_spec 승계, 라벨만 교체)

depth≤4 · min_samples_leaf≥2000 · seed **20260706** · 서브셋 전체25×1+무작위12피처×4 · 채택 lift≥1.5 ∧ support≥2000 ∧ 연도별(2023·2024) lift>1 · 일블록 부트스트랩 1000 · BH-FDR q=0.05 · 교차창(2023↔2024) 병행. n_trials 태그 **V2M**(리프 전수).

## 4. 필터 A/B (엔진, 봉인 예산 ≤30회 + OOS S+1회)

- **후보**: 채택 리프 중 mined lift 내림차순 상위 **F=6**(동률 시 support desc→규칙 json asc).
- **페어**: buy = `(챔피언 매수식 원문) and (리프절)` — 챔피언 시간 구조·유니버스 동작 보존을 위해 그 외 어떤 절도 추가하지 않는다. sell = 챔피언 현직 매도식 원문(A와 동일). 등재명 `ALP_V2F_01~06`.
- **대조군 A(재실행 0회)**: loop_runs.db `alp_exitchk_warm64_*` 실측 재사용 —
  full **+15,284,154 / MDD 7.14 / 665거래**, y2022 +4,130,117/9.19/101, y2023 +5,649,359/6.98/197, y2024 +2,852,246/13.58/156, y2025 +3,062,696/12.87/190.
- **실행**: F=6 × 5창(full+4개년) = **30회**.
- **성공 필터 판정(봉인 확정값)** — 아래 (우월 ∨ 위험개선) ∧ 공통하한:
  - 우월: full.profit > A.full.profit ∧ full.mdd ≤ A.full.mdd
  - 위험개선: full.mdd ≤ A.full.mdd − 2.0pp ∧ full.profit ≥ 0.85 × A.full.profit
  - 공통하한: full.trades ≥ 0.4 × A.full.trades(=266) ∧ 연도 4창 중 ≥3에서 profit > 0
- **OOS**: 성공 필터만 validation_sealed(2025-01~2026-02) 각 1회 + A 대조 1회(미실측이므로). OOS에서도 (우월 ∨ 위험개선) 유지 시 승격 후보.

## 5. 시드 환류 (실행 아님, 등재 기록만)

채택 리프 상위 10을 provenance(리프 절·lift·support·연도 재현·V2M 문맥)와 함께 시드 풀 등재 기록(bridge receipts) — 힐클라임 소비는 본선(wt-dev) 루프 소관, 머지 후.

## 6. v2 전체 판정 (봉인)

- **성공**: OOS까지 통과한 개선 필터 ≥1 → 승격 후보 관리자 보고(공식 게이트 체인·can_promote 절차는 본선 규율).
- **부분 성공**: 필터 전멸이어도 시드 등재 자산은 유지(§7 경로 유효).
- **포기**: 전 필터 A 미달 → v3 방향으로 이월. **v3 후보(사전 명시)**: (a) 시드-힐클라임 위임 집중(기존 검증 경로에 채굴 시드 대량 공급), (b) 레짐/포트폴리오 레이어(일 단위 가동 스케줄 — 단 제안 4의 표본 한계 유의), (c) 매도측 조건부 채굴(청산 시점 상태 조건 — P5 기각과 구분: 정책 격자가 아니라 상태 조건).

## 7. 무결성

모든 임계·선정 규칙은 본 문서와 `preregistration_v2.json`(sha 커밋)에 봉인 — 측정 후 변경은 새 n_trials로만. v1 원장·봉인은 불변. 대조군 A 수치는 loop_runs.db 실측을 봉인에 동결(사후 재측정으로 기준 이동 금지).
