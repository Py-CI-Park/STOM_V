# S-트랙 사전등록 V2-A — 라벨-only 재검정 (2026-07-11)

> 지위: **측정 전 봉인.** 이 문서 커밋 후에만 V2-B(파일럿)·V2-C(게이트)를 실행한다. 사후 변경은 새 사전등록으로만.
> 상위: 마스터 플랜 v2 §9(감사 반영 — 구속력), 감사 `2026-07-11_alpha_restart_research_validity_audit.md` §8, W3-R `research_runs/alpha_restart_20260710/w3r_boundary_reseal.md/.json`, 창-지위 원장(위반 V-1 포함).
> 쉬운 설명: 지도의 **눈금(칸 경계·세금)** 은 감사에서 적발된 오류를 고친 값으로 바로잡고, **자(라벨)** 하나만 바꿔서 — "300초 뒤" 대신 "챔피언 매도식대로 팔았을 때" — 우승자의 사냥터가 지도에서 밝게 보이는지 검정한다. 갭·돌파 등 다른 변경은 이 라운드에서 금지(순차 ablation).

---

## 1. 기반 교정 (실험 변수 아님 — 오류 수정이므로 전 라벨에 공통 적용)

| 항목 | 봉인 값 | 근거 |
|---|---|---|
| 등락율 축 경계 | **1.59 / 3.56 / 6.88** (발견창 2022-03-23~2023-12-31 전량 437일, L0 32,624,426 — 표본일 목록은 w3r json에 명시) | W3-R. 오염 경계(1.71/3.47/6.70)와의 차이표·연도 드리프트는 w3r 문서 공개. 유리한 쪽 선택 없음(재산정값 그대로) |
| 매도세율 (연도 키) | **2022 = 0.23%, 2023 = 0.20%** (발견창). 참고: 2024=0.18, 2025=0.15, 2026=0.20(2026-01-01 시행령) — 미래창 적재 시 해당 연도 키 적용 | W3-R 작업2 (law.go.kr lsiSeq=282431 포함). KOSPI/KOSDAQ 합계 동률이라 시장 구분 불요 |
| 수수료·불리체결 | 0.015%×2, 진입/청산 각 2틱(기존 봉인 유지) | 변경 없음 |
| 기타 축·온셋·창 | v1 봉인 유지: 시간대 5분×6, 시총 3구간, L1 서지 온셋(2.0×, 쿨다운 30초), 발견창만 | **라벨-only 원칙** — 갭 축·돌파 온셋 편입 금지(O-1/O-3에서 별도) |

## 2. 실험 변수 — 라벨 2종 병렬 산출 (동일 L1 온셋 표본)

| 라벨 | 정의 | 역할 |
|---|---|---|
| h300(교정판) | 고정 300초 뒤 청산, §1 교정 비용 | 대조군 — 라벨 효과 분리용 |
| **L3_replay** | **RR8_12 계열 검증 매도식 리플레이 실현 순손익**(진입 매도호가1+2틱, 청산 리플레이 체결가-2틱, §1 연도 세율) — `alpha_lab/dataset/labels_v2.py`·`distill/replay.py` 재사용 | 검정 대상. **"RR8_12 출구에 조건부인 라벨"임을 모든 산출물에 명기** — 다른 출구면 다른 지도 |

## 3. V2-B 파일럿 리플레이 재현 게이트 (감사 §8.4 채택)

파일럿 창 1개월(2022-03-23~04-30) L1 온셋 대상. **전 기준 동시 충족 필수**:

| 기준 | 문턱 |
|---|---|
| 청산 시각·가격 동시 일치율 | ≥ 99.9% (기존 labels_v2 코드 기준) |
| 수익률 오차 중앙값 | 기존 봉인 기준 이내 (0.0%p) |
| 극단 오차 | p99 ≤ 0.10%p, 최대 오차 별도 보고 |
| 불일치 거래 | 원인 분류(체결 컨벤션/절단/데이터 결측)와 대표 사례 기록 |
| 미달 시 | 벡터 경로 금지 → 검증된 순수 경로로 재시도, 그것도 미달이면 **라벨 무효·V2 중단** |

## 4. V2-C 챔피언 게이트 v2 — family vote (감사 §8.3 채택)

표는 2개뿐이다: **RR8 가문**(RR8_0·21·12 — 상관 0.84~0.93이므로 1표, 내부 3전략 민감도는 병기)과 **GPTAUTH 가문**(1표).

| 항목 | 봉인 기준 |
|---|---|
| 판정량 | 가문 점유 칸(발견창 실거래 진입 사상 — v1 파이프라인, 사상 오차 0 검증됨)의 **L3 mean − 전체 L1 L3 mean** |
| 가문 통과 | 개선폭 ≥ **+0.10%p** ∧ 일자 블록 부트스트랩(n_boot 400) **CI 하한 > 0** |
| 약신호 대역 | +0.05~0.10%p ∧ CI>0 은 "약신호"로 보고만(통과 아님) |
| 해석 사다리 | RR8 가문만 통과 = **positive control**("RR8_12 출구 조건부 지도 작동") / 양 가문 통과 = "제한적 일반화 신호" / 0 가문 = 아래 kill |
| 대조군 보고 | 동일 판정을 h300(교정판)으로도 산출·병기 — 라벨 효과의 분리 증거 |
| kill (감사 §8.2 폐기 범위 한정) | 0 가문 통과 시 **"현재 축·서지 온셋·RR8_12 출구 조건부의 칸-조준"을 폐기** (칸 방법론 전체 폐기 아님). 함정 설명 지도 자산은 유지 |

## 5. 시행·원장 규율

- 본 게이트 판정 = n_trials **type b 1건**(가문 2표는 1판정의 구성요소). 축·라벨·게이트 설계 변경(본 문서)은 적응적 설계 결정으로 원장에 기록.
- known 2025 데이터 접촉 없음(발견창 전용). 구 프로그램 누계(1,100+) 병기 원칙 준수.
- 산출물: v2 빌드 영수증(입력·sha), 파일럿 리포트, 게이트 리포트(가문별·내부 민감도·대조군 병기), 전부 `research_runs/alpha_restart_20260710/`에. stats_map.db 파티션은 v1과 분리(빌드ID `v2a_*`), git 제외 유지.
- 구현: 비용 상수는 단일 출처(config)에서 연도 키로 전파 — labels/costs 지점 간 패리티 테스트 필수. 엔진 백테 0회, 원본 read-only.

## 6. 이 라운드에서 하지 않는 것 (즉시 금지 재확인)

갭 축·돌파 온셋 편입(O-1/O-3 별도 봉인), S-v1 지도의 하드 veto 사용, RR8 3종 독립 3표, 전당 19종 절 파싱(H-0 출처 게이트 전), O-5 엔진 스윕.
