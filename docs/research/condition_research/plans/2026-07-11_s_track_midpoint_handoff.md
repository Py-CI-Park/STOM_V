# S-트랙 중간 핸드오프 — W0~W4 완료·kill-2 판정·다음 라운드 진입 (2026-07-11)

> 대상 독자: 후속 AI code agent 및 사용자.
> 목적: 2026-07-10~11 실행 라운드(계획 수립 → 규율 → 전당 랭킹 → 지도 구축 → 게이트 판정)의 중간 기록. 새 세션이 이 문서 → 마스터 플랜 v2 → 창-지위 원장 순으로 읽으면 즉시 재개 가능해야 한다.
> 현행 작업 기준: **`2026-07-11_alpha_restart_master_plan_v2.md`** (WBS v2가 유일한 착수 목록이다).

---

## 0. 한 줄 상태

**S-트랙 v1은 "정확하게 그려졌지만 자(라벨)가 얕아 챔피언 엣지를 못 보는 지도"로 판정(kill-2, 0/4)** — 함정/veto 자산으로 전환 보존. 다음 라운드는 ① 자를 바꾼 v2(L3 리플레이 라벨) ② 전당-시드 트랙(인간 19종) ③ 신설 O-트랙(시초 갭·돌파·소수 정예 큰 베팅, 사용자 아이디어 R12)의 3갈래이며, 전부 엔진 0회로 시작한다.

## 1. 이번 라운드 커밋 체인 (전부 이 브랜치 `research/alpha-lab-idea5-foundation-20260707`)

| 커밋 | 내용 |
|---|---|
| `703ccbcc` | 마스터 계획 v1 + 프로그램 정본 v4 + 핸드오프 v4 + 증거 JSON 4종 (검토 추적 R1~R11) |
| `0608043c` | W1a 창-지위 원장 + 전역 n_trials 규약 봉인 (분석보다 선행 — git 순서 증명) |
| `721db080` | W1b~d 규율 3종 + **W2 전당 랭킹**(5,482전략 — 인간 19종 우위 확인) |
| `0b8dcd43` | W3 프로파일링(148만 표본) + S-트랙 사전등록 v1 봉인 |
| `4a7ae6c0` | W4-1 파일럿(게이트 4/4) + 코드 10모듈 + 테스트 11 |
| `3a9b7843` | W4-3 본 적재(437일, 3,262만) + **W4-2b 챔피언 게이트 0/4 실패(kill-2) 기록** + n_trials 원장 개시 |
| (본 커밋) | 마스터 플랜 v2(O-트랙 신설·S-트랙 v2 재설계) + 본 중간 핸드오프 |

> **감사 우선 원칙(2026-07-11 추가)**: 타당성 감사 `2026-07-11_alpha_restart_research_validity_audit.md`(`0f89796e`)와 마스터 플랜 v2 §9(감사 반영)가 본 문서의 서술과 상충하면 감사의 보수적 판정이 우선한다. 특히: ① 아래 §2-1·2의 "함정/veto 지도"는 **기술통계·함정 설명 지도**(하드 veto 금지)로, "엣지는 조합+출구"는 **미증명 가설(C등급)**로 읽는다 ② §2-3 전당 19종은 원문·동일 엔진 재현 전까지 **외부 참고 벤치마크**다(H-0 출처 게이트) ③ W3 경계 오염(위반 V-1)으로 W4 v1은 "오염 경계 기술통계 기준선"이며 W3-R 재산정이 선행한다 ④ 실행 순서는 마스터 플랜 §9.2가 정본이다.

## 2. 확정 판정 (후속 세션이 뒤집으면 안 되는 것)

1. **S-트랙 v1 지도의 조준 용도는 무효** — 고정지평(h60/180/300) 라벨 기준 챔피언 점유 칸의 L1 mean_net(-1.15~-1.21%)이 전체 평균(-1.04%)보다 전원 열위(사상 스팟체크 오차 0으로 지도 결함 아님). 발견창 전체 양EV 칸 0. **함정 지도·veto 자산으로만 사용.**
2. **엣지의 소재**: 칸(시간대×등락율×시총)이 아니라 미세 절 조합 + 출구(청산) — RC-0 계보의 신규 정량 증거. v2 가설은 "L3 리플레이 라벨이면 보인다"(마스터 플랜 §4).
3. **전당 기준선**: 인간 19종(연 134~262%, 일 10.6~23.2거래) — 엔진 최고(연 81.9%)를 전원 상회. 후보 시드의 1차 원천. 창 불일치 한계(인간 2022-23 in-sample) 명기 유지.
4. 기존 대원칙 전부 유지: 수집 없음·매수1+매도1·총수익 1차(MDD는 킬스위치 제약)·전당 기준선·원장 선행·known 창 veto 전용·쉬운 설명 의무.

## 3. 현재 자산 상태

| 자산 | 위치 | 상태 |
|---|---|---|
| 지도 DB (v1, 함정/veto) | `research_runs/alpha_restart_20260710/stats_map/stats_map.db` (git 제외) | 발견창 437일 적재 완료, summary_sha 26e8254a. 재생성: `STOM_ALLOW_MINIMAL_SETTING=1 python scripts/s_track_build_main.py` |
| 지도 코드 | `alpha_lab/stats_map/` 12모듈 + `scripts/s_track_*.py` 4종 + 테스트 11 | 전부 커밋·테스트 green. 게이트 러너: `scripts/s_track_champion_gate.py` |
| 전당 랭킹 | `research_runs/alpha_restart_20260710/w2_strategy_inventory.md/.json` | 인간 19종 목록·지표 포함 |
| 판정 증거 | 같은 폴더 `w4_full_build.json`·`w4_champion_overlay.json`·`w4_pilot_*` | 게이트 수치 전부 |
| 시행 장부 | 같은 폴더 `n_trials_ledger.jsonl` | 1행(챔피언 게이트, type b) — 이후 전 시행 append 의무 |
| 재사용 예정 | `alpha_lab/dataset/labels_v2.py`(L3 리플레이 라벨), `alpha_lab/distill/replay.py`(P5 재현게이트 통과 이력) | v2 라벨 레이어의 핵심 |

## 4. 다음 착수 목록 (마스터 플랜 v2 §6의 WBS가 정본 — 요약)

1. **V2-1** S-트랙 v2 사전등록 작성·커밋 (L3 라벨·게이트 v2·갭 축·돌파 온셋 봉인) — O-1 갭 스팟 결과를 반영해 작성
2. **V2-2→V2-3** L3 파일럿(리플레이 재현 게이트) → 본 적재 → **챔피언 게이트 v2** (결정적 판정)
3. 병렬: **O-2** 전당·챔피언 원문 갭/돌파 절 사전 / **H-1** 전당-시드 설계 / **W6a** give-back 포렌식 / **W6d** API 필터 / **W7** 대시보드 요구사항
4. 이후: O-4 후보 봉인 → (U-7 엔진 승인) O-5 베팅 스윕

## 5. 재개 루틴 (새 세션)

1. 본 문서 → `2026-07-11_alpha_restart_master_plan_v2.md` → `2026-07-10_window_status_ledger.md` 순으로 읽는다.
2. `git log --oneline -8`로 §1 커밋 체인 존재 확인. 브랜치 확인.
3. 착수 전 확인: 해당 작업이 WBS v2에 있고, 창-지위 원장 위반이 없고, 측정 전 봉인(사전등록)이 필요한 작업인지 구분.
4. 모든 수치 인용은 `research_runs/alpha_restart_20260710/` JSON에서. 모든 보고에 쉬운 설명(비유) 동반.
5. 금지: 기존 핸드오프 v4 §6(금지 15종) + kill-2 판정 번복(§2-1) + 시행의 원장 미기록.

## 6. 쉬운 설명 (사용자 보고용 요약)

- 지도 v1: "정밀하게 그렸는데, 자가 '300초 뒤'라서 챔피언의 비밀(출구·조합)이 안 보였다. 지도는 '함정 표시판'으로는 훌륭하다."
- 다음 라운드: 자를 '챔피언처럼 팔았을 때'로 바꿔 다시 재고(v2), 사람 최고 전략 19종을 시드로 올리고(전당-시드), 사장님 아이디어대로 시초 갭·돌파·소수 정예 큰 베팅(O-트랙)을 같은 지도 인프라로 측정한다.
- 셋 다 엔진(백테스트 예산) 없이 시작하고, 엔진은 후보가 압축된 뒤 한 번씩만 쓴다.
