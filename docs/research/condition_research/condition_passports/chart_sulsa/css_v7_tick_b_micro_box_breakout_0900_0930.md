# Condition Passport — CSS_V7_TICK_B_MICRO_BOX_BREAKOUT_0900_0930

| Field | Value |
|---|---|
| condition_id | `CSS_V7_TICK_B_MICRO_BOX_BREAKOUT_0900_0930` |
| human_name | `1tick 매수: 미세 박스 상단 돌파` |
| role | `hypothesis_seed` |
| lane | `tick` |
| side | `buy` |
| principle_ids | `P1, P3, P13` |
| time_window | `09:00:00-09:30:00` |
| buy_strategy_id | `CSS_V7_TICK_B_MICRO_BOX_BREAKOUT_0900_0930` |
| sell_strategy_id | `CSS_V7_TICK_S_MASTER_0900_0930` |
| buy_code_sha256 | `5e1f7b2e5e94741a7a9f3b2bc017f93a63bcae443093ac4bc347d850cab26831` |
| sell_code_sha256 | `68bedcf47100da014064336f80e31451247902aefba2e2f6e617880de9f7331b` |
| oos_status | `none` |
| prior_profit | `none` |
| prior_mdd | `none` |
| prior_trades | `none` |
| promotion_status | `research_only / not_promoted` |
| source | `chart_sulsa_v7_0` (원문 4.1절) |

## Core hypothesis

이 passport는 차트술사 구조론 보고서 v7.0에서 추출한 **무근거 가설 시드**다.
관련 원리: P1, P3, P13. 이 조건식 자체는 `buy` 측이며, 반대 측 코드는 같은 레인의
통합(MASTER) 조건식으로 페어링했다. 모든 임계값은 백테스트로 검증되지 않은
가설이며 검증된 사실로 취급하면 안 된다. oos_status=`none` — OOS 검증 이력이 없다.

## Buy condition full code

```python
# =============================================================================
# CSS_V7_TICK_B_MICRO_BOX_BREAKOUT_0900_0930
# 1tick 전용: 09:00~09:30 미세 박스 상단 돌파 매수
# 차트술사 대응: 박스 밖 추세매매 + 사건 거래대금 + 체결강도 확인
# =============================================================================
매수 = False

if 데이터길이 >= 90 and 90000 <= 시분초 < 93000 and 관심종목 == 1:
    박스틱수 = 30
    평균틱수 = 30
    박스상단 = 최고현재가(박스틱수, 1)
    박스하단 = 최저현재가(박스틱수, 1)
    박스폭율 = ((박스상단 - 박스하단) / 박스하단) * 100 if 박스하단 > 0 else 999
    돌파율 = ((현재가 - 박스상단) / 박스상단) * 100 if 박스상단 > 0 else -999
    거래대금비율 = 초당거래대금 / 초당거래대금평균(평균틱수) if 초당거래대금평균(평균틱수) > 0 else 0
    체결강도비율 = 체결강도 / 체결강도평균(평균틱수) if 체결강도평균(평균틱수) > 0 else 0
    호가압력 = 매수총잔량 / (매수총잔량 + 매도총잔량) if (매수총잔량 + 매도총잔량) > 0 else 0.5
    순매수수량 = 초당매수수량 - 초당매도수량

    if 1000 <= 현재가 <= 100000 and 2 <= 등락율 <= 25 and 현재가 < VI가격 and not 라운드피겨위5호가이내:
        if 박스폭율 <= 1.2 and 돌파율 >= 0.25 and 거래대금비율 >= 2.0 and 체결강도 >= 110 and 체결강도비율 >= 1.05:
            if 순매수수량 > 0 and 호가압력 >= 0.45 and 초당매수수량 >= 초당매도수량 * 1.1:
                매수 = True

if 매수:
    self.Buy(종목코드, 종목명, 매수수량, 현재가, 매도호가1, 매수호가1, 데이터길이)
```

## Sell condition full code

```python
# =============================================================================
# CSS_V7_TICK_S_MASTER_0900_0930
# 1tick 통합 매도식: 09:30 종료 + 구조 무효화 + 도지 하단 이탈 + 트레일링
# =============================================================================
매도 = False

if 시분초 >= 93000:
    매도 = True
elif 등락율 >= 29.5:
    매도 = True
elif 수익률 <= -2.2:
    매도 = True
elif 데이터길이 >= 60:
    체결강도비율 = 체결강도 / 체결강도평균(30) if 체결강도평균(30) > 0 else 0
    매도우위 = 초당매도수량 > 초당매수수량 * 1.4 and 체결강도비율 <= 0.9
    호가하락압력 = 매수총잔량 / (매수총잔량 + 매도총잔량) if (매수총잔량 + 매도총잔량) > 0 else 0.5
    매수가기능선이탈 = 현재가 < 매수가 * 0.992
    단기저점이탈 = 보유시간 >= 10 and 현재가 < 최저현재가(20, 1) * 0.998
    미세중심 = 이동평균(30, 1)
    미세하단 = 최저현재가(30, 1)
    도지하단이탈 = 현재가 < 미세하단 * 0.997 and 현재가 < 미세중심 and 매도우위
    트레일링 = 최고수익률 >= 1.5 and 수익률 <= 최고수익률 * 0.45
    시간정체 = 보유시간 >= 240 and 수익률 < 0.3

    if 매수가기능선이탈 and (매도우위 or 호가하락압력 <= 0.35):
        매도 = True
    elif 단기저점이탈 and 매도우위:
        매도 = True
    elif 도지하단이탈:
        매도 = True
    elif 현재가 < 이동평균(30) * 0.995 and 체결강도비율 <= 0.9:
        매도 = True
    elif 트레일링 or 시간정체:
        매도 = True

if 매도:
    self.Sell(종목코드, 종목명, 매도수량, 현재가, 매도호가1, 매수호가1, 강제청산)
```
