# chart_sulsa v7 조건식 코드 부록 — 1tick (09:00~09:30) 레인

> 상태: `hypothesis_seed` | 생성일: 2026-07-02 | 연구 레인 전용 (승격/export/live 사용 금지)
> 원천 문서: `C:/System_Trading/chart_sulsa_stom_quant_insight_report_v7_0.html`
> 원천 문서 sha256: `454715a9faad0f6efcb0f24c12ad7dad0087a3d51afc5f9935c6d49c4b78f5d4`
> 데이터 원천: `ai_strategy_loop/brain/data/chart_sulsa_v7_conditions.json` (코드 재추출 없이 원본 수록, sha256 일치 검증 완료)
> 마스터 카탈로그: [2026-07-02_chart_sulsa_v7_condition_catalog.md](2026-07-02_chart_sulsa_v7_condition_catalog.md)

무근거 가설 시드: 모든 조건식/조합과 임계값은 차트술사 구조론 보고서 v7.0에서 추출한 검증되지 않은 가설이다. 백테스트/OOS 검증 이력이 없으며 검증된 사실로 취급하면 안 된다. 연구 레인 전용 (승격/export/live 사용 금지).

## CSS_V7_TICK_B_MICRO_BOX_BREAKOUT_0900_0930

- 한국어명: 1tick 매수: 미세 박스 상단 돌파 | lane/side: `tick/buy` | time_window: `09:00:00-09:30:00` | 원천 섹션: §4.1
- code sha256: `5e1f7b2e5e94741a7a9f3b2bc017f93a63bcae443093ac4bc347d850cab26831` | DB 저장명: `CSS_V7_TICK_B_MICRO_BOX_BREAKOUT_0900_0930` | passport: [css_v7_tick_b_micro_box_breakout_0900_0930.md](../condition_passports/chart_sulsa/css_v7_tick_b_micro_box_breakout_0900_0930.md)

```python
# =============================================================================
# CSS_V7_TICK_B_MICRO_BOX_BREAKOUT_0900_0930
# 1tick 전용: 09:00~09:30 미세 박스 상단 돌파 매수
# 차트술사 대응: 박스 밖 추세매매 + 사건 거래대금 + 체결강도 확인
# =============================================================================
매수 = False

if 데이터길이 >= 90 and 90000 <= 시분초 < 93000 and 관심종목 == 1:
    박스틱수 = 30
    평균틱수 = 30
    박스상단 = 최고현재가(박스틱수, 1)
    박스하단 = 최저현재가(박스틱수, 1)
    박스폭율 = ((박스상단 - 박스하단) / 박스하단) * 100 if 박스하단 > 0 else 999
    돌파율 = ((현재가 - 박스상단) / 박스상단) * 100 if 박스상단 > 0 else -999
    거래대금비율 = 초당거래대금 / 초당거래대금평균(평균틱수) if 초당거래대금평균(평균틱수) > 0 else 0
    체결강도비율 = 체결강도 / 체결강도평균(평균틱수) if 체결강도평균(평균틱수) > 0 else 0
    호가압력 = 매수총잔량 / (매수총잔량 + 매도총잔량) if (매수총잔량 + 매도총잔량) > 0 else 0.5
    순매수수량 = 초당매수수량 - 초당매도수량

    if 1000 <= 현재가 <= 100000 and 2 <= 등락율 <= 25 and 현재가 < VI가격 and not 라운드피겨위5호가이내:
        if 박스폭율 <= 1.2 and 돌파율 >= 0.25 and 거래대금비율 >= 2.0 and 체결강도 >= 110 and 체결강도비율 >= 1.05:
            if 순매수수량 > 0 and 호가압력 >= 0.45 and 초당매수수량 >= 초당매도수량 * 1.1:
                매수 = True

if 매수:
    self.Buy(종목코드, 종목명, 매수수량, 현재가, 매도호가1, 매수호가1, 데이터길이)
```

## CSS_V7_TICK_B_MICRO_RETEST_SUPPORT_0900_0930

- 한국어명: 1tick 매수: 미세 리테스트 지지 | lane/side: `tick/buy` | time_window: `09:00:00-09:30:00` | 원천 섹션: §4.2
- code sha256: `a5d1e54f5be1195b683bc8efd76cfa4bbd305b90872387b3d95843cb4549a5e4` | DB 저장명: `CSS_V7_TICK_B_MICRO_RETEST_SUPPORT_0900_0930` | passport: [css_v7_tick_b_micro_retest_support_0900_0930.md](../condition_passports/chart_sulsa/css_v7_tick_b_micro_retest_support_0900_0930.md)

```python
# =============================================================================
# CSS_V7_TICK_B_MICRO_RETEST_SUPPORT_0900_0930
# 1tick 전용: 미세 저항 돌파 후 리테스트 지지 전환 매수
# 차트술사 대응: 저항→지지 스위치, 돌파 후 공포 구간 매수
# =============================================================================
매수 = False

if 데이터길이 >= 120 and 90000 <= 시분초 < 93000 and 관심종목 == 1:
    기준틱수 = 60
    돌파확인틱수 = 20
    평균틱수 = 30
    기준상단 = 최고현재가(기준틱수, 돌파확인틱수)
    기준하단 = 최저현재가(기준틱수, 돌파확인틱수)
    기준폭율 = ((기준상단 - 기준하단) / 기준하단) * 100 if 기준하단 > 0 else 999
    최근돌파율 = ((최고현재가(돌파확인틱수, 1) - 기준상단) / 기준상단) * 100 if 기준상단 > 0 else -999
    현재위치율 = ((현재가 - 기준상단) / 기준상단) * 100 if 기준상단 > 0 else -999
    거래대금비율 = 초당거래대금 / 초당거래대금평균(평균틱수) if 초당거래대금평균(평균틱수) > 0 else 0
    체결강도비율 = 체결강도 / 체결강도평균(평균틱수) if 체결강도평균(평균틱수) > 0 else 0
    호가압력 = 매수총잔량 / (매수총잔량 + 매도총잔량) if (매수총잔량 + 매도총잔량) > 0 else 0.5

    if 1000 <= 현재가 <= 100000 and 1 <= 등락율 <= 24 and 현재가 < VI가격 and not 라운드피겨위5호가이내:
        if 기준폭율 <= 1.8 and 최근돌파율 >= 0.5 and -0.35 <= 현재위치율 <= 0.45:
            if 0.6 <= 거래대금비율 <= 4.0 and 체결강도 >= 100 and 체결강도비율 >= 0.95 and 호가압력 >= 0.45:
                if 현재가 >= 기준상단 * 0.996 and 초당매수수량 >= 초당매도수량:
                    매수 = True

if 매수:
    self.Buy(종목코드, 종목명, 매수수량, 현재가, 매도호가1, 매수호가1, 데이터길이)
```

## CSS_V7_TICK_B_MICRO_DOJI_COMPRESSION_0900_0930

- 한국어명: 1tick 매수: 미세 도지 압축 돌파 | lane/side: `tick/buy` | time_window: `09:00:00-09:30:00` | 원천 섹션: §4.3
- code sha256: `77265a7c2f8d22ba44f7533d1b134fcc76f857950d1432dc06393b96faffde99` | DB 저장명: `CSS_V7_TICK_B_MICRO_DOJI_COMPRESSION_0900_0930` | passport: [css_v7_tick_b_micro_doji_compression_0900_0930.md](../condition_passports/chart_sulsa/css_v7_tick_b_micro_doji_compression_0900_0930.md)

```python
# =============================================================================
# CSS_V7_TICK_B_MICRO_DOJI_COMPRESSION_0900_0930
# 1tick 전용: 도지 대체 정의 = 미세 횡보 압축 후 상단 돌파
# 차트술사 대응: 도지는 하위 프레임 박스의 압축이라는 원리 적용
# =============================================================================
매수 = False

if 데이터길이 >= 90 and 90000 <= 시분초 < 93000 and 관심종목 == 1:
    압축틱수 = 30
    평균틱수 = 30
    도지상단 = 최고현재가(압축틱수, 1)
    도지하단 = 최저현재가(압축틱수, 1)
    도지중심 = 이동평균(압축틱수, 1)
    압축폭율 = ((도지상단 - 도지하단) / 도지중심) * 100 if 도지중심 > 0 else 999
    중심괴리율 = abs(현재가N(1) - 도지중심) / 도지중심 * 100 if 도지중심 > 0 else 999
    돌파율 = ((현재가 - 도지상단) / 도지상단) * 100 if 도지상단 > 0 else -999
    거래대금비율 = 초당거래대금 / 초당거래대금평균(평균틱수) if 초당거래대금평균(평균틱수) > 0 else 0
    체결강도비율 = 체결강도 / 체결강도평균(평균틱수) if 체결강도평균(평균틱수) > 0 else 0

    if 1000 <= 현재가 <= 100000 and 1 <= 등락율 <= 25 and 현재가 < VI가격:
        if 압축폭율 <= 0.8 and 중심괴리율 <= 0.25 and 돌파율 >= 0.2:
            if 거래대금비율 >= 1.8 and 체결강도 >= 115 and 체결강도비율 >= 1.05 and 초당매수수량 > 초당매도수량:
                매수 = True

if 매수:
    self.Buy(종목코드, 종목명, 매수수량, 현재가, 매도호가1, 매수호가1, 데이터길이)
```

## CSS_V7_TICK_B_MASTER_0900_0930

- 한국어명: 1tick 통합 매수식 | lane/side: `tick/buy` | time_window: `09:00:00-09:30:00` | 원천 섹션: §4.4
- code sha256: `ba3421962481b8ad895794c11bc75e912dada2161bf2a13f940c71a40b816f25` | DB 저장명: `CSS_V7_TICK_B_MASTER_0900_0930` | passport: [css_v7_tick_b_master_0900_0930.md](../condition_passports/chart_sulsa/css_v7_tick_b_master_0900_0930.md)

```python
# =============================================================================
# CSS_V7_TICK_B_MASTER_0900_0930
# 1tick 통합 매수식: 미세 박스 돌파 + 리테스트 + 도지 압축 상방 돌파
# 운영 시간: 09:00:00 ~ 09:30:00 전용
# =============================================================================
매수 = False

if 데이터길이 >= 120 and 90000 <= 시분초 < 93000 and 관심종목 == 1:
    박스틱수 = 30
    기준틱수 = 60
    돌파확인틱수 = 20
    평균틱수 = 30

    미세상단 = 최고현재가(박스틱수, 1)
    미세하단 = 최저현재가(박스틱수, 1)
    미세폭율 = ((미세상단 - 미세하단) / 미세하단) * 100 if 미세하단 > 0 else 999
    미세돌파율 = ((현재가 - 미세상단) / 미세상단) * 100 if 미세상단 > 0 else -999

    기준상단 = 최고현재가(기준틱수, 돌파확인틱수)
    기준하단 = 최저현재가(기준틱수, 돌파확인틱수)
    기준폭율 = ((기준상단 - 기준하단) / 기준하단) * 100 if 기준하단 > 0 else 999
    최근돌파율 = ((최고현재가(돌파확인틱수, 1) - 기준상단) / 기준상단) * 100 if 기준상단 > 0 else -999
    현재위치율 = ((현재가 - 기준상단) / 기준상단) * 100 if 기준상단 > 0 else -999

    압축중심 = 이동평균(박스틱수, 1)
    중심괴리율 = abs(현재가N(1) - 압축중심) / 압축중심 * 100 if 압축중심 > 0 else 999

    거래대금비율 = 초당거래대금 / 초당거래대금평균(평균틱수) if 초당거래대금평균(평균틱수) > 0 else 0
    체결강도비율 = 체결강도 / 체결강도평균(평균틱수) if 체결강도평균(평균틱수) > 0 else 0
    호가압력 = 매수총잔량 / (매수총잔량 + 매도총잔량) if (매수총잔량 + 매도총잔량) > 0 else 0.5
    순매수수량 = 초당매수수량 - 초당매도수량

    기본필터 = 1000 <= 현재가 <= 100000 and 2 <= 등락율 <= 25 and 현재가 < VI가격 and not 라운드피겨위5호가이내
    수급필터 = 체결강도 >= 105 and 체결강도비율 >= 0.95 and 순매수수량 >= 0 and 호가압력 >= 0.43

    미세박스돌파 = 미세폭율 <= 1.2 and 미세돌파율 >= 0.25 and 거래대금비율 >= 2.0 and 초당매수수량 >= 초당매도수량 * 1.1
    리테스트지지 = 기준폭율 <= 1.8 and 최근돌파율 >= 0.5 and -0.35 <= 현재위치율 <= 0.45 and 0.6 <= 거래대금비율 <= 4.0 and 현재가 >= 기준상단 * 0.996
    도지압축돌파 = 미세폭율 <= 0.8 and 중심괴리율 <= 0.25 and 미세돌파율 >= 0.2 and 거래대금비율 >= 1.8

    if 기본필터 and 수급필터 and (미세박스돌파 or 리테스트지지 or 도지압축돌파):
        매수 = True

if 매수:
    self.Buy(종목코드, 종목명, 매수수량, 현재가, 매도호가1, 매수호가1, 데이터길이)
```

## CSS_V7_TICK_S_FAST_INVALIDATION_0900_0930

- 한국어명: 1tick 매도: 빠른 구조 무효화 | lane/side: `tick/sell` | time_window: `09:00:00-09:30:00` | 원천 섹션: §4.5
- code sha256: `eafcedd962bd9e45822f8729479b2ecfae833d0d656a8ff2ed6fb340b989505d` | DB 저장명: `CSS_V7_TICK_S_FAST_INVALIDATION_0900_0930` | passport: [css_v7_tick_s_fast_invalidation_0900_0930.md](../condition_passports/chart_sulsa/css_v7_tick_s_fast_invalidation_0900_0930.md)

```python
# =============================================================================
# CSS_V7_TICK_S_FAST_INVALIDATION_0900_0930
# 1tick 전용 매도식: 장초반 빠른 구조 무효화 + 09:30 강제 종료
# =============================================================================
매도 = False

if 시분초 >= 93000:
    매도 = True
elif 등락율 >= 29.5:
    매도 = True
elif 수익률 <= -2.0:
    매도 = True
elif 데이터길이 >= 30:
    체결강도비율 = 체결강도 / 체결강도평균(30) if 체결강도평균(30) > 0 else 0
    매도우위 = 초당매도수량 > 초당매수수량 * 1.5 and 체결강도비율 <= 0.9
    매수가기능선이탈 = 현재가 < 매수가 * 0.992
    단기저점이탈 = 보유시간 >= 10 and 현재가 < 최저현재가(20, 1) * 0.998
    이평이탈 = 현재가 < 이동평균(30) * 0.995 and 체결강도비율 <= 0.9

    if 매수가기능선이탈 and 매도우위:
        매도 = True
    elif 단기저점이탈 and 매도우위:
        매도 = True
    elif 이평이탈:
        매도 = True
    elif 최고수익률 >= 1.5 and 수익률 <= 최고수익률 * 0.45:
        매도 = True
    elif 보유시간 >= 240 and 수익률 < 0.3:
        매도 = True

if 매도:
    self.Sell(종목코드, 종목명, 매도수량, 현재가, 매도호가1, 매수호가1, 강제청산)
```

## CSS_V7_TICK_S_MICRO_DOJI_FAIL_0900_0930

- 한국어명: 1tick 매도: 미세 도지 실패 | lane/side: `tick/sell` | time_window: `09:00:00-09:30:00` | 원천 섹션: §4.6
- code sha256: `f93b031c4a24d68ab3fb01b02b60150196d73b9f0b34a0d5a51fc19dc54807a0` | DB 저장명: `CSS_V7_TICK_S_MICRO_DOJI_FAIL_0900_0930` | passport: [css_v7_tick_s_micro_doji_fail_0900_0930.md](../condition_passports/chart_sulsa/css_v7_tick_s_micro_doji_fail_0900_0930.md)

```python
# =============================================================================
# CSS_V7_TICK_S_MICRO_DOJI_FAIL_0900_0930
# 1tick 전용: 미세 도지/압축 박스 하단 이탈 또는 상단 돌파 실패 청산
# =============================================================================
매도 = False

if 시분초 >= 93000:
    매도 = True
elif 데이터길이 >= 60:
    압축틱수 = 30
    도지상단 = 최고현재가(압축틱수, 1)
    도지하단 = 최저현재가(압축틱수, 1)
    도지중심 = 이동평균(압축틱수, 1)
    하단이탈율 = ((현재가 - 도지하단) / 도지하단) * 100 if 도지하단 > 0 else 0
    중심이탈 = 현재가 < 도지중심 * 0.997
    매도우위 = 초당매도수량 > 초당매수수량 * 1.3 and 체결강도 < 체결강도평균(30)

    if 하단이탈율 <= -0.25 and 매도우위:
        매도 = True
    elif 중심이탈 and 매도우위 and 수익률 <= 0.3:
        매도 = True
    elif 최고수익률 >= 1.2 and 현재가 < 도지상단 and 수익률 <= 최고수익률 * 0.5:
        매도 = True

if 매도:
    self.Sell(종목코드, 종목명, 매도수량, 현재가, 매도호가1, 매수호가1, 강제청산)
```

## CSS_V7_TICK_S_MASTER_0900_0930

- 한국어명: 1tick 통합 매도식 | lane/side: `tick/sell` | time_window: `09:00:00-09:30:00` | 원천 섹션: §4.7
- code sha256: `68bedcf47100da014064336f80e31451247902aefba2e2f6e617880de9f7331b` | DB 저장명: `CSS_V7_TICK_S_MASTER_0900_0930` | passport: [css_v7_tick_s_master_0900_0930.md](../condition_passports/chart_sulsa/css_v7_tick_s_master_0900_0930.md)

```python
# =============================================================================
# CSS_V7_TICK_S_MASTER_0900_0930
# 1tick 통합 매도식: 09:30 종료 + 구조 무효화 + 도지 하단 이탈 + 트레일링
# =============================================================================
매도 = False

if 시분초 >= 93000:
    매도 = True
elif 등락율 >= 29.5:
    매도 = True
elif 수익률 <= -2.2:
    매도 = True
elif 데이터길이 >= 60:
    체결강도비율 = 체결강도 / 체결강도평균(30) if 체결강도평균(30) > 0 else 0
    매도우위 = 초당매도수량 > 초당매수수량 * 1.4 and 체결강도비율 <= 0.9
    호가하락압력 = 매수총잔량 / (매수총잔량 + 매도총잔량) if (매수총잔량 + 매도총잔량) > 0 else 0.5
    매수가기능선이탈 = 현재가 < 매수가 * 0.992
    단기저점이탈 = 보유시간 >= 10 and 현재가 < 최저현재가(20, 1) * 0.998
    미세중심 = 이동평균(30, 1)
    미세하단 = 최저현재가(30, 1)
    도지하단이탈 = 현재가 < 미세하단 * 0.997 and 현재가 < 미세중심 and 매도우위
    트레일링 = 최고수익률 >= 1.5 and 수익률 <= 최고수익률 * 0.45
    시간정체 = 보유시간 >= 240 and 수익률 < 0.3

    if 매수가기능선이탈 and (매도우위 or 호가하락압력 <= 0.35):
        매도 = True
    elif 단기저점이탈 and 매도우위:
        매도 = True
    elif 도지하단이탈:
        매도 = True
    elif 현재가 < 이동평균(30) * 0.995 and 체결강도비율 <= 0.9:
        매도 = True
    elif 트레일링 or 시간정체:
        매도 = True

if 매도:
    self.Sell(종목코드, 종목명, 매도수량, 현재가, 매도호가1, 매수호가1, 강제청산)
```

## CSS_V7_OPT_TICK_B_MASTER_0900_0930

- 한국어명: 1tick 최적화형 매수식 | lane/side: `tick/buy` | time_window: `09:00:00-09:30:00` | 원천 섹션: §6.1 | 최적화 변수셋: `CSS_V7_OPT_VARS_TICK_0900_0930` (conditions JSON `vars_ranges` 참조)
- code sha256: `c4775e12f4db4e7f54580c024202d7f8446ce304504c158b36bfcf9718a85179` | DB 저장명: `CSS_V7_OPT_TICK_B_MASTER_0900_0930` | passport: [css_v7_opt_tick_b_master_0900_0930.md](../condition_passports/chart_sulsa/css_v7_opt_tick_b_master_0900_0930.md)

```python
# =============================================================================
# CSS_V7_OPT_TICK_B_MASTER_0900_0930
# 1tick 최적화형 매수식: CSS_V7_OPT_VARS_TICK_0900_0930 사용
# =============================================================================
매수 = False

if 데이터길이 >= int(self.vars[0]) + int(self.vars[1]) + 30 and 90000 <= 시분초 < 93000 and 관심종목 == 1:
    박스틱수 = int(self.vars[0])
    기준틱수 = int(self.vars[1])
    돌파확인틱수 = int(self.vars[2])
    평균틱수 = int(self.vars[3])

    미세상단 = 최고현재가(박스틱수, 1)
    미세하단 = 최저현재가(박스틱수, 1)
    미세폭율 = ((미세상단 - 미세하단) / 미세하단) * 100 if 미세하단 > 0 else 999
    미세돌파율 = ((현재가 - 미세상단) / 미세상단) * 100 if 미세상단 > 0 else -999
    기준상단 = 최고현재가(기준틱수, 돌파확인틱수)
    기준하단 = 최저현재가(기준틱수, 돌파확인틱수)
    기준폭율 = ((기준상단 - 기준하단) / 기준하단) * 100 if 기준하단 > 0 else 999
    최근돌파율 = ((최고현재가(돌파확인틱수, 1) - 기준상단) / 기준상단) * 100 if 기준상단 > 0 else -999
    현재위치율 = ((현재가 - 기준상단) / 기준상단) * 100 if 기준상단 > 0 else -999
    거래대금비율 = 초당거래대금 / 초당거래대금평균(평균틱수) if 초당거래대금평균(평균틱수) > 0 else 0
    체결강도비율 = 체결강도 / 체결강도평균(평균틱수) if 체결강도평균(평균틱수) > 0 else 0
    호가압력 = 매수총잔량 / (매수총잔량 + 매도총잔량) if (매수총잔량 + 매도총잔량) > 0 else 0.5

    기본필터 = self.vars[4] <= 등락율 <= self.vars[5] and 1000 <= 현재가 <= 100000 and 현재가 < VI가격 and not 라운드피겨위5호가이내
    미세돌파 = 미세폭율 <= self.vars[6] and 미세돌파율 >= self.vars[7] and 거래대금비율 >= self.vars[8]
    리테스트 = 기준폭율 <= self.vars[9] and 최근돌파율 >= self.vars[10] and -self.vars[11] <= 현재위치율 <= self.vars[12] and 현재가 >= 기준상단 * (1 - self.vars[11] / 100)
    수급 = 체결강도 >= self.vars[13] and 체결강도비율 >= self.vars[14] and 호가압력 >= self.vars[15] and 초당매수수량 >= 초당매도수량 * self.vars[16]

    if 기본필터 and 수급 and (미세돌파 or 리테스트):
        매수 = True

if 매수:
    self.Buy()
```

## CSS_V7_OPT_TICK_S_MASTER_0900_0930

- 한국어명: 1tick 최적화형 매도식 | lane/side: `tick/sell` | time_window: `09:00:00-09:30:00` | 원천 섹션: §6.2 | 최적화 변수셋: `CSS_V7_OPT_VARS_TICK_0900_0930` (conditions JSON `vars_ranges` 참조)
- code sha256: `72549be977ecf17e434b1c671c74180c7409ac3d187c467a3362b811cdaad16b` | DB 저장명: `CSS_V7_OPT_TICK_S_MASTER_0900_0930` | passport: [css_v7_opt_tick_s_master_0900_0930.md](../condition_passports/chart_sulsa/css_v7_opt_tick_s_master_0900_0930.md)

```python
# =============================================================================
# CSS_V7_OPT_TICK_S_MASTER_0900_0930
# 1tick 최적화형 매도식: CSS_V7_OPT_VARS_TICK_0900_0930 사용
# =============================================================================
매도 = False

if 시분초 >= 93000:
    매도 = True
elif 수익률 <= self.vars[20]:
    매도 = True
elif 데이터길이 >= int(self.vars[3]):
    평균틱수 = int(self.vars[3])
    체결강도비율 = 체결강도 / 체결강도평균(평균틱수) if 체결강도평균(평균틱수) > 0 else 0
    매도우위 = 초당매도수량 > 초당매수수량 * self.vars[21] and 체결강도비율 <= self.vars[22]
    기능선이탈 = 현재가 < 매수가 * self.vars[23]
    단기이탈 = 보유시간 >= self.vars[24] and 현재가 < 최저현재가(20, 1) * self.vars[25]
    트레일링 = 최고수익률 >= self.vars[26] and 수익률 <= 최고수익률 * self.vars[27]
    시간정체 = 보유시간 >= self.vars[28] and 수익률 < self.vars[29]

    if 기능선이탈 and 매도우위:
        매도 = True
    elif 단기이탈 and 매도우위:
        매도 = True
    elif 트레일링 or 시간정체:
        매도 = True

if 매도:
    self.Sell()
```
