# SYS05 B2/B3 통합·전체 페이지 핸드오프

## 최종 검증 갱신 — 2026-09-09 KST

**통합 코드10b67ed3 기준 Fast8,080통과·27skip / Slow24통과, 모두 exit0.** 서로 겹치지 않는 분할이므로 총8,104통과·27skip이며, 아래 초기 실패 이력은 그대로 보존한다. 전체 프로그램/운영 검증/경제성과 완료가 아니다.

- 정확한 영수증: planning/2026-09-08_integrated_unit_receipt.json.
- 현재 진행: planning/2026-09-08_page_progress.json, SYS05 5/8 원자 검증·전체 단계1/7·페이지 완결0/14.
- 다음 상세 수용계획: planning/2026-09-08_next_acceptance_plan.json (계획 branch fd376527).
- Fast 최종 로그: C:/Users/parkc/STOM_Verification/v1.5_20260907/sys05-integrated-fast-default-temp.log, 1171.67초.
- Slow 최종 로그: C:/Users/parkc/STOM_Verification/v1.5_20260907/sys05-integrated-slow.log, 1571.85초.

검사 경로 정정: 이 환경의 TEMP/TMP는 C:/Temp지만 Python은 TMPDIR=C:/Users/Public/Documents/ESTsoft/CreatorTemp를 우선한다. env:TEMP로 강제한 basetemp도 안전 allowlist와 달라3건이 재실패했다. 최종 Fast는 basetemp를 지정하지 않았다. 안전 코드/ACL을 수정하지 않았고 독립 검토2개가 이 원인 분리를 확인했다.

별도1회 D3 checkpoint os.replace WinError5는 원인이 미확정이다. 기본temp 해당12시험 및 최종Fast에서 재현되지 않았으나, 잠금/ACL/보안 프로그램 중 무엇 때문인지 확정하거나 수정 완료로 보고하지 않는다. 재발하면 별도 관측한다. 전체 단위시험 이전의 중간 RUNNING/FAIL 기록은 당시 상태이며 이 최종 갱신과 구분한다.

아래 코드 Gate 이후 추가되는 계획·영수증은 비실행 문서만 변경한다. 최종 문서 병합 뒤 생성 색인을 별도 branch로 갱신하고 문서 수용 검사를 다시 수행한다. 다음 코드 작업은 최신 loop에서 B4a branch를 만들어 시작한다.

## 현재 위치

```text
정본 loop/process-research-pipeline
  e30197c4 ── 9개 원래 작업을 각각 no-ff 병합 ── 2cea8914
   └─ Git 관리문서 d467792d ── merge 1f9b6459
   └─ B2 세대 품질 b522a71a ── merge cf133ef3
   └─ B3 보고서 품질 0c9b09d9 ── merge bf5d02a1

RES04P[완료] → SYS05[5/8 원자 검증] → ANA05 → ANA06 → ANA07 → ANA08 → UX06
                      ↑ 현재
다음: 통합 Gate 정리 → B4 개별 분석/MC → B5 복수 입력 → SQ02 지표 정의
```

5/8=62.5%는 동일 작업 묶음의 개수 비율일 뿐 전체 프로그램 개발률이 아니다. 전체 단계 Gate1/7, 새 전체 페이지 수용0/14. 기존 STOM 기능이0이라는 뜻이 아니다. 연구 실제 실행/경제 성공/자동채택0회이며 별도 승인 경계는 유지한다.

## 브랜치와 작업 폴더

| 경로 | 용도 | 보호 |
|---|---|---|
| C:/System_Trading/STOM/STOM_V.wt-dev | research/v516-d3-mcap-dev @97a59ad3 | dirty 사용자 작업; checkout/reset/stage 금지 |
| C:/System_Trading/STOM/STOM_V.wt-process-research-restart | 기능 구현 branch, B3 @0c9b09d9 | tmap WIP 보존 |
| C:/System_Trading/STOM/STOM_V.wt-pipeline-integration | 정본 통합·단위시험 | 시험 중 HEAD 변경 금지 |

common Git은 C:/System_Trading/STOM/STOM_V/.git. GitKraken에 여러 worktree가 보이는 것은 정상이다. 정본 브랜치와 디렉토리는 별개다. 공개30e05d63/0db7457c는 그대로 두었다. origin/loop는 fetch 시점e30197c4. push/force-push/원격PR 없음.

## 실제 개발 결과

| 기능 | 사용자에게 달라지는 점 | 검증 |
|---|---|---|
| 일반 job 결과 B1 | CSV불량/실행실패를 성과0으로 표시하지 않음 | 이전217회귀 |
| 세대 결과 B2 | 손실·gate탈락은 진단 보존, 실행실패는 차단, CSV없음은 미검증 요약 | 229결합·5영역·실제 브라우저 |
| 보고서 B3 | 품질 검사 우회 제거, 같은 snapshot 분석/MC, HTML과 JSON에 hash | 251결합 및 마지막10·5영역·실제 브라우저 |

시험 수는 겹치므로 합산하지 않는다. 실제 브라우저는 production API/컴포넌트와 합성 GET fixture를 사용했으며 시장자료 연구가 아니다. 정상손실/실행실패/CSV없는 저장손실을 확인했다. 검증 서버·탭·TemporaryDirectory는 종료했다.

## 통합 단위시험의 실패 분류

정본2cea8914 전체 unit 실제 결과: **4 failed, 8078 passed, 27 skipped, 54 warnings, 4831.58초, exit1**.
로그: C:/Users/parkc/STOM_Verification/v1.5_20260907/pipeline-integration-full-v2.log.

| 실패 | 원인/처리 | 변경하지 않은 것 |
|---|---|---|
| tick_seed_timeout_probe 3개 | 검사자가 지정한 basetemp가 허용 temp/.omo 밖. 기본 pytest tmp로 전체 해당8시험 재실행→8pass12.90초 exit0 | 안전 allowlist·원본 코드·assertion |
| 문서 색인1개 | 새 체크아웃의 실제 문서 bytes/hash와 sidecar 불일치. 통합 체크아웃에서 생성 sidecar만 재생성 | 봉인 원본/evidence/문서본문 |

예: 동일 B1 receipt Git 내용이지만 restart raw SHA256=6e1cb57b…/integration=01092011…이며 Windows checkout 줄바꿈 차이가 있다. 핀된 RES04P JSON3개는 -text 적용과 원래 rawhash 복원으로 보존했다. 원시근거의 hash를 새 값으로 승인하지 않았다.

18~19%는 기존 SYS04 slow authority 구간이다. 스택/CPU로 진행을 확인했고 강제 timeout이나 assertion 삭제로 PASS를 만들지 않았다. 최초 잘못된 basetemp 전체 실행을 PASS로 고쳐 쓰지 않는다.

병합 후에는 tests/AGENTS의 SYS04 Fast/Slow 분리 Gate를 모두 실행한다. 출력은 외부 sys05-integrated-fast.log / sys05-integrated-slow.log로 보존한다. **이 문서 작성 시 재검증 결과는 미확정**이며 마지막 출력/exit code를 확인한 후 최종 보고한다. 이후 영수증은 별도 기록한다.

위 문장은 최초 작성 시점이다. 최종 재검증 결과는 문서 상단의 2026-09-09 갱신을 우선한다.

## 전체 페이지별 남은 수용 항목

| 화면군 | 현재 구현 연결 | 남은 핵심 | 순서 |
|---|---|---|---|
| P01 종합 | 기존 대시보드 | 현재 Gate/허용행동/차단사유를 한곳에 | ANA07·UX06 |
| P02 라이브러리 | job/세대 결과 품질 | 모든 source identity·Bundle 목록 | SYS05·ANA05 |
| P03 품질/계보 | job/세대/보고서 품질 | 개별/복수소비자 전파·영속계보 | B4/B5·ANA05 |
| P04 성과/위험 | 차단/미검증 표시 | 단위·자본·기간·비용·미정의 지표 | SQ02 |
| P05 거래/Replay | 기존 상세 + CSV계약 | trade key로 같은 거래 왕복 | ANA06 |
| P06 집단/피처 | 사건/control 기반 | 표본·결측·불확실성/집단 비교 | ANA06 |
| P07 마이닝 | 발견/평가 통계 분리 | 별도 확인·FDR·dataset identity | ANA07 |
| P08 A/B/반사실 | 기존 개별 비교 자산 | 적격성·공통/신규/제거 효과 | B5·ANA07 |
| P09 Fold/Control | control/overlap 기반 | 누락cell 사유·실제Bundle 연결 | ANA07 |
| P10 지식/반증 | 계획 | 적용범위·실패학습·재개조건 | ANA08 |
| P11 가설/사전등록 | RES04P 정의 Gate | 검토/승인 화면과 원장 연결 | ANA08 |
| P12 큐/예산 | 예산 정의 | 실제 attempt/cancel 영수증 | ANA08 |
| P13 보고서 | 품질·같은 요청 snapshot | 시간차 화면/export의 동일Bundle | ANA05·UX06 |
| P14 설정/건강 | 기존 설정 | default/effective·환경/자료/금지 구분 | UX06 |

## 다음 실행의 원자 단위

1. 현재 branch/HEAD/dirty/진행 중 pytest를 먼저 확인한다. 두 통합 Gate가 실제 끝났는지 확인 전 다시 실행하거나 파일을 바꾸지 않는다.
2. 통합 Gate가 green이면 최신 loop에서 codex/process-research-sys-05-individual-quality 생성한다. 같은 이름 존재 시 기존 HEAD/내용 확인 후 재사용 여부를 결정한다.
3. B4a: _analysis_for_job/_analysis_for_run 및 summary/equity/distribution/heatmap/underwater/insights/mae_mfe/exit_reasons/montecarlo 직접 API를 대상으로 불량 CSV·실행실패에서 계산함수 미호출 red를 만든다. 오류를 []로 바꾸어 정상0처럼 보이게 하지 않는다. 기존 response field를 유지하면서 typed analysis_ready/quality와 null 차단을 추가하고 소비 UI도 검증한다.
4. B4b: leaf_matrix/feature_map/revision_proposals/orderflow/gui_parity의 raw CSV 재읽기를 별도로 조사한다. label_dataset의 기존 소유 코드를 재사용하고 검증 snapshot·entry-time 변수/결과변수 역할을 구분한다. 마이닝 연구 실행은 하지 않는다.
5. B5: compare/overlay/portfolio는 입력별 hash·품질·공통 범위를 검증한다. 일부 입력 누락을 총성과에서 조용히 제외하지 않는다.
6. SQ02: 0과 미정의, 유한값, 보유시간 단위, 초기자본 없는 MDD/MC, 같은 기간/비용을 새 definition/projection으로 구분한다. 과거 봉인 결과 소급 수정 금지.
7. 이후 ANA05→06→07→08→UX06을 정본 순서로 실행한다. Bundle v3와 reference namespace 혼용 금지, Catalog6-table 유지. 실제 연구/경제 Gate를 기능 완료와 혼동하지 않는다.

각 단위: red→최소 수정→green→API/실제 합성 브라우저→독립5영역→TASK_RECEIPT/원장→명시 stage/한국어 commit→loop no-ff merge→Gate. 원격 push는 별도 요청 전 하지 않는다.

## 다음 작업 독립 검토의 추가 위험

- B4a에는 orderflow/gui_parity도 포함한다. 빈 list를 반환해 정상0 통계를 만드는 우회를 막아야 한다. MC의 n상한·seed·method/block_length·범위 필터는 유지한다.
- B4b의 leaf_matrix/feature_map은 전체 B/S/R 컬럼을 사용한다. 현재 NormalizedTrade만 전달하면 피처가 손실될 수 있으므로 동일 raw snapshot에서 기존 DataFrame/enrich owner로 연결하는 adapter가 필요하다. 검사 후 path를 다시 read_csv하는 방식은 불충분하다.
- revision_proposals는 전략 조회/제안 계산 전에 품질을 확인한다. 메모리 apply 미리보기를 저장/채택 권한으로 확대하지 않는다.
- BtLeafExplorer/BtFeatureMap뿐 아니라 trade-path의 BtEntryAutopsy도 품질 이유를 표시해야 한다. source A→B 전환에서 이전 제안·늦은 A응답·baseUrl 의존성을 실제 브라우저로 검증한다.
- 기존 MC/orderflow 합성 fixture에는 custom 헤더와15자리 날짜가 있다. 공식 입력으로 고치되 n상한/통계 assertion을 유지한다. GUI parity missing-job의 옛 n=0 계약은 typed 차단으로 명시 변경한다.
- leaf/feature/revision은 URL 배선시험만으로 기능 검증을 대신하지 않는다. 실제 API 실패재현부터 추가한다. 별도 evolution_gui_parity와 B5 소비자는 별도 추적한다.

정적 독립 검토이며 위 결함을 구현/수정 완료한 것은 아니다.

색인을 안정된 문서 상태에서 재생성한 뒤 `build_research_docs_index.py --check` PASS1120, 문서 API7시험37.27초 exit0을 확인했다. 앞선 재생성 중 INDEX를 편집해 발생한 stale 재실패1건은 검사 순서 문제로 기록한다. 이제 전체 Fast/Slow 실행 중에는 문서/HEAD도 고정한다.
