# SQ01 완료·전체 페이지 고도화 인계

2026-09-08. 최신 시작점은 이 문서 → SQ01_TASK_RECEIPT → 실행계획_진행원장이다.
현재 worktree: `C:/System_Trading/STOM/STOM_V.wt-process-research-restart`.
현재 branch: `codex/process-research-sys-05-quality`.
부모: `23d3b16d` (SQ00). 현재 파일들의 최종 commit은 `git log -1`로 확인한다.

## 현재 위치와 완료율의 의미

```text
정의 Gate: [■□□□□□□] 1/7 완료 — 작업량 비율이 아님

RES-04P [완료]
   └─ SYS-05 [진행]
        ├─ SQ00 평가 통계 재집계                  [완료]
        ├─ SQ01 데이터 계약 CSV 품질 API·V4 연결 [부분 경로 완료]
        ├─ SQ01B 기존 분석 소비자 품질 전파      [다음]
        └─ SQ02 지표 정의·비유한 효과값          [남음]
   → ANA-05 정본 Bundle
   → ANA-06 사건·거래·집단
   → ANA-07 별도 확인·반사실
   → ANA-08 지식·가설·사전등록
   → UX-06 전페이지 수용
```

전체14화면군 최종수용 완료 확인은 0/14다. 기존 STOM 기능이0이라는 뜻이 아니라 이번 고도화의 모든 수용조건을 닫은 화면군이 아직 없다는 뜻이다. P03 데이터 계약 부분 경로는 I(구현)/C(연결)/V(합성 API·브라우저 검증)를 완료했다. 전체 기능의 노력 가중 완료율은 산정하지 않는다. 기계 판독 진행표는 `planning/2026-09-08_page_progress.json`.

## 페이지별 진행·미구현 범위

| 페이지 | 현재 신규 개선 상태 | 아직 남은 기능 |
|---|---|---|
| P01 종합 대시보드 | 계획·정의 정리 | 허용 행동·차단 사유·다음 단계 API/UI |
| P02 결과 라이브러리 | 계약 화면의 실패/누락 결과 선택 가능 | 전체 라이브러리 identity·자료 존재·실행 상태 통합 |
| P03 품질·계보 | CSV→계약 API→품질 카드 실제 연결/검증 | 모든 분석 소비자 전파·Bundle 계보·전체 수용 |
| P04 성과·자본·위험 | 기존 자산 유지 | 기간/자본/비용 공통 기준·null/0·비유한 값 |
| P05 거래·Replay | 사건 기반·계약 품질 표시 | trade key로 모든 상세/차트 연결 |
| P06 집단·피처 | 사건/대조군 코드 기반 | 집단별 표본/결측/불확실성 화면 |
| P07 마이닝 | 평가 통계 v2 및 발견값 보존 | train/eval identity·별도 확인·FDR·연속 라벨 |
| P08 A/B·반사실 | 기존 자산 유지 | 적격성·공통/제거/신규 거래 효과 분해 |
| P09 Fold·Control | E1 STOP·R1 overlap 구현 | 실제 결과 fold cell과 누락 사유 UI |
| P10 지식·반증 | 계획 | 적용범위·반증·재개조건 원장 |
| P11 가설·사전등록 | RES04P 정의 Gate 완료 | 가설/검토/승인 UI·후속 실험 연결 |
| P12 큐·예산 | 예산 정의 | source inventory 감독·deadline·attempt/retry/cancel |
| P13 보고서 | 마이닝 definition·발견값 보존 | 모든 화면·HTML·JSON의 동일 Bundle/hash |
| P14 설정·건강 | 환경 차이 기록 | default/effective·자료 상태·금지 행동 UI |

## 왜 SQ01B를 명시했는가

SQ01은 사전에 정한 'typed consumer 하나 연결'을 완료했다. 그러나 기존 `backtest_analysis.load_trades_csv`는 여전히 list facade이며 `full_analysis`와 다수 backtest API가 이를 쓴다. 따라서 이 작업만으로 잘못된 자료가 모든 분석 경로에서 차단된다고 말할 수 없다. 독립 맥락 검토도 이 경계를 확인했다. 해당 남은 작업을 SYS05의 SQ01B로 명시해 누락과 재작업을 방지한다.

## SQ01B 착수 단위

1. branch/HEAD/dirty/common-dir/worktree 및 적용 AGENTS 확인. 최신 quality commit에서 `codex/process-research-sys-05-analysis-quality`를 생성한다. 이름이 있으면 강제 checkout/reset하지 말고 ancestry 확인.
2. `backtest_analysis.full_analysis`, `backtest_api`의 결과 분석 route와 실제 V4 consumer를 연결도로 정리한다. 해당 경로의 기존 test·UI 동작을 먼저 확인한다.
3. 오류/부분 CSV가 현재0건·정상 성과로 보이는 경우를 실제 production import 회귀로 재현한다. source scope는 결과 분석 route 하나로 한정한다.
4. `load_trade_quality`의 한 snapshot/정상행을 기존 순수 분석 함수에 공급한다. 재읽기 캐시나 두 번째 해시 계산을 넣지 않는다. 기존 facade를 강제로 제거하지 않는다.
5. VALID가 아니면 정본 성과 계산을 막고 typed 품질·이유를 반환한다. 부분 정상행 진단은 명시적으로 분리하고 전체 결과의 근거로 표시하지 않는다. NO_TRADES는 오류와 구분하되 거래 기반 미정의 지표를0으로 만들지 않는다.
6. API/component/Golden 검증 → 실제 브라우저 정상·오류·무거래·source전환 → 독립 검토 → TASK_RECEIPT/master/page_progress/index → Korean scoped commit.

기존 비용·지표 산식을 변경하는 작업은 SQ02와 구분한다. 다른 분석 endpoint 전파는 같은 패턴으로 별도 원자 commit을 반복한다. ANA05에서는 동일 Bundle로 정본화·영속성을 마무리한다.

## 이번 검증과 유보

- 113개 집중 회귀 및 기존 분석/V4 배선 검사 통과; 독립 신규30시험/JSX8시나리오는 중복 집계하지 않는다.
- 실제 인하우스 브라우저: 정상/무거래/부분/실패/누락/지연A→B/640px+키보드 상세 확인.
- 390px 도구 측정 timeout, 200% 전체 확대, 전체 운영 dashboard, 전체14화면 접근성은 미완료.
- static: 신규5모듈0오류/기존private normalizer 재사용1경고. 이전 F02타입부채10오류는 별도 기록 유지.
- 경제적 성과·조건식 승격/자동채택·실제 연구 결과는 이번 시험에서 확인하지 않았다.

## Git·보호·권한

loop 정본 `e30197c44507acf2f2360e78463e48b1ccfdc81c`은 그대로다. dirty wt-dev/운영DB/봉인evidence/tmap feedback은 건드리지 않는다. 새 branch의 source·tests·docs·정상 빌드 산출물만 명시적으로 stage한다. merge/push/원격 Issue/PR은 별도 승인 전 금지.

실제 StageF/G0/G2/Holdout/자동채택/실주문/운영 DB write는 계속 금지다. 전체 개발 위임을 경제 실행 승인으로 해석하지 않는다. StageF 전에 source inventory·supervisor·semantic receipt 검증 및 별도 승인이 필요하다.

## 재개 프롬프트

> 최신 quality branch commit과 이 인계·진행원장·page_progress를 읽고 상태를 확인하라. RES04P/SQ00/SQ01 데이터 계약 경로 완료를 유지하고, 최신 commit 위에 codex/process-research-sys-05-analysis-quality를 만들어 SQ01B의 첫 실제 결과 분석 route에 품질 계약을 연결하라. 실제 import red→최소 green→API/UI/브라우저→독립 검토→문서 갱신→scoped Korean commit 순서를 지켜라. 운영 데이터·봉인 원본·tmap은 수정하지 말고 연구/경제/merge/push 없이 전체 페이지의 완료·부분·남은 범위를 보고하라.
