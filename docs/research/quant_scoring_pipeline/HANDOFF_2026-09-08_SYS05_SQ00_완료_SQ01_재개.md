# 전체 페이지 개발 인계: RES-04P 완료 → SYS-05 SQ01

2026-09-08. 이 문서는 작업 branch의 최신 인계다. 기존 2026-09-07 초안 인계는 당시 기록으로 보존한다.

## 1. 현재 위치

```text
RES-04P [정의 Gate 완료 ba18e141]
    |
SYS-05 [진행 중]
    +-- SQ00 평가 통계 혼용 수정 [구현·합성 검증 완료]
    +-- SQ01 CSV + TradeArtifactContract + typed 품질 [바로 다음]
    +-- SQ02 지표 미정의/버전·입력 경계 [남음]
    |
ANA-05 정본 Bundle -> ANA-06 사건/집단 -> ANA-07 확인/반사실
    -> ANA-08 지식/사전등록 -> UX-06 전체 동선
    -> [별도 실행 승인] Stage F -> [조건부 별도 경제 Gate]
```

7단계 중 정의 Gate 1개 완료, SYS-05 부분 구현이다. 전체 기능/페이지 완성률로 환산하지 않는다. 새 시장 연구·수익성 확인은 0건이다.

## 2. 작업 위치·브랜치

정확한 경로: `C:/System_Trading/STOM/STOM_V.wt-process-research-restart`.
현재 branch: `codex/process-research-sys-05-evaluation-stats`.
정본 `loop/process-research-pipeline @e30197c44507acf2f2360e78463e48b1ccfdc81c`는 변경하지 않았다.

```text
e30197c4 loop 정본
  48f95986 계획 -> b84a90f2 준비 -> 8de7c70e 사건 -> 993001cd source binding
    ba18e141 RES04P control/STOP/overlap + 정의 Gate
      현재 branch: JSON 바이트 보존 commit + F02 기능 commit
        다음: codex/process-research-sys-05-quality
```

재개 시 `git log -5 --oneline`으로 최신 기능 commit을 읽고 부모 SHA를 다음 원자 영수증에 기록한다. commit 전 문서가 자기 commit hash를 임의로 만들지 않는다. merge/push/원격 Issue/PR은 별도 승인 전 금지.

wt-dev는 다른 dirty branch이며 작업 overlay/reset/stage 금지. restart의 `.omo/evidence/tmap-walkforward/_discovery_feedback.txt`는 사용자 WIP로 그대로 유지한다. `_database/strategy.db` 0-byte도 이번에는 삭제하지 않았다.

## 3. 처음 읽을 문서

1. 이 인계와 `2026-09-07_v15_실행계획_진행원장.md`.
2. `2026-09-08_SYS05_F02_TASK_RECEIPT.md`.
3. `2026-09-08_RES04P_controls_TASK_RECEIPT.md`, `2026-09-08_RES04P_최종사전등록_정의.md`.
4. `planning/2026-09-08_audit_requirements_crosswalk.json`: 56이슈/100수용시험계획/14방법/43아이디어/16WP 원문 추적.

RES04P 정의 SHA `05c14a641d3e4933c5c607f10f4285b655d4452496072d7315a6af7000a5a83c`를 확인한다. 미래 event output hash가 없다는 이유로 정의 Gate를 다시 미완료로 돌리지 않는다. 미래 실행에는 여전히 source-bound inventory/supervisor/semantic verifier/권한이 필요하다.

## 4. 페이지별 목적과 남은 실제 연결

기존 화면군 재사용이다. 아래는 신규 계획의 상태이며 기존 STOM 화면 자체가 없다는 뜻이 아니다.

| 페이지 | 사용자 질문 | 현재 개발 영향 | 다음 완료 조건 |
|---|---|---|---|
| P01 Mission | 지금 무엇을 할 수 있나? | 계획/실행 Gate 구분 정리 | API의 허용 행동·차단 사유를 실제 화면에서 확인 |
| P02 결과 라이브러리 | 어떤 결과를 분석하나? | 아직 신규 연결 없음 | result ID·artifact 존재·terminal 상태를 분리 |
| P03 품질·계보 | 이 자료를 믿어도 되나? | SQ01 바로 다음 | loader의 raw/accepted/rejected/hash/issues 표시 |
| P04 성과·자본·위험 | 같은 기준으로 비교하나? | 아직 신규 연결 없음 | 기간/자본/비용/정의별 null·0 구분 |
| P05 거래·Replay | 어떤 거래 때문인가? | source/event 기반 구현 | TradePath와 같은 trade key로 상세 연결 |
| P06 집단·피처 | 어떤 상황에서 달라지나? | event/control 기반 구현 | 표본/결측/선택 범위·불확실성 표시 |
| P07 마이닝 | 발견이 다시 확인되나? | F02 평가 수치 재집계·보고서 version 보존 | train/eval identity·독립 확인 연결 |
| P08 A/B·반사실 | 거래 감소와 효과를 구분하나? | 아직 신규 연결 없음 | 적격성 및 공통/제거/신규 거래별 분해 |
| P09 Fold·Control | 근거가 충분한가? | typed STOP/누락/R1 overlap 구현 | 실제 bundle 기반 fold cell 및 누락 사유 UI |
| P10 지식·반증 | 무엇을 다시 하면 안 되나? | 아직 신규 연결 없음 | 적용범위·반증·재개 조건 원장 |
| P11 가설·사전등록 | 다음에 무엇을 검증하나? | 정의/schema/코드 identity 확정 | 검토·자료 역할·승인과 UI 연결 |
| P12 큐·예산 | 실제 실행이 어떻게 진행되나? | 정의 예산만 고정 | 감독자 deadline·attempt/retry/cancel 영수증 |
| P13 보고서 | 화면과 같은 근거인가? | 마이닝 평가 정의/발견값 보존 | 같은 Bundle/context/hash를 HTML/JSON/UI로 확인 |
| P14 설정·건강 | 실제 적용값은 무엇인가? | runtime 차이 기록 | default/effective·자료 상태·금지 행동 표시 |

## 5. SQ01 실행 명세

목적: 잘못된 CSV, 정상 무거래, 파일 없음, 일부 행 파싱 실패가 같은 빈 목록으로 보이지 않게 한다.

기존 owner:

- `ai_strategy_loop/dashboard/backtest_analysis.py::load_trades_csv` (현재 약164행), `_normalize_row` (약226행).
- `ai_strategy_loop/dashboard/trade_contract.py::build_trade_artifact_contract` (약184행).
- 호출자와 해당 디렉터리 AGENTS를 먼저 읽고 source scope를 한 adapter 경계로 제한한다.

절차:

1. branch/HEAD/dirty/worktree/common-dir·AGENTS 확인. 기능 commit 위에 `codex/process-research-sys-05-quality` 생성. 기존 branch가 있으면 자동 강제 재사용하지 말고 ancestry 확인.
2. 실제 loader import 부작용을 확인하고 DB 없는 fixture에서 실패 재현. reference 7열 parser를 복사하지 않는다.
3. 생산 54/37열 입력에서 missing/정상0/헤더오류/부분파싱/NaN/행수불일치/mtime-size동일bytes변경/ERROR terminal+정상CSV를 구분.
4. 같은 bytes snapshot으로 parse+hash. typed status와 여러 issue를 함께 반환하는 새 좁은 경계. 기존 list 반환 facade는 하위호환 유지.
5. TradeArtifactContract의 실제 consumer 하나에 연결하고 API/component/Golden 검증. UI를 변경한 경우 바로 브라우저에서 정상·오류·무거래 및 source 전환/늦은 응답을 확인.
6. 독립 검토 → TASK_RECEIPT → master/index 갱신 → 지정 파일만 Korean commit. 운영 DB·봉인 evidence·tmap 제외.

수용: 같은 bytes는 같은 판정/hash, 누락·오류를 수익0/성공으로 표시하지 않음, 정상0값 유지, 유한 JSON, 기존 정상 CSV 호환, 모든 거부 행 설명. 6-table 규칙 및 production Bundle v3 namespace 유지. 새 schema/table 필요 시 먼저 설계결정, 자동 migration 금지.

롤백: 신규 typed consumer 연결을 되돌리고 구형 facade 유지. 기존 artifact나 보고서 덮어쓰기 없음.

## 6. 그다음 순서와 미해결 위험

| 순서 | 작업 | 반드시 필요한 이유 |
|---|---|---|
| 1 | SQ01 품질 경계 | 잘못 읽은 자료에서 정확한 분석은 불가능 |
| 2 | SQ02 지표 정의·미정의·유한값 | 결측/NaN/0을 혼동하면 잘못된 통과 발생 |
| 3 | ANA-05 Bundle/API/export 정본 | 화면마다 서로 다른 결과를 보지 않도록 함 |
| 4 | ANA-06 거래/사건/집단 근거 | 평균 성과 이면의 실패 원인 탐색 |
| 5 | ANA-07 평가·반사실·다중검정 | 발견 착시와 재현 가능한 신호 구분 |
| 6 | ANA-08 지식/가설/사전등록 | 실패를 누적 학습하고 반복을 통제 |
| 7 | UX-06 전체 사용 시나리오 | 사용자가 근거·현재 위치·다음 행동을 이해 |

F02 함수 결함은 수정했지만 dataset identity/FDR/연속 라벨은 남았다. F04의 비유한 효과값 판정도 별도 회귀로 처리한다. 기존 10개 dict 타입 오류와 환경 의존성 차이는 typed 계약 작업에 기록했다. 기능/시험 완료와 경제적 성과는 계속 분리한다.

Stage F/경제 G0/G2/Holdout/자동채택/실주문/실전 자본/운영 DB write는 실행하지 않는다. 이번 결과에서 좋은 조건식 또는 수익성 확보를 주장할 수 없다.

## 7. 재개 프롬프트

> 이 인계와 최신 TASK_RECEIPT·진행원장을 먼저 읽고 실제 HEAD/dirty를 확인하라. RES-04P 계획 Gate와 SYS05 SQ00는 완료로 유지한다. 최신 기능 commit 위에 codex/process-research-sys-05-quality를 만들고 SQ01의 실제 CSV loader + TradeArtifactContract + typed 품질 상태 연결을 red→최소수정→green→필요한 API/브라우저→독립검토→영수증 순서로 구현하라. 사용자 WIP·운영 DB·봉인 원본은 수정하지 말고 Stage F/경제/Holdout/자동채택/merge/push 없이 로컬 scoped commit과 전체 페이지 상태를 보고하라.
