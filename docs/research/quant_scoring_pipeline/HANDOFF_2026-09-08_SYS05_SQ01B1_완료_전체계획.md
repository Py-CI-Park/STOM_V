# SQ01B1 완료·전체 페이지 고도화 재개

현재 worktree: `C:/System_Trading/STOM/STOM_V.wt-process-research-restart`.
현재 branch: `codex/process-research-sys-05-analysis-quality`.
부모 commit1933e7fb. 이 문서 포함 최신 commit은 `git log -1`로 확인한다.

## 현재 위치

```text
RES04P [완료] → SYS05 [진행] → ANA05 → ANA06 → ANA07 → ANA08 → UX06

SYS05 작업 묶음: [■■■□□□□□] 3/8 완료 (동일 묶음 수 기준)
  ■ SQ00 평가 통계 재집계
  ■ SQ01 데이터 계약 품질 API·V4
  ■ B1 일반 job 결과 분석 품질      ← 이번 두 번째 완료
  □ B2 진화 세대 결과 품질          ← 다음
  □ B3 보고서·export
  □ B4 개별 분석·MC
  □ B5 비교·overlay·portfolio
  □ SQ02 지표 정의·비유한 값
```

전체 단계 Gate는1/7이며 전체14화면 최종수용은 아직0/14다. 기존 STOM 기능이0이라는 뜻이 아니다. 현재 P03 데이터 계약, P02/P04 일반 job 결과 경로는 부분 기능의 구현·연결·합성검증이 이루어졌다. 전체 프로그램 노력 가중 완료율은 산정하지 않는다.

## 14화면 현황

| 화면군 | 달라진 부분 | 남은 범위 |
|---|---|---|
| P01 종합 | 계획·상태 정의 | 허용 행동·차단 사유 통합 |
| P02 결과 라이브러리 | 실패/누락 선택, job 결과 품질 차단 | 전체 source identity·라이브러리 |
| P03 품질·계보 | 공식54/37·snapshot·품질 UI·job 분석 차단 | 세대/보고서/개별/복수 소비자·Bundle 계보 |
| P04 성과·위험 | 불량 job의 지표/차트 생성 중지 | 자본/기간/비용 정의·미정의 지표 |
| P05 거래·Replay | source/event 및 데이터 계약 기반 | 모든 거래키/상세/차트 연결 |
| P06 집단·피처 | 사건·대조군 기반 | 집단별 품질·표본·불확실성 |
| P07 마이닝 | 평가 통계v2·발견값 보존 | dataset identity·별도확인·FDR·연속라벨 |
| P08 A/B·반사실 | 기존 자산 보존 | 입력 적격성·공통/제거/신규 효과 |
| P09 Fold·Control | STOP/누락/겹침 코드 | 실제 fold cell UI·정본근거 |
| P10 지식·반증 | 계획 | 적용범위·반증·재개조건 |
| P11 가설·사전등록 | 정의 Gate 완료 | 검토·승인·후속실험 UI |
| P12 큐·예산 | 정의 예산 | 원천 inventory·supervisor·deadline·attempt |
| P13 보고서 | 마이닝 provenance·job 결과 JSON 품질 | HTML/JSON/UI 전체 동일 Bundle |
| P14 설정·건강 | 환경 차이 기록 | 실제 적용 설정·건강 UI |

기계 진행표: `planning/2026-09-08_page_progress.json`. 단계 원장: `2026-09-07_v15_실행계획_진행원장.md`.

## B2 다음 실행 단위

1. 최신 HEAD·dirty·AGENTS를 확인하고 `codex/process-research-sys-05-generation-quality` 생성. 기존 branch가 있으면 ancestry 확인, 강제 reset 금지.
2. `backtest_api._result_for_run`, `_gen_row_readonly`, `_resolve_gen_csv`와 실제 shared ResultDetailBody를 읽는다. loop_runs.db는 기존 readonly 경로만 사용한다.
3. 세대 status의 `ok/rejected/error`를 일반 job success로 단순 치환하지 않는다. **조건식 gate 탈락과 backtest 실행 실패를 구분**하고 기존 research_truth_contract 소유 코드의 의미를 재사용한다. 손실 전략을 분석 대상에서 임의 제거하지 않는다.
4. 실제 생성 metadata trade_count·source identity를 품질과 연결한다. 존재하는 CSV의 불량을 성공0으로 만들지 않고, CSV 없는 과거 세대의 저장 metrics는 '미검증 요약'이라는 기존 낮은 권위를 보존한다.
5. 검증된 snapshot을 prepared_trades seam으로 공급한다. 정상 source의 빈 조회 범위는 source 품질 실패가 아니다.
6. 실제 import red→최소수정→기존 회귀/API/공유UI→브라우저→독립검토→receipt/master/progress/index→scoped Korean commit.

완료 후 B3 report/export → B4 개별/MC → B5 복수입력 → SQ02 순서다. 각 범위에서 source 해시와 분석값의 동기화를 확인하고 원본 결과를 덮어쓰지 않는다. 전체 지표/Bundle 정본화는 ANA05와 구분해 진행한다.

## 검증·권한·인계 주의

최종 결합217시험, 독립184+경계3, 공유UI4시나리오 통과. 217은 앞선SQ01 검사도 포함하므로 합산 금지. 인하우스 브라우저의 정상/부분/무거래/실패/누락 결과를 확인했다. 정상 fixture의 부수 leaf/feature endpoint는 미제공404, MC는미실행stub이므로 전체 기능 검사로 과장하지 않는다. 200%·전페이지 접근성·실데이터 운영 검증은 남았다.

loop 정본e30197c4는 그대로. tmap feedback·dirty wt-dev·운영 DB·봉인 evidence는 수정/stage하지 않는다. StageF/경제G0/G2/Holdout/자동채택/주문/운영DB쓰기/merge/push 금지. 이 개발 완료는 수익성 확보가 아니다.

재개 프롬프트:

> 최신SQ01B1 receipt와 이 인계·master·page_progress를 읽고 현재 commit/dirty를 확인하라. RES04P/SQ00/SQ01/B1 완료를 유지하고 다음 B2 진화세대 결과 품질 전파를 새 원자 branch에서 구현하라. 기존 execution truth와 source권위를 재사용하고 실제 import회귀·API/브라우저·독립검토·scoped Korean commit 및 전체 페이지 현황 갱신을 수행하라. 운영/봉인 데이터와 연구·경제·원격 권한은 확장하지 마라.
