# R1 리뷰 — P1 라벨 데이터셋 강화 (2026-07-30)

## 1. 구현 내역 (C2·C3 + UX)

| 항목 | 내용 | 검증 |
|---|---|---|
| 엔진 결과부 v2 확장 | `TRADE_RESULT_B_COLUMNS` 14→31 (공통 8 + tick 4 + min 5). 이름 기반 캡처라 목록 추가만으로 끝 — 로직 무수정. 구형 행은 normalize 0-패딩(하위호환) | 신선 백테로 실증(아래) |
| 오프라인 파생 계산기 | `autopsy/label_dataset.py` — **조건식으로 재현 가능한 수식만** D_* 12종(전일종가·총호가매수비율·시가등락율·현재가위치·체결강도비율·폭발배수·수급비 …). 분석 결과가 곧 조건식 수정안이 되도록 | 단위테스트 10건 |
| 컬럼 자동 편입 | 하드코딩 금지 — CSV 헤더의 B_*/D_* 수치 컬럼 전부 설명변수. zero-variance(타 타임프레임 필드)·결측 50%+ 자동 제외. 미래 컬럼 자동 반영 테스트 포함 | 단위테스트 |
| 잔차표 강화 | `leaf_matrix()` — 평균과 **중앙값·승률 병기**(R0 F: min CSS 복권형 왜곡 대응) + reliable(n≥30) 정직 표기 | 단위테스트 |
| autopsy SSOT 통일 | analyze.py 의 B 컬럼 하드코딩 사본 → back_static import(어긋남 사고 차단) | 단위테스트 |
| 배치 연결 | `claude_candidate_batch_eval` 이 쌍마다 `leaf worst/best` 로그 출력(best-effort) | 코드 반영 |
| 대시보드 API | `GET /bt/analysis/leaf_matrix` (job/run 양쪽, 무예외) | 실서버 200 · 0.36s |
| **탐색기 UI** | `bt-leaf-explorer.jsx` — 리프 히트맵(평균/중앙값 토글) + 셀 클릭→상세(대표 거래 승4·패4) + 변별 상위 변수 바 | 실브라우저 폐루프 확인 |

## 2. R1 게이트 — 변별력 실측 (RQ-C2/C3 답변)

### 신선 tick 백테 (`20260730-0006_tick_qsp1-colcheck`, 1개월 934거래)

- v2 컬럼 17종 **전부 산출**: 결측 0% · 분산>0 (tick 전용 4종 포함)
- 파생 12/12 활성(D_시가등락율·D_현재가위치·D_체결강도비율·D_거래대금폭발배수·D_누적수급비 …)
- 설명변수 **15 → 34개**

### 변별력 (Cohen's d, 승 vs 패)

| 순위 | 변수 | d | 출처 |
|---|---|---|---|
| 1 | D_전일종가 | −0.210 | 파생(v1 가능) |
| 2~5 | B_저가/고가/현재가/시가 | −0.207 | **v2 신규** |
| 6 | B_등락율각도 | +0.201 | **v2 신규** |
| 7 | B_당일거래대금각도 | +0.184 | **v2 신규** |
| 8 | B_초당거래대금 | +0.176 | **v2 신규** |
| (참고) v1 최고 | B_시분초 | −0.101 | 구 14컬럼 |

**판정**: v2 확장이 최고 변별력을 **2배**(|d| 0.10→0.21)로 끌어올렸고, 상위 8중 6개가
신규 컬럼. "가격 수준(저가주 유리)·각도(모멘텀 기울기)·초당거래대금"이 승패를 가른다 —
P2 리프 경계 수정의 1차 재료.

### 신선 min 백테 — 결함 발견 → 해소 → 재검증 (연구 과정 그 자체)

**F-R1-3 (실측 결함·해소)**: 첫 min 검증(`20260730-1008`)이 **900초 타임아웃**.
A/B(어제 성공한 11개월 config 재실행 `20260730-1035`)도 동일 증상 → 엔진 v2 확장이
min 경로 원인으로 확정. 기전: v1 B컬럼은 전부 원시 DB 열(인덱스≤23)이었지만 v2 의
계산열(B_RSI=98 등)은 min 런타임 배열 폭(이 모드에선 계산열 일부 미포함)을 초과 →
매수 캡처 IndexError → 엔진 정지 → 타임아웃. **처방**: `_get_snapshot_value` 에
열 경계 가드(폭 밖 → default 0, 절대 예외 금지) + 회귀 테스트.
재검증(`20260730-1120_min_qsp1-colcheck2`): **54초 완주**(1,256거래).

| min 실측 | 값 |
|---|---|
| v2 컬럼 실값 | 10/11 (B_RSI 만 이 모드 미제공 → 가드 0 → 자동 제외) |
| 파생 활성 | **12/12** (D_수급비·D_거래대금폭발배수 min 판 포함) |
| 설명변수 | **38개** |
| 최고 변별 | **D_고가근접율 d=−0.147 (신규 파생이 1위)** |
| 배치 leaf 로그 | 작동 확인 — "worst: B3_한산×S_3000미만(−1.13%,n=270)" |

## 3. 발견·한계 (원장 등재)

- F-R1-1: 가격 수준 계열(현재가·저가·고가·전일종가)의 d 가 서로 동치 — 다중공선.
  P2 번역기는 이들을 한 축(가격대)으로 묶어야 중복 제안을 피한다.
- F-R1-2: 효과크기만으로는 방향 처방 불충분 — 분위수 경계(어디서 자를지)는
  autopsy 의 t검정+분위수 경로가 담당(P2 에서 배선).

## 4. 스코어카드 (게이트 기준 90)

| 항목 | 배점 | 득점 | 근거 |
|---|---|---|---|
| 기능 완결성 | 30 | 29 | 계획 6항목 전부 + 배치 로그 연결. 감점 −1: 전체화면 오버레이에는 탐색기 미탑재(소스 식별자 부재 — 의도적 제외) |
| 검증 | 25 | 25 | 신규 18 + 기존 계약(31건) 통과 · 신선 tick/min 백테 실증(min 은 결함 발견→A/B 격리→가드→54초 재검증의 완전한 사이클) · 실서버·실브라우저 폐루프 |
| UX/UI | 20 | 19 | 탐색기(히트맵·토글·상세·변별바) 공통 카드 규약 준수 · 표본부족 정직 표기. 감점 −1: 셀→조건식 코드 하이라이트 연동은 P2 |
| 신뢰성 | 15 | 15 | 엔진 additive-only(로직 무수정) · 하위호환 테스트 · backtest/graph 불변 · 무예외 계약 |
| 문서/재현 | 10 | 10 | 본 리뷰 + 원장 + colcheck config/pairs 커밋 · run_id 기록 |
| **합계** | 100 | **98** | **PASS (≥90) → P2 진행** |
