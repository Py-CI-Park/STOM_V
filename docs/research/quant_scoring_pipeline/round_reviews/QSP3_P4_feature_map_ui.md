# QSP3 P4 게이트 — 다차원 수익률 맵 탐색기 (2026-07-31)

> 자가점수 없음 — 증거 체크리스트만. 채점은 P5 독립 감사.

## 구현

| 층 | 내용 | 검증 |
|---|---|---|
| API | `GET /bt/analysis/feature_map` — job_id/run_id+gen_no, `x`(+`y`) 분위 grid(n·손익·평균수익률·승률) / `mode=regions` 손실 영역 랭킹 / `variables` 선택 목록. 무예외 계약(실패=빈 구조) | 실서버 curl: 1D grid(available:true·34변수) · 2D grid 16셀 · regions top5(D_체결강도비율 −22.5M 발견) |
| 프론트 | `bt-feature-map.jsx` — X/Y 변수 선택, 표시값 4종(손익합/평균수익률/승률/거래수), 구간 3~10, [맵]/[손실 영역 랭킹] 토글, 다이버징 색+셀 수치 병기+툴팁, 백테 결과 영역에 리프 탐색기 아래 배치 | esbuild 번들 성공, 서빙 번들에 컴포넌트 포함 확인 |
| 스타일 | v4.css `.bt-fm-*` (기존 리프 탐색기 규약 준수) | — |
| 배포 | v5.13.4 → **v5.14.0** (프론트 상수+백엔드 릴리즈 동기), 8770 단일 서버 재기동 | 실브라우저: 타이틀 v5.14.0 · 콘솔 에러 0 · 호환성 경고 해소 |

## 한계 (정직)

- 패널 데이터 렌더링의 육안 확인은 API 레벨 실데이터 + 콘솔 무에러 + 리프 탐색기와 동일 패턴까지 —
  결과 로드 상태의 셀 렌더 스크린샷은 사용자 실사용 시 확인 요청(백테 결과 로드 필요).
- 3D 표면도는 계획대로 후순위(2D×색×크기 다요소가 기본) — 사용자 피드백 후 결정.
- 영역 클릭→제거 후보 전송(파이프라인 연결 버튼)은 백로그 — 현재는 랭킹 표가 후보를 보여주고
  실제 제거는 캠페인 러너가 같은 계산으로 자동 수행.
