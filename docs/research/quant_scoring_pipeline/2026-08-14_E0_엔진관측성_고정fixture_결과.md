# E0 엔진 관측성 고정 fixture 대조 결과 (2026-08-14)

> 사전등록: `2026-08-14_E0_엔진관측성_고정fixture_사전등록.md` (`34c9d1cc`)
> 원시 증거: `evidence/2026-08-14_e0_observability.json`
> 판정: **`BLOCKED_ENVIRONMENT`**

## 1. 실행 결과

| arm | 반복 | 상태 | 경과(초) | metrics | protocol checkpoint |
|---|---:|---|---:|---|---|
| 챔피언 기준선 | 1 | success | 196.989 | 있음 | 응답 미노출 |
| 챔피언 기준선 | 2 | success | 113.930 | 있음 | 응답 미노출 |
| 챔피언 기준선 | 3 | success | 147.401 | 있음 | 응답 미노출 |
| G2 생성식 | 1 | timeout | 305.184 | 없음 | 응답 미노출 |
| G2 생성식 | 2 | timeout | 304.521 | 없음 | 응답 미노출 |
| G2 생성식 | 3 | timeout | 302.872 | 없음 | 응답 미노출 |

기간·전략·엔진 수를 고정했을 때 **상태 차이는 3/3 반복**됐다. 그러나 사전등록의 종속변수는 상태가 아니라 first-MQ 도달과 마지막 protocol checkpoint다. 8771 job 결과가 `backtest_process_diagnostics`를 노출하지 않아 해당 기준을 판정할 수 없다.

## 2. 판정 교정

초기 실행기는 상태 signature만으로 `REPRODUCED`를 반환했다. 이는 사전등록보다 약한 기준이라 보고 전에 자가 감사에서 차단했다. 다음과 같이 교정했다.

- checkpoint가 한 행이라도 없으면 `BLOCKED_ENVIRONMENT`.
- 상태 차이만으로 절·조건식·엔진 단계 원인을 주장하지 않는다.
- 원시 JSON에 blocker를 명시했다.
- 회귀 테스트를 추가했다.

## 3. 남은 기술 조건

E0를 재개하려면 8771 job 결과 또는 별도 읽기 전용 진단 endpoint가 다음을 노출해야 한다.

- `event_count`
- `last_checkpoint`
- `last_by_source`
- first-MQ 수신 여부

이 배선이 완료되기 전 delta debugging·절 원인 귀속·엔진 비용 모델로 진행하지 않는다.

## 4. 안전 확인

- 6개 job은 success 또는 timeout terminal 상태다.
- 잔존 실행 job은 없다.
- 실거래·운영 DB·홀드아웃·V3K gate를 건드리지 않았다.
- 수익성 판정과 원장 적재를 하지 않았다.
