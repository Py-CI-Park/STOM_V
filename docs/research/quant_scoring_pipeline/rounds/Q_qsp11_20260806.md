# QSP11 라운드 로그 — 파생 특징 주입 (2026-08-06)

> 브랜치 `feature/qsp10-reach-map` · 계기: [QSP10 결과보고](../2026-08-06_qsp10_결과보고.md) §7
> — 게이트 FAIL 의 근본 원인이 "지도에 변화율 재료가 없었다"로 규명됨.
> 권위: 관측(탐색). 공식 판정은 엔진·사다리에서만.

## 1. 🔑 엔진 창 함수 정의 — 정답지 대조로 역산 (영구 지식)

엔진 소스에 구현이 없었다(`back_code_test.py` 는 문법 검사용 스텁). 대신 엔진이 거래
CSV 에 기록해 둔 `B_*` 값을 **정답지**로 삼아 역산했다 — 체결 모델(매도호가1/매수호가1)을
역산했던 QA-1 과 같은 방법이다.

**엔진 기본 창(`평균값계산틱수`) = 60** (실측: 창 10~120 스캔에서 60에서만 100% 일치)

| 함수 | 확정 정의 | 일치율 | 표본 |
|---|---|---:|---|
| `체결강도평균(n)` | 최근 n틱 **평균** | **100%** | 22건 |
| `누적초당매수수량(n)` | 최근 n틱 **합** | **100%** | 22건 |
| `초당거래대금평균(n)` | **`round`(최근 n틱 평균)** — 엔진이 반올림한다 | **100%** | 41건 |
| `등락율각도(n)` | `deg(atan((r[t] − r[t−n]) / n × 5))` | **100%** | 31건 (3일) |
| `당일거래대금각도(n)` | **미확정** | — | 후보 3종 전부 불일치 → **추측으로 넣지 않고 제외** |

> 반올림을 놓쳤다면(원시 평균 9.2833 vs 엔진 9) 지도의 급증 배율이 엔진과 어긋나
> 후보가 전이되지 않았을 것이다. 정답지 대조가 아니었으면 발견 불가능했다.

## 2. 신규 파생 특징 11종 (`derived.py`)

창 30·60 각각: `초당거래대금배율`(급증) · `체결강도평균` · `등락율각도` · `누적매수매도비`
공통: `초당거래대금직전비` · `매수흐름_매도잔량비` · `잔량비`

| 검증 | 결과 |
|---|---|
| 테스트 | 6종 GREEN — 각도 공식·반올림 평균·누적/호가 비율·min 컬럼·이름 커버리지·**미래 누출 검사** |
| 실데이터(20250403) | 79열 · 2.0초/일 |
| **방증** | `초당거래대금배율_30` 의 **p90 = 3.217** — 902/905 임계 **3.0** 이 정확히 상위 10% 자리 |
| 집행 우주 커버리지 | 대부분 NaN 0% (`직전비`만 23% — 직전 초에 체결이 없던 경우, 정상) |

## 3. 라벨 v3 재빌드

| 항목 | 값 |
|---|---|
| 구간 | 20240304~20250822 · **355일 성공**(실패 1 = 20250102 연초 10시 개장) |
| 산출 | `state/labels/design_v3/` · **3.6GB** · 35.7분 |
| 탐색 변수 | **19종(원값) → 30종(원값 19 + 파생 11)** |

## 4. 동일 파이프라인 재실행 (진행)

QSP10 과 **완전히 같은 도구·같은 게이트**로 돌린다. 달라진 것은 **재료뿐**이므로
결과 차이는 곧 파생 특징의 기여다.

_(결과는 완료 후 기록)_
