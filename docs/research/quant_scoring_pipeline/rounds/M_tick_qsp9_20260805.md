# QSP9 M 라운드 로그 — tick 라벨 공장·지형도 (2026-08-05 착수)

> 브랜치: `feature/qsp9-m0-label-factory` · 실행계획 정본 v2 §3(라벨 명세)·§8(마일스톤)
> 권위: 이 문서의 수치는 별도 표기 없으면 **관측(진단)** 이다. 공식 판정은 M-5 게이트에서만.

## 1. 마일스톤 1 — M-0 라벨 공장 + M-1 지형도

### 1.1 tick DB 스키마 실사 (가정 금지 규율 이행)

| 항목 | 실측 |
|---|---|
| 일별 DB | `stock_tick_YYYYMMDD.db` × **952일** (설계 356일 · 홀드아웃 119일) |
| 하루 구성 | `moneytop`(거래대금순위) + 종목 테이블 60~96개 · 26~37MB |
| 스키마 | **54열 modern** — 호가 5단·잔량·VI가격/호가단위·시가총액·관심종목 포함 |
| 특이 | 종목은 수집 진입/이탈이 동적(관측 창이 부분적) · 일부 일자는 09:30 전 수집 종료 |

### 1.2 라벨 공장 구현 (`ai_strategy_loop/labeling/`)

| 모듈 | 내용 |
|---|---|
| `label_spec` | 명세 §3 상수 정본 — horizon {30,60,120,300,close} 사전 고정 · 비용 0.21% · 스테일 10초 · VI 5호가 · 시각 산술 함수 |
| `label_factory` | 초 그리드 재색인+ffill · 가격 기준 A(호가)/B(현재가) · MFE/MAE 경로 라벨 · 플래그 3종 · close 절단 감지 |
| `build_labels` | 멱등 실행기 — **홀드아웃 생성은 규율 L2 로 기본 봉인** |
| `label_qa` | QA 3종 |
| `terrain` | 분 프로파일 · 분위 격자(임계 스냅 경계) · 파셋 히트맵(전 칸 표본수) |

테스트: 합성 tick DB 11종 GREEN. 실데이터 스모크(20250403): 30,547행·30종목·1.1초/일,
플래그·스프레드·절단 감지 정상.

### 1.3 ⚡ QA-1 이 잡아낸 결함 — 엔진 체결 모델 실측 (이 라운드 최대 수확)

첫 QA-1 은 **실패**했다 (중앙값 오차 +0.207%p — 왕복비용과 같은 크기, 매수가 일치율 41.7%).
역산 결과:

| 실측 | 값 |
|---|---|
| 엔진 매수 체결가 | **매도호가1** (일치율 83%) — 현재가 아님 |
| 엔진 매도 체결가 | **매수호가1** (일치율 92~93%) |
| 엔진 내재 비용 | **0.208%** (IQR 0.2046~0.2111) — 라벨 비용 모델과 일치 |

→ **주 라벨 = `frA_*`(호가 기준)로 확정.** `frB_*`(현재가)는 참조용.
가격 기준 A 로 재실행한 QA-1: **중앙값 오차 0.0000%p · IQR 0.0068%p · 90.8%가 ±0.1%p → PASS**

교훈: "현재가로 산다"는 가정으로 지도를 그렸다면 **전 지도가 스프레드(평균 0.25%)만큼
낙관 편향**됐을 것이다. QA-1 없이는 이 결함이 M-3 엔진 실측까지 숨어 있었다.

### 1.4 라벨 QA 성적표 — **3종 전부 정식 PASS (게이트 통과)**

| QA | 내용 | 결과 |
|---|---|---|
| QA-1 정합 | 실거래 2,256건 재계산 vs 엔진 수익률 | ✅ 오차 **중앙값 0.0000%p** · IQR 0.0068 · 90.8% ±0.1%p |
| QA-2 자기 검증 (개정) | **실제 902/905 체결 138건** 진입 시점 라벨 | ✅ 라벨 평균 **+0.569%** ≈ 실현 **+0.500%** · 상관 **0.647** |
| QA-3 음성 대조군 | 민 라벨(다른 날 라벨) 정보력 | ✅ 진짜의 **0.7~10%** (임계 33% 이하, 누출 없음) |

QA-2 개정 사유: 첫 정의(거친 905 필터 영역 평균 > 0)는 실전 조건식보다 수십 배 넓은
조악한 대리라 전 구간에서 −0.0225%p 로 FAIL — 판정 기준으로 부적합. 실체결 지점 검사로
교체. **부수 관측**: 거친 902 영역의 평균이 앞 215일에서는 +0.028, 전 구간에서는 음수 —
넓은 근사 영역의 엣지는 국면 의존적이다(M-2 에서 국면 절단으로 후속 확인).

### 1.5 라벨 빌드 (설계 구간 전체) — 완료

| 항목 | 값 |
|---|---|
| 구간 | 20240304 ~ 20250822 (356일) |
| 결과 | **355일 성공 · 실패 1**(20250102 — 연초 10시 개장, 09시 창 부재 = 정상 특이일) |
| 산출 | `day=*.parquet` 355개 · **2.1GB** · 사용가능 라벨 점 **18,249,784** · 22.9분 |
| QA 리포트 | `_qa_report.json` (전 항목 정식 수치) |

### 1.6 M-1 시초 지형도 — 첫 판독 (관측)

분당 무차별 E[frA_300](비용·스프레드 차감 후) — `_minute_profile.json`:

| 분(09:xx) | 표본수 | 평균 | 양수비율 | MFE | MAE | **MFE/\|MAE\|** |
|---|---:|---:|---:|---:|---:|---:|
| **00** | 808,832 | **−0.147%** | **41.1%** | **+1.955** | −1.391 | **1.41** |
| 01 | 1,027,109 | −0.217% | 39.8% | +1.672 | −1.297 | 1.29 |
| 02 | 1,016,059 | −0.253% | 38.7% | +1.609 | −1.286 | 1.25 |
| 05 | 981,162 | −0.360% | 35.7% | +1.420 | −1.230 | 1.15 |
| 10 | 905,449 | −0.387% | 33.9% | +1.194 | −1.101 | 1.08 |
| 15 | 835,936 | −0.372% | 33.0% | +1.091 | −0.990 | 1.10 |
| 19 | 789,026 | −0.412% | 30.9% | +1.011 | −0.974 | 1.04 |

**판독 3줄**:
1. 무차별 진입은 **어느 분에도 흑자가 아니다** — 선별 없이는 비용+스프레드를 못 넘는다 (예상 정합).
2. **엣지의 원료(MFE)는 시초 0~5분에 몰려 있고 단조 감소** — 09:00분만 MFE/\|MAE\|=1.41.
3. 902/905 가 09:00~05 를 고른 것은 우연이 아니라 **지도의 원료 분포와 일치**한다.

### 1.7 검증 3종

| 검증 | 결과 |
|---|---|
| 집중 테스트 | **1,137 passed** + 신규 11 = 1,148 (플레이크 1건 — `test_windows_process_job` 병행 부하 기인, 격리 재실행 PASS) |
| verifier | PASS |
| JSX | 118/568 PASS |

## 2. 커밋 이력 (이 라운드)

| 커밋 | 내용 |
|---|---|
| `0d6626ad` | 라벨 공장 + 실행기 + 테스트 8종 |
| `47544f0a` | QA 3종 + 지형도 집계 + 주 라벨 frA 확정 |
| (이번) | QA-2 실체결 지점 검사로 개정 + 라운드 로그 + 마일스톤 1 마감 |

## 3. 마일스톤 2 — M-2 변수 정보력·이익 포켓 (완료 · **게이트 통과**)

`_m2_report.json` (169초 · 18.25M점 · 변수 21종 · 전부 일 클러스터 추론 + FDR):

### 3.1 지도가 말하는 것 — 시초 30분의 넓은 엣지는 **역추세**다

| 발견 | 수치 |
|---|---|
| 과열 변수는 전부 음의 정보 | 당일거래대금 −0.82 · 등락율 −0.78 · 시가등락율 −0.61 · 전일비 −0.54 · 회전율 −0.42 (상−하위 분위 스프레드, q≈0) |
| 양의 정보는 단 2개 | **초당순매수금액 +0.187** · 체결강도 +0.083 |
| 현상 P1: 조용한 종목 드리프트 | 당일거래대금 하위 + 거래대금증감 완만 → **+1.25%/5분** (현실성 필터 후 155,268점) |
| 현상 P2: 낙폭 반등 | 활발 종목 중 등락율≤−0.56 & 시가등락율≤−0.97 → **+0.40%/5분** (481,793점) |

### 3.2 인공물 검사 (P1 이 "너무 좋아서" 의심 → 전부 기각)

| 검사 | 결과 |
|---|---|
| 현실성 필터(가격 1000~50000·스프레드≤1%·초당거래대금≥1M) | P1 +1.27→**+1.25%** 유지 · P2 +0.33→**+0.40%** 개선 |
| 종목 편중 | 3,144 종목-일 · 상위10 기여 **4.8%** (편중 없음) |
| 일 일관성 | P1 일평균 양수 **88.4%**/353일 · P2 74.6%/335일 |
| 시간 대리변수 여부 | 분 0~2 **고정**해도 당일거래대금 하위 20%만 +0.559%, 나머지 4분위 전부 음수 → **시간 효과 아님** |

### 3.3 M-3 후보 5종 (조립기 렌더 · 임계 분위 스냅 · 시간창 09:00~09:20)

| 후보 | 절 |
|---|---|
| C1 조용한드리프트 | 당일거래대금≤snap(736.5) · 거래대금증감>snap(−6.9e9) |
| C2 드리프트수요 | C1 + 초당순매수금액>0 + spread_pct≤1.0 |
| C3 낙폭반등 | 당일거래대금>snap(1448.5) · 등락율≤snap(−0.56) · 시가등락율≤snap(−0.97) |
| C4 반등수요 | C3 + 초당순매수금액>0 |
| C5 수요압력 | 초당순매수금액>q80 · 당일거래대금≤snap(1448.5) |

스크리닝 매도 = `hold300`(라벨 fr_300 의 거울) — 진짜 매도는 M-4.
공통 가드: 가격 1000~50000 · VI 5호가 · 라운드피겨. **관심종목 게이트 제거**(라벨 우주 정합).

### 3.4 인프라 함정 2건 (실측 기록)

| 함정 | 해법 |
|---|---|
| `/ui/v4` 는 307 리다이렉트 — 최종 응답 헤더만 보면 세션 쿠키를 놓침 | cookiejar 필수 |
| mutation 은 **same-origin Origin 헤더** 필수 (403 forbidden) | `Origin: http://127.0.0.1:8771` 첨부 |

## 4. 다음 — 마일스톤 3 잔여

- [ ] 엔진 5런 완료 대기 → 실측 수집(설계 구간만 — 홀드아웃은 M-5)
- [ ] 라벨 예측 vs 엔진 실측 전이율 기록
- [ ] 게이트: 설계 건당 > 0 후보 ≥ 1
