# 개선 루프 파이프라인 감사 — "부품은 다 있는데 조립이 안 됐다"

> 작성일: 2026-08-06 · 브랜치 `research/program-reassessment-20260806`
> 질문(사용자): 백테 결과→통계→시각화→사람·AI 공동 이해→수정 지점 검토→프롬프트→조건식 생성의
> 각 단계가 존재하고 작동하는가? 어디가 막혀 좋은 프로세스가 안 되는가?
> 방법: 실코드 전수 확인 (backtest/back_static.py · ai_strategy_loop/{autopsy,brain,revision,provider,controller,dashboard})

## 1. 단계별 감사 결과

| # | 단계 | 존재 | 품질 | 판정 근거 |
|---:|---|---|---|---|
| 1 | 백테스트 결과 산출 | ✅ | 좋음 | 거래 CSV에 B_*(매수 시점 상태 20+열)·S_*(매도 시점)·R_*(MFE/MAE 경로) — 부검 원료 완비 |
| 2 | 통계 분석 | ✅ | 좋음 | autopsy 26모듈: 승/패 Cohen's d+FDR·세그먼트 히트맵·loss_profile·exit_counterfactual·**Analysis Card v2**(root_cause 1~3개 + mutation_axis + 정직 라벨) |
| 3 | 시각화 | ⚠️ 반쪽 | 대시보드 24페이지·리플레이·호가창 좋음 | **분석 카드가 대시보드 미배선** — grep 결과 소비자 0 |
| 4 | **사람·AI 공동 검토 단계** | ❌ **부재** | — | 카드는 AI 프롬프트로만 흐름. "어디를 고칠지 주식과 함께 보며 검토"하는 화면·절차가 물리적으로 없음 |
| 5 | 수정 지점 결정 | ⚠️ 반자동 | hypothesis.py 설계 좋음(가설 1급 객체·자동 채택/기각) | 판정이 **같은 설계 구간 in-sample 델타** 기준. graduation_holdout 토글은 같은 holdout 슬라이스를 세대마다 재사용 — 반복 사용이 holdout 을 소모 |
| 6 | 생성 프롬프트 | ⚠️ 이중 상태 | prompt.py(1,653줄·주입 채널 27개) 현역 | **최상급 프롬프트 자산 2종(2026-06-11: 구조 가설 템플릿·단일 변경 변이)이 "기본 OFF·미배선"** — 부검 숫자 주입({autopsy_summary})·성공/실패 사례·한 번에 한 절 원칙을 갖춘 모범답안이 잠들어 있음 |
| 7 | LLM 공급자(뇌) | ❌ 취약 | chatgpt_oauth 프록시+codex+openrouter failover | 인증 전멸 이력 → **provider="batch"(zero-LLM) 퇴화 운행**. 마지막 기록 2026-08-01 run: provider=batch·MDD 139%·FAIL — 뇌 없이 루프가 돌았다 |

## 2. 좋은 프로세스를 막는 5가지 (실측)

| # | 문제 | 증거 | 결과 |
|---:|---|---|---|
| 1 | **사람 검토 단계 부재** | analysis_card → dashboard 소비자 0 | AI 혼자 카드→프롬프트→생성 폐쇄 루프. 사람은 최종 성적만 봄 — 902/905 를 만든 "사람의 판단"이 루프에서 배제됨 |
| 2 | **루프 안에 표본 밖 검증 없음** | hypothesis 판정 = in-sample 델타 · holdout 슬라이스 세대별 재사용 | 세대를 거듭할수록 설계 구간에 과적합 — QSP13 이 측정한 선택 편의가 여기서도 축적 |
| 3 | **최상급 프롬프트 미배선** | p5_*.md 헤더: "기본 OFF — 어떤 경로에도 자동 주입되지 않는다" | 생성 품질이 구식 프롬프트/템플릿 변이 수준에 머묾 |
| 4 | **뇌(LLM) 불안정** | provider=batch 퇴화 · GPT OAuth 전멸 이력 | 조건식이 "논리 없이" 만들어짐 — 탐색과 같은 함정으로 회귀 |
| 5 | **두 시스템 분열** | QSP 지도 레인(라벨 v3·진입단위·워크포워드·편의 예산)과 brain 레인이 상호 미참조 | 각자 반쪽: 지도는 생성이 없고, 생성은 검증 규율이 없다 |

## 3. 배선 계획 (W1~W4) — 새로 만드는 게 아니라 잇는 것

| # | 배선 | 내용 | 대응 문제 |
|---:|---|---|---|
| **W1** | 카드 → 화면 | Analysis Card v2 를 대시보드 페이지로 (root_cause·mutation_axis·손실 거래 리플레이 링크). **사람이 mutation_axis 를 승인/수정하는 체크포인트** 신설 | 1 |
| **W2** | 프롬프트 교체 | p5 구조가설·단일변경 프롬프트 배선 + provider 를 안정 LLM 으로. 승인된 mutation_axis 만 {autopsy_summary} 로 주입 | 3, 4 |
| **W3** | 루프 내 워크포워드 | hypothesis 채택/기각을 walkforward.py 폴드 델타로. holdout 재사용 금지 | 2 |
| **W4** | 레인 통합 | 부검이 라벨 v3(봉투·도달시각)·진입 단위 교정·편의 예산(수정 횟수 원장)을 소비 | 5 |

## 4. 완성형 루프 (재평가 문서 §7 의 절차와 결합)

```
아이디어(사람) → 가설 문장 → 완성 조건식(매수절+매도규칙군, 절마다 이유)
   → 엔진 백테 → 부검(카드) → [W1: 사람+AI 공동 검토 화면 — 승인] 
   → [W2: 단일 변경 프롬프트] → 수정 1절(원장 기록, 상한 ~15회)
   → 재백테 → … → [W3: 워크포워드 게이트] → 감독 소액 실전
```

핵심: 사람은 매 세대 "무엇을 고칠지"의 승인자로 루프 안에 들어오고,
AI 는 부검 숫자와 프롬프트로 그 판단을 보좌하며, 표본 밖 게이트가 과적합 축적을 차단한다.

---

## 5. 자율화 개정 (2026-08-06 사용자 지시 — "사람 검토 없이 성공이 목표")

W1 의 "사람 승인 체크포인트"를 **Claude 에이전트 검토**로 대체한다. 사람 게이트는
실전 투입 1곳만 남긴다. 근거 실측:

| 확인 | 결과 |
|---|---|
| Claude 직접 주입 경로 | ✅ 이미 존재·사용 이력: `scripts/claude_candidate_batch_eval.py` (LLM 인증 전멸 대응기 실전 사용) |
| GPT OAuth 배선 | ✅ 이미 완료 — `provider/chatgpt_oauth/constants.py` 가 Newsletter_AI 와 **같은 토큰 파일** (`~/.config/newsletter-ai/chatgpt_auth.json`, 존재 확인) 을 바라봄. 토큰 검증·갱신 실행만 세션 권한상 미실시 |
| STOM 백파인더 원리 | ✅ 이미 이식: `fitness/backfinder_principle.py` — 상승 전 진입 셋업 피처분포 채굴 → BandSpec 시드. **용도 = 가설 시드 전용** (in-sample 채굴이므로 심판 금지) |
| STOM 최적화 스위트 | ✅ 존재: `backtest/optimiz_conditions.py`(값 스윕)·`optimiz_genetic_algorithm.py`(GA)·`optimiz_3d_visualization.py`(응답면) — **구조 고정 후 숫자 보정** 단계에 적합. 3D 응답면은 고원/절벽 검사로 재사용 가능 |

### 자율 루프 완성형 (개정)

```
시드(사람 아이디어 ∪ 백파인더 채굴) → Claude: 가설 문장 + 완성 조건식(절마다 이유)
  → 엔진 백테 → 부검 카드 → Claude: 카드 판독·단일 변경 결정(가설 원장 기록, 상한 예산)
  → (구조 안정 시) OptimizeConditions/GA 로 숫자 보정 + 3D 응답면 고원 검사
  → 워크포워드 게이트(홀드아웃 재사용 금지) → 통과 후보만 사람에게 보고 → [사람: 실전 투입 게이트]
```

역할 분담: **구조·논리 = Claude** · **숫자 = STOM 최적화기** · **심판 = 엔진+워크포워드** ·
**사람 = 실전 게이트만**. 대시보드 카드 화면(W1)은 자율 루프의 필수가 아니라 관측용으로 격하.
