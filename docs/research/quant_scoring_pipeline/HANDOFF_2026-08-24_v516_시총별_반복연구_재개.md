# 핸드오프 — v5.16 시총별 반복 연구 재개 (2026-08-24)

> 기준 정본: `research/v516-d3-mcap-dev @ 8f39d0e7c048c8609c75f863031791560a6f81c0`
> **다음 세션은 이 문서 하나로 현재 상태를 복원한 뒤 원 Evidence를 대조해 재개할 수 있다.**
> 이 문서는 `2026-08-15_v5.16_전체로드맵_C1_최종보고.md`의 프로그램 체크리스트 완료와 사용자가 요구한 실제 반복 연구 완료를 구분하는 후속 정정·핸드오프다.

---

## 1. 가장 중요한 정정

`8f39d0e7`까지 완료된 것은 다음이다.

- v5.16 Research Cockpit·Program API
- immutable SQLite·Native sidecar/connector 경계
- 기존 27.69GB Tick DB의 시총 4Band Census
- Tick 공통 시간계약 `09:00:00 <= 시분초 < 09:30:00`
- D3 상태전이 5Family 구현
- QMC seed `20260815`로 640개 시드 생성
- 성과를 사용하지 않은 cell별 최대거리 2개, 총 40개 선택
- 2023-11-14 **단 하루**의 공식엔진 feasibility screen
- Evidence·테스트·PR #136~#147 통합

그러나 사용자가 요구한 핵심 연구는 다음 반복 루프다.

```text
시총 Band 하나 선택
→ 조건식 시드 생성
→ 다중 development 기간 공식 백테스트
→ 거래·무거래·오류·시장상태 분석
→ 분석에 근거한 조건식 구조 개선
→ 개선 세대 재백테스트
→ 사전등록된 중지 기준까지 반복
→ 대조군·Fold·통계 판정
→ 다음 Band에서 독립 반복
```

**이 반복 루프는 완료되지 않았다.** `81/81, 100%`는 당시 작성한 플랫폼·거버넌스 체크리스트의 typed terminal 수치이지, 사용자 목적의 연구 완료율이 아니다. 이후 보고에서 두 의미를 혼용하지 않는다.

---

## 2. `8f39d0e7` 정본 지도

### 2.1 Git 계보

| 범위 | PR/정본 |
|---|---|
| Program API·Cockpit·Census·Native·D3 | PR #136~#146 |
| 당시 C1 보고 | PR #147 |
| 통합 브랜치 | `research/v516-d3-mcap-dev` |
| 기준 HEAD | `8f39d0e7c048c8609c75f863031791560a6f81c0` |

### 2.2 핵심 코드

- `utility/sqlite_readonly.py` — immutable/query-only SQLite 경계
- `ai_strategy_loop/revision/mcap_bands.py` — 고정 시총 4Band
- `ai_strategy_loop/revision/window_contract.py` — Census 기반 시간계약
- `ai_strategy_loop/revision/mcap_state_machine.py` — D3 5Family 조건식 렌더링·실행계약
- `ai_strategy_loop/revision/mcap_qmc.py` — QMC proposal·거리 선택
- `ai_strategy_loop/revision/mcap_event_estimator.py` — 결과/PnL 비사용 Event 수 gate
- `ai_strategy_loop/revision/mcap_controls.py` — timestamp/symbol/direction/event-offset/random controls
- `ai_strategy_loop/revision/mcap_screen_decision.py` — D3/D4 조건부 gate
- `ai_strategy_loop/labeling/run_c0_mcap_census.py` — C0 실행기
- `ai_strategy_loop/labeling/run_d3_engine_screen.py` — direct-source 공식 screen·checkpoint
- `ai_strategy_loop/labeling/run_d3_screen_decision.py` — screen 판정 봉인
- `ai_strategy_loop/dashboard/backtest_api.py` — `research_direct_source` 제출·exact source snapshot
- `ai_strategy_loop/native_research/` — Native sidecar·adapter·connector/isolation gates
- `cli/runner.py`, `backtest/backengine_base.py`, `backtest/backtest.py` — market DB read-only 연결

조건식 소스를 새로 만들거나 개선하기 전에는 반드시 다음을 읽는다.

- `utility/ai_agent/strategy.txt`
- `utility/ai_agent/rules.txt`
- `utility/ai_agent/AGENTS.md`

### 2.3 핵심 Evidence

- `evidence/2026-08-15_mcap_census.json`
- `evidence/2026-08-15_d3_candidate_manifest.json`
- `evidence/2026-08-15_d3_engine_screen.json`
- `evidence/2026-08-15_d3_screen_decision.json`
- `evidence/2026-08-15_n0_native_gates.json`
- `utility/ai_agent/strategy/D3_OpeningStateMachine_시총4Band_20260815.txt`

고정 W0 SHA-256:

```text
69b2d05c2fd682cdf3f61f78ef33fb0f18f6a3b962fe7e68973662760db9c97b
```

D3 screen manifest SHA-256:

```text
39a6d3fd8b4cce65979a530375cdd228fda542ef03dd5963968f7d1ddf326fc0
```

---

## 3. 실제 D3 실행 범위와 결과

실행 설정은 `start=end=20231114`, engine 1, 독립 Dashboard manager 4개, workers 4였다. 이는 실행 가능성 확인용 1일 screen이지 Band 연구 종결용 다기간 검증이 아니다.

| 시총 Band | Metrics success | No-trades | Error | Timeout | 합계 |
|---|---:|---:|---:|---:|---:|
| `<3000` | 2 | 2 | 4 | 2 | 10 |
| `3000~5000` | 0 | 6 | 4 | 0 | 10 |
| `5000~10000` | 0 | 8 | 2 | 0 | 10 |
| `>=10000` | 0 | 5 | 5 | 0 | 10 |
| **합계** | **2** | **21** | **15** | **2** | **40** |

40/40 exact source snapshot hash는 일치했다. 그러나 17건의 error/timeout은 성공한 연구 결과가 아니라 **미해결 실행 실패**다.

Metrics가 생긴 `<3000` 후보:

| Family | 거래 | 평균수익률 | 총수익률 | MDD | 해석 |
|---|---:|---:|---:|---:|---|
| Absorption Reversal | 2 | -1.67% | -1.67% | 1.03% | 표본 부족·음수 |
| Compression Breakout | 4 | -0.48% | -1.93% | 4.01% | 표본 부족·음수 |

정확한 결론:

> 2023-11-14 단일 일자에서 선택된 40개 초기 시드는 양성 후보를 만들지 못했고 17개는 실행 실패했다. 이 결과는 시총 분할 또는 5Family 전체의 실패를 증명하지 않는다.

---

## 4. 미실행 작업

다음 산출물은 `8f39d0e7`에 없다.

- D3 Event estimator의 실제 실행 Evidence
- Negative controls의 실제 실행 Evidence
- Band별 다기간 공식엔진 결과
- 거래·무거래·오류 원인 분석 Evidence
- 분석 결과로 만든 개선 세대 lineage
- 개선 세대 공식 재백테스트
- Nested development folds
- BH-FDR·Bayesian posterior 실제 계산
- `BO_ELIGIBLE` 후보
- D4 Entry/Exit/Joint Native 최적화
- RWFT development·봉인 확인

Native 7/7은 connector/import/isolation gate다. BackFinder·OptimizeConditions·Optimize·Genetic·RWFT를 실제 조건식 개선 연구에 사용한 결과가 아니다.

---

## 5. 왜 사용자 지시와 달라졌는가

1. **목표 치환** — 실제 연구 반복보다 플랫폼·문서·게이트 구축을 완료 목표로 삼았다.
2. **초기 시드 탈락 구조** — 약한 시드를 분석·개선 대상으로 보지 않고 Rule-pass 전 탈락 대상으로만 처리했다.
3. **1일 smoke 과대해석** — 단일 일자 feasibility 결과로 Band 연구를 닫았다.
4. **실행 실패의 완료 계산** — exact source terminal이라는 이유로 error/timeout까지 완료 행에 포함했다.
5. **계획 순서 불이행** — 실제 Controls Evidence 없이 QMC·screen을 먼저 수행했다.
6. **프로그램 Batch 종료와 사용자 과업 완료 혼동** — `GATE_NOT_ENTERED`를 반복 연구 완료로 보고했다.
7. **체크리스트 최적화** — API·화면·PR·terminal row 수를 세고 개선 세대·반복 횟수·성과 변화는 세지 않았다.
8. **승계 오류** — 이전 세션의 `81/81` 해석을 사용자 원지시와 재대조하지 않고 강화했다.

재개 후에는 플랫폼 진행률과 연구 진행률을 반드시 별도 표로 보고한다.

---

## 6. 재개 우선순위

### P0 — 기존 문구와 Evidence 해석 고정

- `81/81`은 플랫폼·거버넌스 체크리스트 완료로만 표시한다.
- 연구 루프 완료로 표시하지 않는다.
- `시총 분할이 Edge를 만들지 않았다`고 단정하지 않는다.
- 17 execution failures를 성공 terminal로 집계하지 않는다.
- 과거 Evidence 파일은 삭제·덮어쓰기하지 않고 후속 Batch Evidence를 새 파일로 생성한다.

### P1 — `<3000` 1차 반복 루프

1. 기존 10개 중 error 4·timeout 2의 checkpoint와 job diagnostics를 분류한다.
2. 코드 오류와 실행비용 문제를 성과를 보지 않고 교정한다.
3. 동일 source identity가 필요한 재현 실행과 구조 변경 후보를 분리한다.
4. `<3000`만 대상으로 다중 development 기간과 Fold를 성과 비사용 기준으로 사전등록한다.
5. 5Family×32 원시 시드의 Event 수를 실제 산출한다.
6. Event 하한을 통과한 시드를 공식엔진으로 실행한다.
7. 거래·무거래·손실·실행 실패를 Family별로 분석한다.
8. 분석 결과로 **구조가 달라진** 2세대 시드를 생성한다. 단순 threshold 미세조정은 금지한다.
9. 동일 기준으로 재백테스트하고 세대별 개선/퇴행을 기록한다.
10. 사전등록된 반복 수 또는 개선 정지 기준까지 반복한다.
11. 적격 후보만 controls→folds→FDR/Bayesian으로 보낸다.

### P2 — 다른 Band 독립 반복

`<3000` 루프의 실행·Evidence 계약이 검증된 뒤 아래 순서로 반복한다.

1. `3000~5000`
2. `5000~10000`
3. `>=10000`

각 Band는 독립 시드·lineage·분석을 갖는다. `<3000` 임계값을 복사하거나 성과를 보고 Band 경계를 이동하지 않는다.

### P3 — 조건부 D4

D4는 실제 controls·folds·posterior를 통과한 `BO_ELIGIBLE` Cell에만 실행한다. 적격 전에는 Native BO를 실행하지 않는다.

---

## 7. 실행 재개 방법

### 7.1 시작 확인

```bash
cd C:/System_Trading/STOM/STOM_V.wt-dev
git switch research/v516-d3-mcap-dev
git rev-parse HEAD
# 기준 확인: 8f39d0e7c048c8609c75f863031791560a6f81c0 또는 이 핸드오프를 병합한 후속 commit
```

다음 문서를 순서대로 읽는다.

1. 이 문서
2. `2026-08-15_기존DB_시가총액분할_상태전이연구와_v5.16_재출발_마스터플랜.md`
3. `2026-08-15_D3_시총4Band_OpeningStateMachine_최종결과.md`
4. `2026-08-15_v5.16_전체로드맵_C1_최종보고.md` — 정정 전 역사 보고로 읽는다.
5. 위 Evidence 5종

### 7.2 Dashboard manager 격리

공식 job manager는 서버 하나당 직렬 queue다. 병렬 실행 시 port별 jobs dir를 분리한다.

```bash
STOM_WEBBT_JOBS_DIR=ai_strategy_loop/state/webbt_jobs_8779 python -m ai_strategy_loop --host 127.0.0.1 --port 8779
STOM_WEBBT_JOBS_DIR=ai_strategy_loop/state/webbt_jobs_8780 python -m ai_strategy_loop --host 127.0.0.1 --port 8780
```

필요한 수만큼 8781·8782도 동일하게 분리한다. 여러 서버가 같은 jobs dir를 공유하면 안 된다.

### 7.3 기존 D3 runner 주의

```bash
python -m ai_strategy_loop.labeling.run_d3_engine_screen \
  --base-urls http://127.0.0.1:8779,http://127.0.0.1:8780 \
  --workers 2 --start 20231114 --end 20231114 \
  --engines 1 --job-timeout 150 --poll-timeout 240
```

이 명령은 역사 screen 재현 예시일 뿐 새 연구 Batch 명령이 아니다.

Checkpoint resume는 manifest/config hash가 같을 때 `success`와 `no_trades`+source-match만 재사용한다. 기존 error/timeout은 다시 제출된다. config가 다르면 기존 output을 재사용할 수 없으며 **새 Evidence 경로**를 지정해야 한다. 기존 `2026-08-15_d3_engine_screen.json`을 덮어쓰지 않는다.

취소는 인증된 Dashboard Client가 호출하는 `POST /bt/job/cancel`을 사용한다. blanket `taskkill`과 무관 Python 프로세스 종료는 금지한다.

### 7.4 검증

변경 범위에 맞는 focused tests 후 최종 union에 대해 실행한다.

```bash
python -m pytest tests/unit/test_d3_state_machine.py tests/unit/test_d3_mcap_qmc.py \
  tests/unit/test_d3_engine_screen.py tests/unit/test_d3_screen_decision.py -q
python scripts/audit_condition_process_platform.py
python scripts/verify_nonrelease_sync.py
python scripts/build_research_docs_index.py
git diff --check
```

전체 `tests/unit/`는 7,700개 이상으로 단일 1,800초 실행이 timeout된 이력이 있다. 필요하면 서로 겹치지 않는 파일 shard로 전수 실행하되 합계·skip·warning을 정직하게 기록한다.

---

## 8. 안전·연구 불변식

- 신규 데이터 수집 없음. 기존 DB만 immutable read-only로 사용한다.
- `_database/`, `_database_v3k_shadow/`, `_log/`, `backup/`, `*.db`, `backtest/graph/`를 수정하지 않는다.
- 전략 DB write 없이 direct source snapshot을 사용한다.
- Band 경계 3000·5000·10000은 고정한다.
- D1/D2/Paired·D3 실패 주변 threshold 미세조정을 하지 않는다.
- 양수 월·좋은 거래를 사후 선택하지 않는다.
- 후보 선택에 PnL을 사용하지 않는다.
- 실행 실패와 경제 실패를 분리한다.
- 플랫폼 PASS를 경제적 성공으로 표현하지 않는다.
- development Evidence를 OOS·실전·자동채택으로 표현하지 않는다.
- Rule/controls/folds/posterior 없이 BO를 실행하지 않는다.

---

## 9. 재개 보고 형식

매 보고에서 최소 다음을 분리한다.

| 구분 | 필수 내용 |
|---|---|
| 플랫폼 진행률 | API·UI·DB 경계·engine 실행성 |
| 연구 진행률 | Band·세대·후보·유효 실행·분석·개선 반복 수 |
| 실행 실패 | error·timeout·원인·재실행 상태 |
| 경제 성과 | 거래수·평균·총손익·MDD·표본력 |
| 통계 상태 | controls·folds·FDR·Bayesian |
| 권위 | development/OOS/live/adoption 구분 |
| 잔여 작업 | 다음 Band/세대·중지 기준 |

연구 완료는 Band별 반복 lineage와 사전등록 중지 근거가 있을 때만 선언한다. 플랫폼 체크리스트만으로 연구 완료를 선언하지 않는다.

---

## 10. 현재 최종 판정

| 항목 | 상태 |
|---|---|
| 플랫폼·Census·시드 생성 기반 | 완료 |
| D3 단일 일자 feasibility screen | 수행, execution failure 17 포함 |
| `<3000` 반복 연구 | **미완료** |
| 나머지 3Band 반복 연구 | **미완료** |
| Negative controls 실제 실행 | 미실행 |
| Nested folds·통계 판정 | 미실행 |
| D4 Native 최적화 | 미실행 |
| 경제적으로 유효한 신규 조건식 | 발견되지 않음 |
| 전체 사용자 연구 지시 | **미완료 — 이 문서의 P1부터 재개** |
