# E2 실행가능성·비용계약 결과 (2026-08-14)

> 선행 증거: `2026-08-14_E1_worker_원인규명과_G2_교정결과.md`
> 구현: `ai_strategy_loop/revision/execution_contract.py`
> 권위: 정적 진단·후보 거부 전용, 실행·수익·채택 권한 없음

## 1. E2 범위 교정

E1 실측에서 기준선과 교정 G2의 worker loop 비용은 거의 같았다.

| 항목 | 기준선 | 교정 G2 |
|---|---:|---:|
| worker tick 중앙값 | 131,959 | 131,549 |
| worker loop | 약 1.96~2.11초 | 약 2.22~2.27초 |
| 완료 worker | 15/15 | 15/15 |

따라서 대형 회귀 비용모델이나 worker 타이머를 추가하는 대신, 실제 실패 원인이었던 runtime symbol 완결성을 최우선 비용계약으로 고정했다. 실행조차 불가능한 후보의 비용은 무한대로 취급해 엔진 전에 거부한다.

## 2. 계약

후보는 다음을 모두 만족해야 한다.

1. Python/STOM 구문 파싱
2. canonical AST unknown line 0
3. 허용 함수만 호출
4. lookback 상한
5. 절 수 상한
6. estimated work 상한
7. stock tick runtime 제공 symbol 또는 앞선 top-level assignment만 사용
8. `authority=diagnostic_only_no_adoption`

## 3. E1 결함 재현

| 입력 | 판정 |
|---|---|
| 교정 전 `현재가 < VI아래5호가` | `undefined_runtime_symbol` 거부 |
| `VI아래5호가 = VI가격 - VI호가단위 * 5` 선행 | 통과 |
| assignment 이후에 처음 사용 | 거부 |
| 미허용 함수 | `disallowed_function` |
| work budget 초과 | `estimated_work_exceeded` |

실제 교정 G2 source hash `06c7399d...53ba0`은 다음 결과로 통과했다.

- clause count 10
- estimated work 10.0
- unknown line 0
- undefined symbol 0
- adoption 권한 없음

## 4. 소스 구조 영향

STOM 엔진·조건식 문법을 바꾸지 않았다. 새 모듈은 기존 source를 읽어 engine exec 전에 거부/통과 receipt를 만드는 research control-plane 계층이다.

## 5. 다음 게이트

D1 후보 생성은 이 계약을 통과한 source만 받을 수 있다. 초기 예산은 다음으로 제한한다.

1. QMC로 저차원 문턱 피복
2. 서로 다른 구조 가족을 명시적으로 분리
3. Pareto/MAP-Elites archive는 실행 전 candidate diversity만 보존
4. 공식 엔진은 소수 후보만 사용
5. 결과는 개발 fixture이며 OOS로 표현하지 않음
