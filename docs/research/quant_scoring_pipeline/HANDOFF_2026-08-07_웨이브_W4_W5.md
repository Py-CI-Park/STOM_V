# 핸드오프 — 웨이브 W4~W5 (2026-08-07)

> 브랜치 `wave/w4b-engine-measure` · base `research/program-reassessment-20260806`
> **다음 세션은 이 문서 하나로 재개할 수 있다.** 세부는 §9 문서 지도 참조.
> 안전 경계: 운영 DB 쓰기·실거래 반영·전체청산 이후 시세·홀드아웃 재학습·후보 자동 채택 금지

---

## 1. 🧭 지금 어디에 있나 (한 문단)

QSP10~13 은 "진입 조건 대량 탐색"으로 실패했고, 앞 세션이 **매도 축**을 열어 지도가
챔피언을 재현하게 만들었다(전이율 0.71). 이번 세션은 그 지도 수치를 **엔진에 올렸다** —
진입을 고정하고 매도만 바꾼 A/B 에서 도전자 3종이 전부 챔피언의 건당 수익과 MDD 를
넘었다. 동시에 두 가지를 배웠다: **건당 이득이 곧 자본 이득은 아니고**(CAGR 56→31),
**전이율은 무엇을 바꿔 가며 쟀는지에 따라 방향이 반대**다. min 레인 155일도 처음
읽었고, 결론은 "min 도 공짜 점심은 없다"였다.

---

## 2. ✅ 이번 세션에 완료한 것

| 웨이브 | 산출 | 상태 |
|---|---|---|
| **W4-b** | 트레일링 매도식 엔진 렌더러 + 진입 고정 A/B 러너 + **엔진 실측 완주** | ✅ |
| **W4-c** | 페이지 28 매도 축 종합 (지도·워크포워드·엔진 한 줄) | ✅ |
| **W4-d** | 넓은 격자(36쌍) 선언 + 러너 배선 + `design_v5` 빌드 | ⏳ 빌드 중 |
| **W5-a** | min 레인 v4 재빌드(155일·795MB) + **최초 판독** | ✅ |
| **W5-b** | 상설화 계획기(백필·재검증) + 페이지 29 | ✅ |
| 보고 | 정본 md + HTML 보고서(매니페스트 verified) + Artifact | ✅ |

---

## 3. 🎯 핵심 실측 (이번 세션이 만든 새 지식)

### 3.1 엔진 A/B — 설계 355일 · 진입 `Tick_B_902_905` 고정

| 팔 | 매도 | 거래 | 건당 | CAGR | MDD | Δ건당 |
|---|---|---:|---:|---:|---:|---:|
| **A** | 챔피언 원본(8종) | 161 | **+0.50%** | **56.30** | 14.82 | — |
| B1 | trailing(arm+3/give1.5) | 157 | +0.56% | 30.80 | 12.43 | +0.06%p |
| B2 | trailing(arm+3/give1) | 158 | +0.55% | 30.63 | 12.29 | +0.05%p |
| **B3** | **trailing(arm+5/give2)** | 155 | **+0.92%** | 49.98 | **10.48** | **+0.42%p** |

- **부호가 맞았다** — QSP12 의 전이율 −1.18(부호 반전)과 정반대.
- **건당 이득 ≠ 자본 이득** — B1·B2 는 CAGR 을 절반 가까이 잃었다(회전 저하).
- **B3 만** 건당·MDD·CAGR 을 함께 감당할 자리에 있다.
- 거래 수가 거의 같아(155~161) 차이가 오직 매도에서 왔음이 분명하다.
- **이 구간은 in-sample 이다** — 청산 규칙을 고른 바로 그 구간.

### 3.2 전이율은 축에 따라 방향이 반대다

| 비교 축 | 표본 | 전이율 | 지도의 편향 |
|---|---:|---|---|
| 진입까지 함께 변경 | 2 | 0.105 · 0.343 | **과대**평가 → 깎아야 함 |
| 진입 고정 · 청산만 변경 | 3 | 2.247 · 2.370 · 4.216 | **과소**평가 |

섞은 중앙값 2.247 을 배리어 후보에 쓰면 20배 과대추정이 된다. 축을 나눠 읽는다.

### 3.3 min 레인 — 공짜 점심 없음

집행 우주 1,144,017행 · 155일 · **판정 가능 27셀 중 양수 0**.
최고 정확 셀 `barrier(TP+5/SL-1, 60분)` −0.4035%. tick 무필터(28셀 중 양수 0,
−0.43~−0.71%)와 **같은 자리**다.

→ **min 챔피언(검증된 흑자 전략)이 없으면 min 탐색을 시작하지 않는다.**
그것 없이 하면 QSP10~13 을 min 에서 반복한다.

### 3.4 부수 관측 — 자본 제약이 유리하게 자른다

지도 진입 344건 → 엔진 체결 155~161건(45%). 그 45%의 건당 성적이 지도 전체의
2~4배다. 자본 한도·동시보유가 **무작위가 아니라 유리한 쪽으로** 자르고 있다.
현재는 관측일 뿐 설명이 없다.

---

## 4. 🔴 이번 세션에 잡은 결함 (누적 6~9번째, 전부 "조용히 잘못된 값" 유형)

| # | 결함 | 발견 경로 | 교정 |
|---:|---|---|---|
| 6 | 전이율을 축 없이 섞음 — 방향 반대인 두 무리의 중앙값 | 원장에 5건 쌓이자 계수가 이상 | 축별 계수 + `axis_mixed` 경고 |
| 7 | 긴 러너가 세션 만료(401)로 사망 | 두 번째 전략 등록에서 중단 | 401 시 세션 재발급 후 1회 재시도 |
| 8 | 중간 결과 미저장 — 위 401 로 A·B1 결과 통째 소실 | 위와 동시 | 팔 하나마다 리포트 저장 |
| 9 | **보고서 매니페스트가 조용히 unverified** — CRLF 변환으로 바이트 불일치 | 신규 보고서 등록 중 전량 검증 | `.gitattributes` eol=lf 고정 + LF 정규화 + **회귀 테스트 6종** |

---

## 5. ⬅ 다음 한 걸음

```
1) design_v5(wide) 빌드 완료 확인
   python -c "import json;print(json.load(open('ai_strategy_loop/state/labels/design_v5/_build_summary.json',encoding='utf-8')))"

2) 넓은 격자 재판정 — 후보 6→36 일 때 편의가 얼마나 커지는가
   python -m ai_strategy_loop.labeling.run_reproduction_gate --out-name design_v5 --grid wide
   python -m ai_strategy_loop.labeling.run_exit_walkforward  --out-name design_v5 --grid wide
   · 비교 기준: 후보 28셀일 때 간극 0.1805%p / 수백만 조합일 때 0.6225%p
   · **최고 셀을 뽑아 쓰지 않는다** — 이 실험의 목적은 편의 측정이다

3) B3(arm+5/give2) 검증 사다리 3종 (labeling/ladder.py)
   · 기간 분할 · 자본 민감도 · 비용 민감도
   · 전부 통과해야 사람 보고. 통과해도 채택은 사용자 결정

4) 자본 제약이 왜 유리하게 자르는가 (§3.4) — 체결 순서·슬롯 경합 관측

5) (보류) min 레인 탐색 — min 챔피언 확보 전에는 시작하지 않는다
```

### 엔진 실측 재현 방법

```bash
# 서버 (전략 쓰기 권한 필요 — W4_S_* 이름공간에만 쓴다)
STOM_ALLOW_MINIMAL_SETTING=1 STOM_DASHBOARD_ALLOW_STRATEGY_WRITE=1 \
  python -m ai_strategy_loop --port 8771

# A/B (약 23분 · 4팔)
STOM_ALLOW_MINIMAL_SETTING=1 python -m ai_strategy_loop.labeling.run_engine_measure \
  --out-name design_v4 --top 3 --engines 16 --timeout 7200
```

---

## 6. 📐 헌법 (변경 없음 — 반드시 지킬 것)

1. **사람 게이트는 실전 투입 1곳** — 루프 내부 판독·수정 결정은 Claude 가 수행한다.
2. **격자·수정은 실행 전 고정하고 전셀 보고** — 좋은 셀만 골라 보고하면 그게 편의다.
3. **홀드아웃 재개봉 금지** — 판정은 워크포워드로. 경계는 전진만 한다.
4. **지도 수치는 반드시 엔진 실측·전이율 기록을 거친다.**
5. **상한(미래 참조) 값은 판정 근거가 아니다** — 천장으로만 읽는다.
6. **수정 예산 15회/아이디어** — 초과 시 폐기하고 원장에 기록한다.
7. **in-sample 채굴(백파인더 등)은 시드 전용** — 심판 금지.
8. 실패 전량 기록 · 정정 문서 규칙 유지(INDEX §5).

**이번 세션이 추가한 실무 규칙 2가지**

9. **전이율은 축(무엇을 바꿨는가)을 밝히지 않으면 인용하지 않는다.**
10. **레인에 챔피언이 없으면 그 레인에서 탐색을 시작하지 않는다.**

---

## 7. 🗂 코드 자산 지도 (이번 세션 신규/변경)

### 연구 파이프라인 (`ai_strategy_loop/labeling/`)

| 모듈 | 역할 | 상태 |
|---|---|---|
| `run_engine_measure.py` | **진입 고정 A/B 엔진 실측** — 팔마다 즉시 저장 | 신규 |
| `run_min_lane_read.py` | min 레인 판독기(분 단위 격자) | 신규 |
| `assembler.py` | `render_trailing_sell_expression` — 커널과 동치임을 테스트로 고정 | 변경 |
| `trailing.py` | `WIDE_TRAILING_GRID`(36쌍) · `GRIDS` · `resolve_grid` | 변경 |
| `label_factory.py` `build_labels.py` | 격자 인자화 · `--trailing-grid` · 요약에 격자 기록 | 변경 |
| `exit_axis.py` · 러너 3종 | `default_grid(trailing_grid=…)` · `--grid` | 변경 |
| `run_p5.py` | `Client` 가 401 에 세션 재발급 후 1회 재시도 | 변경 |

### 루프·대시보드

| 모듈 | 역할 | 상태 |
|---|---|---|
| `controller/standing.py` | 백필·재검증 **계획기**(순수 함수·홀드아웃 잠금) | 신규 |
| `dashboard/exit_axis_api.py` + `frontend/bt-exit-axis.jsx` | 페이지 28 | 신규 |
| `dashboard/autoloop_api.py`(`/loop/standing`) + `frontend/loop-standing.jsx` | 페이지 29 | 신규 |
| `dashboard/transfer_ledger_api.py` + `frontend/bt-transfer-ledger.jsx` | **축 분리**·`estimate?axis=` | 변경 |

### 데이터

| 자산 | 크기 | 비고 |
|---|---|---|
| `state/labels/design_v4/` | 3.9GB | 355일 · 트레일링 6쌍 ← **현재 정본** |
| `state/labels/design_v5/` | ~6.8GB | 355일 · 트레일링 **36쌍**(wide) ← 빌드 중 |
| `state/labels/min_design_v4/` | 795MB | 155일 · 트레일링 포함 · 최초 판독 완료 |
| `state/labels/min_design_v2/` | 545MB | 구버전(트레일링 없음) |

### 산출 JSON

| 파일 | 내용 |
|---|---|
| `design_v4/_p5_engine_report.json` | 엔진 A/B 결과 (페이지 12·28 이 읽음) |
| `design_v4/_reproduction_gate.json` · `_exit_walkforward.json` | 지도·워크포워드 |
| `min_design_v4/_min_lane_read.json` | min 최초 판독 |

---

## 8. 🔁 재개 절차

```bash
cd C:/System_Trading/STOM/STOM_V.wt-dev
git switch wave/w4b-engine-measure     # 또는 병합 후 research/program-reassessment-20260806

python -m pytest tests/unit -q                    # 기대: 7000+ passed, 0 failed
cd ai_strategy_loop/dashboard/webui-build && node runtime-jsx-check.mjs   # 기대: 125 PASS
```

### 재개 마스터 프롬프트

```text
STOM 웨이브 자율 루프 — W4-d 넓은 격자 판정과 B3 검증 사다리를 진행하세요.
저장소: C:/System_Trading/STOM/STOM_V.wt-dev
브랜치: wave/w4b-engine-measure (base=research/program-reassessment-20260806)
먼저 읽기: docs/research/quant_scoring_pipeline/HANDOFF_2026-08-07_웨이브_W4_W5.md
→ 2026-08-07_wave_w4_w5_results.md → 2026-08-06_master_wave_plan.md

확정 전제:
- 엔진 A/B 완주 — 진입 고정, 도전자 3종 전부 기준선(건당 +0.50%) 상회.
  최고 B3 trailing(arm+5/give2) 건당 +0.92% · MDD 10.48 (기준선 14.82) · CAGR 49.98
- 단 B1/B2 는 CAGR 56→31 로 손실 — 건당 이득이 곧 자본 이득이 아니다
- 전이율은 축에 따라 방향이 반대다(진입 변경 0.1~0.34 / 청산만 2.2~4.2)
- min 레인 무필터 기저는 tick 과 같은 자리(양수 0) — min 챔피언 없이 탐색 금지
- 라벨 정본 = design_v4, 넓은 격자 실험 = design_v5
- 헌법 10개 항(§6) 전부 준수

다음 작업: 핸드오프 §5 — 넓은 격자 재판정(편의 측정) → B3 사다리 3종 → 사용자 승인.
설계 구간 결과는 in-sample 이다. 사다리를 거치지 않은 채택은 헌법 위반이다.
```

---

## 9. 📚 문서 지도

| 순서 | 문서 | 내용 |
|---:|---|---|
| **0** | **이 핸드오프** | 재개 지점 |
| 1 | `2026-08-07_wave_w4_w5_results.md` | W4~W5 실행 결과 정본 |
| 1.5 | `reports/2026-08-07_wave_w4_w5_report.html` | 위의 HTML 보고서(대시보드 [보고서] 탭) |
| 2 | `HANDOFF_2026-08-06_웨이브_자율루프.md` | 직전 핸드오프 |
| 3 | `2026-08-06_wave_w09_w4_results.md` | W0.9~W4 실행 결과 |
| 4 | `2026-08-06_master_wave_plan.md` | 웨이브 W0~W5 계획 · 페이지 계획 |
| 5 | `2026-08-06_program_reassessment.md` | 프로그램 재평가 — 왜 실패하는가 |
| 6 | `limitation_ledger.md` | 한계·결함 원장(누적) |
