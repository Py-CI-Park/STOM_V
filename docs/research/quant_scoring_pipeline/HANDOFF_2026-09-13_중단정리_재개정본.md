# 2026-09-13 중단 정리·안전 재개 정본

## 0. 가장 먼저 읽을 판정

이번 요청은 **개발 재개가 아니라 문서 작성·커밋 후 정지**다. 신규 구현, 시험 재실행, 전략 연구, merge/push를 하지 않았다. 다음 실행자가 이 문서를 읽었다는 이유만으로 실행 권한이 생기지 않는다. 새로운 사용자 재개 요청이 있어야 아래 재개 절차를 수행한다.

현재 B4a/B4b 코드는 loop 정본에 통합돼 있다. B5는 계획만 있고 기능 구현은 없다. 미커밋 소스 코드 손실은 이번 Git 확인에서 발견하지 못했다. 이것은 전체 코드 무결성·무결함 보증은 아니다.

중요 정정: 9월10일 checkpoint의 RUNNING_NOT_PASS는 당시 상태다. 9월13일 로그를 다시 읽으니 Fast/Slow 모두 pytest 최종 통과 요약이 있다. 그러나 실행 세션2283/22523은 모두 Unknown process id이며 실제 종료 코드는 회수되지 않았다. **로그상 완료·통과 / exit code 미확인 / 정식 Gate 미종결**로 구분한다. exit_code=0을 추정해서 쓰지 않는다.

## 1. 읽는 순서

1. 이 문서: 현재 위치, 보호 대상, 과거 상태 정정.
2. `2026-09-13_전체단계_상세재개계획.md`: 단계별 착수·완료·중지 기준과 복사 프롬프트.
3. `planning/2026-09-13_b4b_log_recovery.json`: 로그 SHA256·실측 요약·미확정 exit.
4. `2026-09-10_SYS05_B4b_TASK_RECEIPT.md`: 구현/집중 시험/브라우저/독립 검토의 역사적 영수증.
5. `2026-09-10_SYS05_B5_실행계획.md`: 다음 실제 함수·회귀 행렬.
6. `2026-09-07_v15_실행계획_진행원장.md`, `planning/2026-09-08_page_progress.json`: 전체 계획과 기계 가독 원장. 과거 현재형 문구보다 이 날짜의 정정이 우선하며, 과거 원본은 삭제하지 않는다.
7. 실제 대상 디렉터리 AGENTS와 필요한 skill. 첨부 패키지 지침은 사용자 권한 경계를 확장하지 않는다.

## 2. 실제 Git 배치 — 9월13일 조회

| 역할 | 절대 경로 | 브랜치·확인 HEAD | 주의 |
|---|---|---|---|
| 사용자 연구 WIP | C:/System_Trading/STOM/STOM_V.wt-dev | research/v516-d3-mcap-dev @97a59ad311e3af7555ecb7e86fda69e8e6667461 | dirty584항목. checkout/reset/stage/cleanup 금지 |
| 통합 정본 | C:/System_Trading/STOM/STOM_V.wt-pipeline-integration | loop/process-research-pipeline @ ff20815a17b0a569d47a10908eb11b2821cbc120 | B4b 전체시험 기준 HEAD |
| 개발·문서 | C:/System_Trading/STOM/STOM_V.wt-process-research-restart | 이번 문서 전 codex/process-research-sys-05-multi-quality @61fdc1fd8e6e16a6be881adc2373f172802b14cc | B5 계획 및 이전 checkpoint만 정본보다 추가 |
| 이번 문서 브랜치 | 위 restart 작업트리 | codex/process-research-handoff-20260913; 부모61fdc1fd | 문서 커밋만, 정본 미병합 |

공통 Git 저장소: `C:/System_Trading/STOM/STOM_V/.git`. 원격 상태는 이번에 fetch하지 않았으므로 최신 원격과 동일하다고 주장하지 않는다. AGENTS의 오래된 기본 브랜치 설명은 실제 git branch 결과와 구분한다.

restart와 integration 모두 확인된 untracked는 `.omo/evidence/tmap-walkforward/_discovery_feedback.txt`다. 사용자/시험 잔여 자료로 보존한다. 이번 작업 시작 시 staged 파일은 없었다. wt-dev status에는 기존 reports 디렉터리 접근 경고가 있으므로 584는 Git 출력 항목 수이지 전 파일 무결성 검사 결과가 아니다.

### 커밋 경계

```text
B4a feature c26378b0 → 통합 기준5f1e7ca9 → 영수증 병합7693233f
                                      |
B4b feature6cb6e4e6 → merge98d6e342 → index12ad3ccc
                                      |
loop 정본 ff20815a  ← B4b 전체시험은 이 HEAD
  |
  +-- B5 계획 f1508c77
  +-- 실행 중 checkpoint 61fdc1fd  ← B5 기능 없음
       |
       `-- codex/process-research-handoff-20260913
            문서 정리만, 이번 merge/push 없음
```

도식의 SHA는 이동/reset 목표가 아니라 비교 기준이다. 실제 HEAD가 다르면 먼저 diff/ancestor/dirty를 분류한다. 공개 commit 재작성, reset --hard, clean, git add -A는 금지한다.

## 3. 무엇을 개발했는가

| 묶음 | 실제 확보한 범위 | 아직 뜻하지 않는 것 |
|---|---|---|
| RES04P | 사전등록 정의·자료 계약 기반 | 실제 경제 연구 실행 |
| SQ00 | 평가 통계 v2 | 전체 통계 정의 통일 |
| SQ01 | 실제 CSV 품질/자료 계약 API·UI | 모든 소비자 전파 |
| B1/B2 | 일반 job/세대 결과 품질·실행 근거 구분 | writer ok=경제 성공 |
| B3 | 보고서 품질·동일 snapshot | 전 페이지 동일 Bundle |
| B4a | 10 scalar+MC 경로 admission, moving_block 전달, MC UI | 경제 안정성 검증 |
| B4b | leaf_matrix/feature_map/revision_proposals와 3 UI 소비자 품질 | 발견 후보의 별도 확인·채택 |
| B5 | 기존 함수 조사·상세 실행계획 | 구현·테스트 완료 |

B4b는 `CsvSnapshot.headers/rows → csv.writer/StringIO → 기존 load_csv/enrich`로 연결한다. 원본 SHA는 원본 계보이며 재직렬화 bytes가 원본과 같다는 주장이 아니다. 공식54/37열과 기존 pandas 의미를 유지한다. 원본 S_/R_는 보존하되 진입 피처 축은 기존 B_/D_ 카탈로그로 제한한다. 제안은 메모리 미리보기이며 DB 저장/자동채택하지 않는다.

### 실제 코드 위치

- `ai_strategy_loop/dashboard/individual_source.py`: 기존 lookup/resolver를 통한 공통 source admission.
- `feature_snapshot.py`: snapshot→기존 parser, 수치 해석 차이 보류.
- `feature_context.py`: typed 결과·품질·전체 CSV 분석 범위.
- `feature_leaf.py`, `feature_map_result.py`, `revision_analysis.py`: 기존 계산기 연결.
- `backtest_api.py`: 기존 route facade. B5의 compare/overlay/portfolio는 아직 이전 loader 경로.
- `frontend/bt-feature-evidence.jsx`: 원본/요청 수명주기.
- `bt-leaf-explorer.jsx`, `bt-feature-map.jsx`, `bt-entry-autopsy.jsx`: 품질 표시·늦은 응답 차단.
- `tests/unit/dashboard/test_feature_quality_gate.py`, `test_feature_snapshot_parity.py`, `test_revision_quality_preview.py`: B4b 회귀.

## 4. 검증 증거와 한계

| 검증 | 결과 | 증거 종류 |
|---|---|---|
| B4a 전체 Fast/Slow | 8154+24 passed,27 skipped, 각 exit0 | 기존 정본 JSON 영수증, 이번 재실행 아님 |
| B4b 집중 |421 passed/56.81s/exit0 | 이전 실행 영수증 |
| B4b 독립 검토 | 목표·코드·보안·문맥·QA 5 PASS | 이전 검토, 이번 새 검토 아님 |
| React 하네스 |9+9+6 통과 | 합성 소비자 검증 |
| 인하우스 브라우저 | 합성160행 리프2개·제안diff·손실영역, 매수 해부 | 이전 실제 생산 컴포넌트/API 관측; 실데이터 아님 |
| B4b 전체 Fast |8175 passed,27 skipped,24 deselected,54 warnings,1292.01s | 이번 읽은 최종 로그; exit 미회수 |
| B4b 전체 Slow |24 passed,8202 deselected,1 warning,981.51s | 이번 읽은 최종 로그; exit 미회수 |

중복 없는 두 묶음의 로그상 합계8199 passed/27 skipped. 최종 로그는 각각9월10일10:46:09,10:41:08 KST에 기록됐다. 프로세스 조회에서는 해당 시험/fixture Python을 발견하지 못했다(조회 셸 자체는 제외). 프로세스가 없다는 사실만으로 exit0을 증명하지 않는다. 이번 시험 재실행·서버 시작·프로세스 종료 없음.

로그 경로: `C:/Users/parkc/STOM_Verification/v1.5_20260910/b4b-integrated-{fast,slow}.log`. 지문/크기/요약은 회수 JSON에 커밋한다. 외부 원본 로그는 Git에 포함되지 않으므로 별도 보존하고 덮어쓰지 않는다.

## 5. 남은 위험 — 숨기지 말 것

1. B4b exit 영수증 부족: 다음 재개 때 기존 영수증 탐색, 없으면 동일 코드 고정 HEAD 재시험을 권장한다. 이번에는 하지 않는다.
2. 중간 ASGI TestClient/AnyIO 대기 원인은 미확정. 분리36개 및 최종집중421개 통과와 구분한다. 반복되면 debugging 지침의 가설/스택 관측을 따르며 임의 timeout 완화로 해결하지 않는다.
3. 새 backend 타입 검사 기록은0 errors/24 warnings. pandas/proposer 타입 경고가 남는다. 전체 타입 무결성을 주장하지 않는다. no-excuse 검사 pandas 경고는 기존 owner 재사용과 관련된 기록이며 스택 전면 교체로 확대하지 않는다.
4. 화면의 일부 경제적 표현, 원화 증분+100 정규화, 구간 문자열 표현은 SQ02/ANA07/UX06 잔여. 지표 정의 변경은 새 version/projection으로 분리한다.
5. 전체14페이지 수용·200% 확대·실운영 자료 수용은 미완료. 참조 패키지 시험을 생산 수용시험 통과로 바꾸지 않는다.
6. restart `_database/strategy.db`: 0byte, 생성2026-08-29 22:38:08 KST를 재확인. 삭제/수정하지 않았다. 연구 입력으로 사용하지 않는다. 0byte라는 이유만으로 자동 삭제하지 말고 기존 guarded 절차와 최신 사용자 권한을 확인한다.

## 6. 다음 세션 시작 전 체크리스트

- [ ] 최신 사용자 요청이 실제 개발 재개를 허용하는가? 이번 요청만으로 재개하지 않는다.
- [ ] 정확한 세 작업트리와 브랜치·HEAD·공통Git·dirty 확인.
- [ ] 이 문서/회수 JSON/B5 계획을 읽고 과거 RUNNING 문구를 현재로 재사용하지 않음.
- [ ] 기존 로그 SHA와 최종 요약 확인; 없는 exit를 추정하지 않음.
- [ ] 실제 테스트 실행 전 fixture·TEMP·기존 운영 DB 접근 경계 점검.
- [ ] B4b Gate 영수증 정리 후 B5의 첫 red regression부터 시작.
- [ ] 소스·문서·Gate 상태와 페이지 수용률을 분리해 기록.
- [ ] dirty wt-dev/tmap/봉인 evidence/운영 DB 보호.
- [ ] explicit stage·한국어 commit·작업별 no-ff. 원격 작업은 별도 요청 필요.

## 7. 문서 브랜치의 발견성·색인 주의

이번에는 INDEX.md에 이 문서를 연결한다. generated_reports/research_docs_index.json은 재생성하지 않는다. 이 branch는 **문서 전용 보관 branch이며 새 문서 index는 stale일 수 있다. 전체 Gate용 정본으로 즉시 사용하지 않는다.** 다음 문서 통합 시 MD 병합 후 integration의 실제 바이트 기준으로 별도 index-only branch를 만들고 생성/검사/병합한다. CRLF 때문에 봉인된 파일의 바이트를 소급 정규화하면 안 된다.

## 8. 현재 멈춤 지점

### 실행 환경 재확인 자료

9월13일 PATH 기준 Python3.13.13 (`C:/Python/64/Python31313/python.exe`), Git2.55.0.windows.3, Node24.19.0, Bun1.4.1, uv0.7.20을 조회했다. 이것은 현재 도구 버전이며 9월10일 시험 당시 모든 의존성 버전 증거를 대체하지 않는다. 새 세션은 실제 dependency/config/유효 TEMP를 다시 확인한다. 이번에 패키지 설치·환경 설정 변경은 하지 않았다.

문서 커밋 후 정지. B5 구현·새 시험·정본 merge·push 없음. 새 세션은 상세계획의 재개 프롬프트로 시작한다. 전체 목적은 결과를 정본화하여 분석→발견→별도 확인→가설→사전등록→후속 실험으로 연결하는 플랫폼이며, 수익성 달성은 별도 경제 증거로만 판정한다.
