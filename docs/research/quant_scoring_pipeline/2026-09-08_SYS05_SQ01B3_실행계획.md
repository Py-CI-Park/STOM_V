# SQ01B3 보고서 품질 연결

branch codex/process-research-sys-05-report-quality, 부모 B2 @b522a71a. 정본 전체 unit이 고정2cea8914에서 실행 중이므로 후속 branch는 검증된 B2 위에 임시 적층한다. 통합 시 B2→B3 순서의 각각 no-ff 병합으로 보존한다.

실제 문제: _report_payload_for_job/run은 기존 list loader를 직접 호출하여 화면의 CSV 품질 차단을 우회하고 MC도 계산한다. 같은 snapshot에서 품질·분석·MC를 조립하며 HTML에는 품질/실행/원본hash와 기계용 JSON을 함께 남긴다. 품질 불량 또는 실행 불가 시 분석/MC를 호출하지 않는다. noCSV 세대의 저장 요약은 미검증 표시를 유지한다.

생산 API import 기반 red → 기존 owner와 typed helper 최소 수정 → 기존 보고서/세대/품질 회귀 → HTML XSS/동일hash/차단 계산 없음 → 실제 합성 브라우저 → 5영역 독립검토 → receipt/원장 → 명시 commit. 새 Bundle namespace/DB/table/경제 연구/정의 변경은 범위 밖이다. 롤백은 해당 기능 새 역변경 commit이며 과거 원본을 변경하지 않는다.
