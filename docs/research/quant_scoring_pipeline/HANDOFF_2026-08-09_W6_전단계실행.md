# 핸드오프 — W6 전 단계 실행 (2026-08-09)

> 브랜치 `research/program-reassessment-20260806`
> **다음 세션은 이 문서 하나로 재개할 수 있다.**
> 안전 경계: 운영 DB 쓰기·실거래 반영·전체청산 이후 시세·홀드아웃 재학습·후보 자동 채택 금지
> **홀드아웃 119일(20250825~) 미접촉** 🔒

---

## 1. 🧭 지금 어디에 있나

마스터 로드맵 5단계를 전부 실행했고, 실행이 만든 합성 가설까지 돌렸다.

> **회전과 우위는 직접 교환 관계다.** 이것이 이번 라운드의 확정 지식이다.
> 자본을 돌리는 규칙을 넣으면 건당 우위가 깎이고, 우위를 주는 규칙을 함께 넣어도
> 돌아오지 않는다.

| 축 | 통과한 후보 | 실패한 축 |
|---|---|---|
| **건당 (통계 확정)** | B3 trailing(5/2) · B3+추세이탈 | 자본 (동시보유 2) |
| **자본 대비** | B3+시간손절 · **합성(301.39%)** · 진입 −905_전일비 | 건당 (CI 가 0 포함) |

**원장: PASS 0 · PROMISING 3 · MIXED 6 · REJECT 2 · BASELINE 1** (후보 12종 · 30행)

---

## 2. ⬅ 다음 한 걸음 (우선순위)

### 1) 교환 곡선 찾기 — 합성이 실패해서 남은 유일한 길

합성(`trend_then_time`)은 **자본은 얻고 우위는 못 되찾았다**(§3.1). 시간손절이
자르는 거래에 "되살아났을 거래"가 섞여 있다는 뜻이다. 그러면 **덜 자르면서**
동시보유 1을 지킬 수 있는 지점이 있는지 본다.

```python
# assembler.EARLY_EXIT_RULES 에 사전 등록할 격자 (실행 전 고정)
"time_stop_240": ("시간손절240", "보유시간 > 240 and 수익률 < 0"),
"time_stop_300": ("시간손절300", "보유시간 > 300 and 수익률 < 0"),
```

```bash
python -m ai_strategy_loop.labeling.run_capital_turnover \
    --out-name design_v4 --tag _ext --out-tag _curve --rules time_stop_240,time_stop_300
python -m ai_strategy_loop.labeling.run_capital_turnover \
    --out-name design_v4 --tag _ext --out-tag _curve --rebuild-report
python -m ai_strategy_loop.labeling.run_engine_ladder --out-name design_v4 --tag _curve_turn
python -m ai_strategy_loop.labeling.run_ledger_sync   --out-name design_v4 --tag _curve_turn
```

**PASS 조건**: 총수익률 ≥ 241.16% **AND** 최대동시보유 ≤ 1 **AND** 짝지은 CI 하한 > 0.
지금까지 셋을 동시에 넘은 후보는 없다.

### 2) 진입 완화 재실험 — 기준선을 B3 로

이번 라운드의 진입 완화는 **청산 축이 어긋나 실패했다**(§4.3). 지도는
`trailing(5/2)` 로 쟀는데 엔진 확인은 챔피언 청산으로 했다. 기준선을
`챔피언 진입 + trailing(5/2)` 로 바꾸면 바뀌는 것이 진입 절 하나뿐이 된다.

`run_entry_relax.py` 에 `--sell` 인자를 추가해 `W4_S_TRAIL_5_2` 를 넘기면 된다.

### 3) ✅ 완료 — `trailing(4/1)` 엔진 순위 확인

지도 1위였으나 엔진에서 2위였고 전이율도 최저(2.80)였다. 자본도 2배를 쓴다.
`run_engine_measure --gate-tag _wide --cells "trailing(arm+4/give1)"` 로 재현 가능.

### 4) 라벨 백필 완료 확인 (진행 중)

```bash
python -m ai_strategy_loop.labeling.build_labels --lane tick \
    --start 20220323 --end 20240301 --out-name design_v5 --trailing-grid wide
```
멱등이다. 끊겼으면 같은 명령으로 이어서 돌린다. **판정 표본과는 무관**하고
**탐색(지도)용**이다.

---

## 3. 📊 이번 라운드 실측 (구간 20220323~20250822 · 833 거래일)

### 3.1 매도 축

| 후보 | 거래 | 건당 | 평균보유 | 동시 | 필요자금 | 총수익률 | 짝지은 95% CI | 판정 |
|---|---:|---:|---:|---:|---:|---:|---|---|
| **챔피언** | 361 | +0.67% | 464초 | **1** | 1,004,095 | **241.16%** | — | 합격선 |
| B1 trailing(3/1.5) | 356 | +0.84% | 443초 | 2 | 1,993,320 | 148.66% | [−0.135, +0.437] | MIXED |
| B2 trailing(3/1) | 358 | +0.77% | 418초 | 2 | 1,991,640 | 138.66% | [−0.178, +0.385] | MIXED |
| B3 trailing(5/2) | 352 | **+1.06%** | 540초 | 2 | 1,996,660 | 185.66% | **[+0.059, +0.658]** ✅ | MIXED |
| B3+손절(−3%) | 355 | +0.94% | 432초 | 2 | 1,993,050 | 167.35% | [−0.027, +0.492] | MIXED |
| B3+시간손절 | 352 | +0.85% | 300초 | **1** | 1,002,500 | 296.02% | [−0.130, +0.426] | PROMISING |
| B3+추세이탈 | 353 | +0.99% | 402초 | 2 | 1,989,180 | 174.94% | **[+0.041, +0.536]** ✅ | MIXED |
| **합성(추세이탈+시간손절)** | 353 | +0.86% | **282초** | **1** | 1,002,000 | **301.39%** | [−0.111, +0.428] | **PROMISING** |
| trailing(4/1) (응답면 1위) | 353 | +0.85% | — | 2 | 1,995,375 | 150.15% | [−0.118, +0.422] | MIXED |

### 3.2 진입 축 (완화 엔진 확인)

매도는 챔피언 원본 고정, 매수 절 하나만 제거(헌법 7항).

| 지도 순위 | 후보 | 거래 | 건당 | 총수익률 | 게이트 |
|---:|---|---:|---:|---:|---|
| — | 챔피언(합격선) | 361 | +0.67% | 241.16% | — |
| 1위 | − 905_거래대금급증 | 390 (+29) | +0.62% | 238.15% | FAIL(근소) |
| 2위 | − 905_시가대비 | 489 (+128) | +0.40% | 97.79% | FAIL |
| **3위** | **− 905_전일비** | **378 (+17)** | **+0.68%** | **253.34%** | **통과** |

`905_전일비` 는 게이트를 넘었지만 **PROMISING 이 상한**이다 — 진입이 다르면
짝지은 검정을 쓸 수 없어 통계 확정 수단이 없다. 게다가 총수익률 이득(+12.18%p)의
거의 전부가 거래 수(+17)에서 오고 건당은 +0.01%p 로 오차 범위다.

### 3.3 매도축 응답면 (지도 36셀)

절벽 **0** · 음수 **0** · 고원 **33** · 경사 **3**.
**이 축에서는 과최적을 걱정할 필요가 없다.**

그러나 지도 순위는 못 믿는다 — 4셀 실측 대조에서 **순위가 정확히 역순**이었다.

| 셀 | 지도 순위 | 엔진 순위 | 전이율 |
|---|:---:|:---:|---:|
| trailing(4/1) | 1위 | 2위 | **2.80** |
| trailing(3/1.5) | 2위 | 3위 | 3.37 |
| trailing(3/1) | 3위 | 4위 | 3.32 |
| trailing(5/2) | 4위 | **1위** | **4.86** |

Spearman ρ = **−0.20**. 지도 1위가 전이율 최저, 지도 꼴찌가 최고다.

---

## 4. 🔑 이번 라운드의 결정적 지식

### 4.1 엔진 A/B 는 라벨을 쓰지 않는다

`run_engine_measure` 는 `--start/--end` 를 그대로 `/bt/run` 에 넘긴다.
표본을 늘리는 것은 **엔진 구간**이지 라벨이 아니다.
설계 구간이 tick DB 952일의 **37%**만 쓰고 있었다.

### 4.2 자본 회전의 레버는 평균이 아니라 **꼬리**다

`back_static_numba.py:57` — `mhct = arry_bct[상위1% 제외:, 1].max()`.
평균 432초 후보가 동시보유 2, 평균 464초 챔피언이 1이었다.

### 4.3 지도는 모양, 엔진은 순위

| 축 | 지도→엔진 전이 |
|---|---|
| 매도(청산) | 부호 유지, 배율 3.32~4.86 — **순위는 다름** |
| **진입** | **부호 뒤집힘** (지도 +0.063%p → 엔진 −0.27%p) · 지도 3위만 통과 |

진입축 실패의 원인은 축 불일치다: 지도 제거 A/B 는 `trailing(5/2)` 로,
엔진 확인은 챔피언 청산으로 쟀다. **비교하려면 청산 축이 같아야 한다.**
거래 수 예측은 정확했다(지도 1.37배 vs 엔진 1.35배) — 틀린 것은 수익 예측이다.

### 4.4 유의성과 검정력은 다른 질문

B3 는 유의(CI 하한 > 0)하지만 검정력 64.9%다. "이번엔 잡았지만 다시 재면
놓칠 수 있다"는 뜻이다. 페이지 31 이 이 둘을 분리해 보여준다.

---

## 5. 📐 헌법 (절대 규칙 8+2항)

1. **합격선은 챔피언이다.**
2. **심판은 엔진 체결 기록이다.** 지도는 탐색용이다.
3. **자본을 밝힌다.** 총수익금만 보면 자본을 더 쓴 쪽이 항상 이긴다.
4. **유의하지 않으면 유의하지 않다고 쓴다.**
5. **격자는 실행 전 고정, 전셀 보고.**
6. **홀드아웃은 사람만 연다.**
7. **진입과 매도를 동시에 바꾸지 않는다.**
8. **원장은 append-only.**
9. **(신규) 지도로 순위를 매기지 않는다.** 모양(고원/절벽)만 본다.
10. **(신규) 비교하려면 나머지 축이 같아야 한다.** 진입을 재려면 청산을 고정한다.

---

## 6. 🗂 이번 라운드 코드 자산

| 모듈 | 역할 |
|---|---|
| `controller/power_gauge.py` | MDE·검정력·필요표본·부족분(거래일) — 페이지 31 |
| `controller/response_surface.py` | 고원/경사/절벽 판정 — 페이지 32 |
| `labeling/champion_clauses.py` | 챔피언 진입 34절 분해 + DSL 절 제거 |
| `labeling/run_capital_turnover.py` | 자본 회전 교정 A/B (`--out-tag`·`--rebuild-report`) |
| `labeling/run_entry_ablation.py` | 지도 축 절 제거 A/B 32셀 |
| `labeling/run_entry_relax.py` | 완화 후보 엔진 확인 |
| `labeling/run_exit_response_surface.py` | 응답면 + ASCII 히트맵 |
| `dashboard/power_gauge_api.py` · `response_surface_api.py` | 페이지 31·32 |
| `state/gitq.sh` | 인덱스 락 유령 제거 래퍼 |

| 산출 | 경로 |
|---|---|
| 확장 실측 | `state/labels/design_v4/_p5_engine_report_ext.json` |
| 자본 회전 | `..._capital_turnover_ext.json` · `_p5_engine_report_ext_turn.json` |
| 응답면 | `state/labels/design_v5/_exit_response_surface_wide.json` |
| 절 제거 | `state/labels/design_v5/_entry_ablation.json` |
| 진입 완화 | `state/labels/design_v4/_entry_relax_ext.json` |
| 원장 | `_database/strategy_ledger.db` |

---

## 7. 🔁 재개 절차

```bash
cd C:/System_Trading/STOM/STOM_V.wt-dev
git switch research/program-reassessment-20260806

# 서버 (페이지 30·31·32 를 보려면 재기동 필요)
STOM_ALLOW_MINIMAL_SETTING=1 STOM_DASHBOARD_ALLOW_STRATEGY_WRITE=1 \
  python -m ai_strategy_loop --port 8771

# 원장 현황
python -c "from ai_strategy_loop.controller.strategy_ledger import summary; print(summary())"

# 검증
python -m pytest tests/unit/test_power_gauge.py tests/unit/test_response_surface.py \
  tests/unit/test_w6_capital_turnover_render.py tests/unit/test_w7_champion_clauses.py -q -p no:randomly
cd ai_strategy_loop/dashboard/webui-build && node runtime-jsx-check.mjs   # 기대 128 PASS
```

> **주의**: 로컬 변경이 있는 상태에서 `git merge` 하면
> `docs/research/condition_research/reports/` 접근 거부로 stash 가 죽는다.
> **머지 전에 전부 커밋한다.** (한계 원장 L-2026-08-09 §5)
> `index.lock` 유령은 `sh ai_strategy_loop/state/gitq.sh <git 명령>` 으로 우회한다.

### 재개 마스터 프롬프트

```text
STOM 조건식 개발 — W7 라운드를 시작하세요.
저장소: C:/System_Trading/STOM/STOM_V.wt-dev
브랜치: research/program-reassessment-20260806

먼저 읽기:
1. docs/research/quant_scoring_pipeline/HANDOFF_2026-08-09_W6_전단계실행.md
2. docs/research/quant_scoring_pipeline/2026-08-09_w6_자본회전_응답면_진입절.md
3. docs/research/quant_scoring_pipeline/2026-08-09_w6_표본확장_결과.md
4. docs/research/quant_scoring_pipeline/limitation_ledger.md (L-2026-08-09)

확정 전제(다시 재지 말 것):
- 구간 20220323~20250822(833거래일) 챔피언: 361건 건당 +0.67%
  총수익률 241.16% CAGR 72.38 MDD 8.17 필요자금 1,004,095 동시보유 1 국면 4/4
- B3 trailing(5/2): 건당 +1.06%, 짝지은 CI [+0.059,+0.658] 확정. 그러나 동시보유 2
- B3+추세이탈: 짝지은 CI [+0.041,+0.536] 확정. 그러나 동시보유 2
- B3+시간손절: 총수익률 296.02% 동시보유 1 로 자본 통과. 그러나 건당 미확정
- 합성(추세이탈+시간손절): 총수익률 301.39%(최고) 동시보유 1. 그러나 건당 미확정
  → **회전과 우위는 직접 교환 관계다.** 합성해도 우위가 돌아오지 않는다
- 매도축 응답면 36셀 전부 고원(절벽 0) — 이 축은 과최적 걱정 없음
- 그러나 지도 순위는 엔진과 역순이다(4셀 실측, Spearman rho = -0.20)
- 진입 완화 3종 중 지도 3위만 통과(253.34%). 지도 1·2위는 실패
- PASS 는 여전히 0종. 총수익률을 넘은 셋 다 통계 확정이 없다
- 헌법 10항(핸드오프 §5) 전부 준수. 사람 게이트는 실전 투입 1곳뿐

작업 순서(핸드오프 §2):
1) 교환 곡선 — 시간손절 문턱 240·300초를 사전 등록하고 A/B
   (덜 자르면서 동시보유 1 을 지킬 수 있는 지점이 있는가)
2) 진입 완화 재실험 (기준선을 B3 로 — run_entry_relax 에 --sell 추가)
3) 라벨 백필 design_v5 완료 확인 (탐색용, 판정 표본과 무관)

각 단계: 엔진 축 심판 → 원장 등재 → 페이지 30·31·32 확인 → 브랜치 커밋 → --no-ff 머지.
홀드아웃은 열지 않는다.
```

---

## 8. 📚 문서 지도

| 순서 | 문서 | 내용 |
|---:|---|---|
| **0** | **이 핸드오프** | 재개 지점 |
| 1 | `2026-08-09_w6_표본확장_결과.md` | 단계 1·2 — 표본 확장과 재판정 |
| 2 | `2026-08-09_w6_자본회전_응답면_진입절.md` | 단계 3·4·5 |
| 3 | `reports/2026-08-09_w6_full_roadmap_report.html` | 시각 보고서(매니페스트 등록) |
| 4 | `2026-08-07_마스터로드맵_조건식개발.md` §8 | 계획 정정 4건 |
| 5 | `limitation_ledger.md` L-2026-08-09 | 한계 5건 |
| 6 | `2026-08-07_조건식_기술해설.md` | 무엇과 무엇을 비교했나 |
