# 핸드오프 — W8 (2026-08-09 C)

> ⚠️ **홀드아웃이 열렸고 후보는 탈락했다.** 재개 지점은
> `HANDOFF_2026-08-10_홀드아웃후.md` 다. 아래는 개봉 **전** 기록으로 남긴다.
> §1 의 `PASS` 5종은 **설계 구간 판정일 뿐**이다 —
> `trailing(6/2)` 는 홀드아웃에서 −0.1684%p 로 REJECT 됐다.

# (원문) 핸드오프 — W8 첫 승격 (2026-08-09 C)

> 브랜치 `research/program-reassessment-20260806`
> **다음 세션은 이 문서 하나로 재개할 수 있다.**
> 안전 경계: 운영 DB 쓰기·실거래 반영·전체청산 이후 시세·홀드아웃 재학습·후보 자동 채택 금지
> **홀드아웃 119일(20250825~) 미접촉** 🔒

---

## 1. 🧭 지금 어디에 있나

> **원장에 처음으로 `PASS` 가 올라왔다 — 2종.**
> 프로그램의 다음 관문은 연구가 아니라 **홀드아웃 개봉(사람 결정)** 이다.

| 후보 | 총수익금 | 필요자금 | MDD | 짝지은 95% CI |
|---|---:|---:|---:|---|
| **`W4_S_TRAIL_6_2`** ← 홀드아웃 후보 | **3,741,337원** | 200만 | **6.30** | **[+0.063, +0.673]** ✅ |
| `W4_S_TRAIL_5_2p5` | 3,732,329원 | 200만 | 6.32 | [+0.061, +0.670] ✅ |
| `W4_S_TRAIL_5_2` | 3,706,986원 | 200만 | 6.38 | [+0.059, +0.658] ✅ |
| `W4_S_TRAIL_5_1p5` | 3,576,597원 | 200만 | 6.54 | [+0.039, +0.603] ✅ |
| `W6_S_TURN_TREND_BREAK` | 3,479,778원 | 199만 | **5.21** | [+0.041, +0.536] ✅ |
| 챔피언(합격선) | 2,421,433원 | 100만 | 8.17 | — |

**원장: 21종 56행 · PASS 5 · PROMISING 10 · MIXED 0 · REJECT 5 · BASELINE 1**

**선택 편의 우려가 크게 줄었다**: 엔진으로 잰 트레일링 8셀이 **전부 양수**이고
값이 무장·되돌림에 따라 매끄럽게 변한다(3/1 +0.10 → 6/2 +0.37). 한 셀이 운 좋게
튄 것이 아니라 **넓은 영역이 통째로 좋다**.

### 1.1 승격은 "실전 가능"이 아니다

| 남은 관문 | 상태 |
|---|---|
| 홀드아웃 119일 표본 밖 확인 | 🔒 **사람만 연다 · 1회성** |
| 감독형 소액 실전 정합 | 🔒 **사람 승인** |
| 선택 편의 보정 | ⚠️ QSP13 실측 0.6225%p 를 빼면 +0.3582 − 0.6225 < 0 |
| 검정력 | 64.9% — 유의하지만 재현은 별개 |

---

## 2. ⬅ 다음 한 걸음

### 1) 🔒 **홀드아웃 개봉 — 사람 결정** (최우선)

`PASS` 후보가 나왔으므로 이제 열 이유가 생겼다. **한 번뿐인 카드**다.

**올릴 후보는 하나여야 한다.** 현재 근거로는 `trailing(6/2)` —
총수익금 최고(3,741,337원) · MDD 최저(6.30) · CI 하한 최고(+0.063).

```bash
# 사람이 명시적으로 승인한 뒤에만
python -m ai_strategy_loop.labeling.run_engine_measure --out-name design_v5 \
    --gate-tag _wide832 --cells "trailing(arm+6/give2)" \
    --start 20250825 --end 20260227 --tag _holdout --timeout 9000
python -m ai_strategy_loop.labeling.run_engine_ladder --out-name design_v5 \
    --tag _holdout --capital-limit 20000000
python -m ai_strategy_loop.labeling.run_ledger_sync --out-name design_v5 --tag _holdout
```

**열기 전에 합격선을 글로 적어 둔다**(사후 조정 금지):
- 홀드아웃에서 챔피언 대비 건당 차이가 **양수**여야 한다.
- 총수익금이 챔피언 이상이어야 한다.
- 119일 · 약 50거래이므로 **통계적 확정은 기대하지 않는다** — 부호 확인이다.

열고 나면 그 구간은 더 이상 표본 밖이 아니다. **재학습·재탐색 금지.**

### 2) 트레일링 이웃 격자 (자본 자유 목표 = 총수익금)

자본이 구속하지 않으므로 목표가 총수익률 → **총수익금**으로 바뀌었다.
5/2 주변 4셀을 엔진으로 쟀다(§3.2). 지도는 순위를 못 매기므로 엔진만 믿는다.

### 3) 무반등 시간손절 (자본이 필요해질 때만)

`보유시간 > 240 and 수익률 < 0 and 최고수익률 < 2`.
자본을 아끼는 방향이므로 **지금은 우선순위가 낮다**. 자본 한도가 다시
조여지면 그때 꺼낸다(W7 문서 §7 에 근거와 격자가 있다).

---

## 3. 📊 실측 요약

### 3.1 자본 정책 (2026-08-09 사람 결정)

| 정책 | 게이트 | 가정 | 기본 |
|---|---|---|---|
| `ratio` | 총수익률 ≥ 챔피언 | 자본이 희소하다 | ✅ |
| `limit` | 필요자금 ≤ 한도 | 자본이 구속하지 않는다 | 사람이 켠다 |

사용자 확정 한도 **2,000만원**. 실측 필요자금은 100만~200만원이므로 5~20배 여유.
판정 산출과 원장 `notes` 에 정책·한도가 함께 남는다.

### 3.2 매도축 엔진 실측 (구간 20220323~20250822 · 833 거래일)

| 매도식 | 거래 | 건당 | 총수익금 | 필요자금 | 판정 |
|---|---:|---:|---:|---:|---|
| **trailing(5/2)** | 352 | **+1.06%** | **3,706,986** | 199만 | **PASS** |
| trailing(5/2)+추세이탈 | 353 | +0.99% | 3,479,778 | 198만 | **PASS** |
| trailing(5/2)+시간손절300 | 352 | +0.97% | 3,391,511 | 198만 | PROMISING |
| trailing(5/2)+손절 | 355 | +0.94% | 3,335,442 | 199만 | PROMISING |
| trailing(5/2)+시간손절240 | 352 | +0.90% | 3,143,075 | **100만** | PROMISING |
| trailing(5/2)+추세+시간 | 353 | +0.86% | 3,019,893 | **100만** | PROMISING |
| trailing(4/1) | 353 | +0.85% | 2,996,011 | 199만 | PROMISING |
| trailing(5/2)+시간손절180 | 352 | +0.85% | 2,967,584 | **100만** | PROMISING |
| trailing(3/1.5) | 356 | +0.84% | 2,963,266 | 199만 | PROMISING |
| trailing(3/1) | 358 | +0.77% | 2,761,663 | 199만 | PROMISING |
| **챔피언** | 361 | +0.67% | 2,421,433 | 100만 | 합격선 |

### 3.3 확정 지식 (다시 재지 말 것)

| # | 지식 |
|---|---|
| 1 | **우위는 트레일링이 만든다.** 청산 사유별 기여 +94~113%p 로 후보와 무관하게 일정 |
| 2 | **조기 청산은 예외 없이 −93~−107%p 를 지불한다.** 자본을 사는 값이다 |
| 3 | 교환 곡선이 단조롭다. 문턱↑ → 건당↑·CI↑, 동시보유는 240↔300초에서 1→2 |
| 4 | 매도축 응답면(832일) 36/36 고원 — 이 축은 과최적 걱정 없음 |
| 5 | **지도는 진입축을 예측하지 못한다(6전 1승).** 축을 맞춰도 틀린다 |
| 6 | 자본 회전의 레버는 평균 보유가 아니라 **겹침의 꼬리** |
| 7 | 엔진 A/B 는 라벨을 쓰지 않는다 — 표본은 **구간**이 정한다 |

---

## 4. 📐 헌법 (11항)

1. **합격선은 챔피언이다.**
2. **심판은 엔진 체결 기록이다.**
3. **자본을 밝힌다.**
4. **유의하지 않으면 유의하지 않다고 쓴다.**
5. **격자는 실행 전 고정, 전셀 보고.**
6. **홀드아웃은 사람만 연다.**
7. **진입과 매도를 동시에 바꾸지 않는다.**
8. **원장은 append-only.**
9. **지도로 순위를 매기지 않는다.** 진입축은 후보 선정에도 쓰지 않는다.
10. **비교하려면 나머지 축이 같아야 한다.**
11. **(신규) 판정 기준에 가정이 들어가면 사람 결정 항목으로 명시한다.**
    가정을 바꿀 때는 정책을 이름으로 두고, 판정과 함께 기록하고,
    기존 판정을 지우지 않는다. — W8 에서 "자본이 희소하다"는 기본값을
    6라운드 동안 아무도 묻지 않아 `MIXED` 가 쌓였다.

---

## 5. 🖥 화면 (29~34 · 전부 라이브)

| 페이지 | 답하는 질문 |
|---:|---|
| 29 | 라벨 백필·재검증 상태 (결손 120일 = 설계 1 + 홀드아웃 119) |
| 30 | 이 후보가 나은가 (원장 · append-only) |
| 31 | 그 판정을 믿을 만한가 (MDE·검정력·부족 거래일) |
| 32 | 채택해도 표본 밖에서 살아남는가 (고원/절벽) |
| 33 | 두 조건식이 정확히 무엇이 다른가 (절 층 + 줄 층) |
| 34 | 그 숫자가 **왜** 그런가 (청산 사유별·개별 거래) |

---

## 6. 🔁 재개 절차

```bash
cd C:/System_Trading/STOM/STOM_V.wt-dev
git switch research/program-reassessment-20260806

STOM_ALLOW_MINIMAL_SETTING=1 STOM_DASHBOARD_ALLOW_STRATEGY_WRITE=1 \
  python -m ai_strategy_loop --port 8771

python -c "from ai_strategy_loop.controller.strategy_ledger import summary; print(summary())"
cd ai_strategy_loop/dashboard/webui-build && node runtime-jsx-check.mjs   # 기대 130 PASS
```

> **주의 1**: 로컬 변경이 있으면 `git merge` 가 stash 단계에서 죽는다
> (`docs/research/condition_research/reports/` 접근 거부) — 머지 전 전량 커밋.
> **주의 2**: `index.lock` 유령은 `sh ai_strategy_loop/state/gitq.sh <git 명령>`.
> **주의 3**: 진입 계열 후보는 짝지은 검정 불가 — PROMISING 이 상한.
> **주의 4**: 자본 정책을 켜려면 `--capital-limit 20000000` 을 명시한다.
> 안 주면 보수 정책(`ratio`)으로 판정된다.

---

## 7. 📚 문서 지도

| 순서 | 문서 | 내용 |
|---:|---|---|
| **0** | **이 핸드오프** | 재개 지점 |
| 1 | `2026-08-09_w8_자본정책_첫승격.md` | 자본 정책·첫 승격 2종·과신 금지 |
| 2 | `2026-08-09_w7_교환곡선_진입축_페이지33_34.md` | 교환 곡선·지도 6전 1승·페이지 33·34 |
| 3 | `2026-08-09_w6_자본회전_응답면_진입절.md` | 자본 회전·응답면·진입 절 |
| 4 | `2026-08-09_w6_표본확장_결과.md` | 표본 확장 |
| 5 | `limitation_ledger.md` L-2026-08-09 | 한계 |
