# 핸드오프 — 백테스트 후 분석·데이터마이닝 전수감사 완료

> 작업 폴더: `C:\System_Trading\STOM\STOM_V.wt-process-research-restart`
>
> 감사 브랜치: `codex/process-research-analysis-mining-audit`
>
> 기준 정본: `loop/process-research-pipeline` @ `30e05d63`
>
> 감사 문서 commit: `cd2e6d9b`
>
> 재출발 정본 병합: `59fa2663`
>
> 파이프라인 정본 병합: `0db7457c`
>
> push: 하지 않음

---

## 0. 30초 요약

```text
백테스트 엔진 ............. 감사 범위 밖 · 정상 가정
분석 기능 자산 ............ 강함
핵심 분석 테스트 .......... 291 PASS · 1 SKIP
실제 Browser .............. 렌더 PASS · console 0
성공 결과 Browser E2E ..... 불가 · 성공 job/CSV 0
통합 분석 파이프라인 ...... 중간 성숙도
다음 ...................... RES-04 prereg
그 다음 ................... SYS-05 golden/data quality
연구 실행 ................. NONE
Holdout ................... SEALED_NOT_TOUCHED
```

---

## 1. Git/worktree 정답

```text
wt-dev
└── research/v516-d3-mcap-dev @ 97a59ad3
    └── dirty · 보호

wt-process-research-restart
└── loop/process-research-pipeline @ 30e05d63
    └── 현재 정본 작업 위치
```

두 폴더는 같은 common Git repository의 worktree다. loop는 97a59ad3를 조상으로 포함한다. GitKraken에 wt-dev가 worktree로 표시되는 것은 정상이며, loop branch가 wt-dev에 체크아웃된 것은 아니다.

---

## 2. 먼저 읽을 문서

1. `2026-08-30_백테스트후_분석_데이터마이닝_전수감사.md`
2. `2026-08-30_분석데이터마이닝_성숙화_수정마스터플랜.md`
3. `2026-08-30_RES-04_새구조가설_검토.md`
4. 본 핸드오프

---

## 3. 핵심 결론

```text
잘된 것
├── 통계·차트·부검·trade path 기능
├── Truth/Identity/Authority
├── AnalysisCardV3 통계 Gate
└── 풍부한 unit test

부족한 것
├── Data Quality 선행 Gate
├── 성공 결과 Golden E2E
├── AnalysisBundle 단일 정본 완성
├── V2/V3 Card 통합·영속성
├── Episode/Cohort fact layer
├── Program Fold/Control aggregator
├── Finding→Hypothesis→Prereg bridge
└── 하나의 Decision Analysis UX
```

---

## 4. 수정된 실행 순서

```text
RES-04 prereg
→ SYS-05 Golden Result/Data Quality
→ ANA-05 AnalysisBundle v3
→ ANA-06 Episode/Cohort
→ ANA-07 Program Aggregator
→ ANA-08 Hypothesis/Prereg Bridge
→ UX-06 Decision Analysis
→ Stage F
→ 조건부 G0
```

---

## 5. 다음 브랜치

```text
codex/process-research-res-04-prereg
```

Stage F를 실행하지 않고 문서와 JSON manifest만 작성한다.

---

## 6. 안전 불변식

- `STOM_V.wt-dev`에서 작업하지 않는다.
- PnL·MDD·승률·미래가격·exit 결과는 Stage F 금지 필드다.
- 새 G0는 P0/P1 분석 기반 전 실행하지 않는다.
- G2·Holdout·자동채택 금지.
- 운영 `_database/` write 금지.
- tmap feedback stage 금지.
- push는 사용자 별도 지시 전 금지.

---

## 7. 재개 명령

```powershell
Set-Location 'C:\System_Trading\STOM\STOM_V.wt-process-research-restart'
git branch --show-current
git rev-parse HEAD
git status --short
git worktree list
python scripts/build_research_docs_index.py --check
```

---

## 8. 다음 프롬프트

```text
C:\System_Trading\STOM\STOM_V.wt-process-research-restart 에서
HANDOFF_2026-08-30_분석데이터마이닝_전수감사_완료.md 를 먼저 읽어라.

현재 loop/process-research-pipeline 정본에서
codex/process-research-res-04-prereg 브랜치를 생성한다.

RES-04 Stage F outcome-free feasibility의 preregistration Markdown과 JSON manifest만 작성한다.
Episode onset/reset, Family overlap/conflict, 입력 evidence SHA, 허용/금지 필드,
sample Gate, missing 처리, seed, matched-count control, 최대 40-job 예산,
가설 선택 규칙과 정상 STOP을 실행 전에 봉인한다.

Stage F 실행·경제 G0·G2·Holdout·자동채택·운영 DB write는 하지 않는다.
문서 commit 후 restart→loop 순서로 병합한다.
이후 SYS-05 Golden Result Fixture/Data Quality Gate를 착수한다.
```
