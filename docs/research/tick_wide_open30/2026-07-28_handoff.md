# 인계 문서 — 대시보드 완료 이후, 프로세스 개선 연구 재개용 (2026-07-28)

## 0. 이 문서만 읽으면 되는 것

- 지금 상태: **대시보드 개발은 완료 단계**(v5.13.0 + GPT 로그인). 작업 축은 **프로세스 개선 연구**로 넘어와 있다.
- 활성 배치: **없음**(R0~R7 전부 완주·분석·커밋). 유령 프로세스 0.
- 다음 할 일: §5 의 ①②③ — ①③은 즉시 착수 가능, ②는 ① 위에 얹는 것이 효율적.

## 1. 기준 상태

```
브랜치   research/tick-wide-open30-loop-20260727
HEAD     14f27f19 (미커밋 0)
부모     loop/process-research-pipeline  (= 9d601016, 이 브랜치가 12커밋 앞섬)
전파금지 STOM_Version_2U_C 로는 아직 가지 않는다(사용자 지시)
서버     http://127.0.0.1:8770 단일 · release v5.13.0 · build 493abdaa
```

### 서버 기동 (셸 백그라운드 금지 — 작업 회수 시 함께 죽는다)

```powershell
$env:STOM_ALLOW_MINIMAL_SETTING='1'
Start-Process -FilePath 'C:\Python\64\Python31313\python.exe' `
  -ArgumentList '-m','ai_strategy_loop','--host','127.0.0.1','--port','8770' `
  -WorkingDirectory 'C:\System_Trading\STOM\STOM_V.wt-dev' -WindowStyle Hidden `
  -RedirectStandardOutput 'C:\Temp\claude\dash8770_out.log' `
  -RedirectStandardError  'C:\Temp\claude\dash8770_err.log'
```
확인: `Invoke-RestMethod http://127.0.0.1:8770/health` → release v5.13.0 · build 493abdaa

## 2. 대시보드 — 무엇이 끝났나

| 영역 | 상태 |
|---|---|
| v5.13.0/.1 육안 검토 47항목 | 전부 반영 + 누락 재감사 4건 추가 수정(9d601016) |
| GPT 로그인(설정) | 카드(로그인 시작·진행 폴링·만료 카운트다운·자동갱신/프록시 상태) + 톱바 상시 🔑칩 + 백엔드 `/gpt_auth/login_start`·`/login_state` (927a5053) |
| 성과 TOP10 | 카드 클릭/📊 → 백테스트 탭 결과 상세 자동 오픈 |
| 관찰성 | 연구 배치 run(clw30_*)이 진화 라이브러리·결과 분석·리포트에 그대로 노출(세대·CSV·라벨) |

**사장님이 하실 일 하나**: 설정 탭 → [ChatGPT 로그인 시작] 클릭(계정 인증이라 대행 불가).
로그인되면 공식 LLM 루프로 전환 가능.

## 3. 프로세스 결함 원장 (연구 중 실측·기전 확정)

| # | 결함 | 기전 | 상태 |
|---|---|---|---|
| A | 구문 오류 조건식이 600초 정지 | elelif 재현 → 수정 후 46s (인과 대조) | ✅ preflight 구문 검사 |
| B | 미정의 변수(`강제청산`) 무한 정지 | CSS_V7 매도식 9종, SSOT 284 밖·로컬 할당 없음 | ✅ preflight 미정의 검사 |
| C | 거버넌스 무음 클램프(93000→92800) | fast 프리셋이 조용히 강제 | ✅ 배치 로그에 유효 창+경고 명시 |
| D | 청산사유 분기 미분해 | CSV 매도조건이 조건식 이름 단위 | ⏸ 엔진 영역(게이트 필요) |
| E | 배치 강제종료 → 엔진 고아 105개 | 다음 prepare 40분+ 지연 | ✅ kill-on-close 잡 자기부착 + 회귀 테스트 |
| F | **GUI식 다인자 self.Buy/Sell → 체결 틱마다 TypeError → 600초 정지** | 엔진은 `Buy(buy_long=False)`(≤1인자) | ✅ preflight gui_signature 검사 (600s→42s 실증) |

**총 뿌리**: CSS_V7·C_T 등 "GUI에서 온" 전략의 백테 정지는 성능 문제가 아니라
**GUI 문법 ↔ 백테 문법 괴리**(미정의 변수 + 다인자 호출) 하나였다.

관련 파일: `ai_strategy_loop/controller/strategy_preflight.py`(검증기),
`tests/unit/test_strategy_preflight.py`(9종), `tests/unit/test_windows_process_job_killonclose.py`(2종),
`ai_strategy_loop/scripts/claude_candidate_batch_eval.py`(게이트·유효창 로그·잡 부착).

## 4. 연구 결과 요약 (R0~R7)

- **R0~R4**: 광범위 진입은 비용 전에도 −0.37%p(전 세그먼트 음수). 출구 튜닝 무효.
  절 스택으로 +0.43%p 회복해도 0 미달. 원장 5,391세대 전수로 **진짜 승자 = ovn_anchor 다밴드**
  (3년 +0.61%/거래·MDD 9.6%) 발견. 그러나 anchor 조차 **2025-04 이후 원엣지 +0.17%p < 비용 0.21%p**.
- **R5~R7**(사장님 지정 다밴드 C_T_900_920_U2 정정본):
  밴드 온도 지도(09:05-10 최선 −0.19 / 09:15-20 −0.23 / 나머지 태움) →
  3축 복합(밴드선별×소형주×트레일 재적합)이 **설계 구간 게이트 통과 +78.8만·MDD 21%** →
  **완전 OOS(2024 −0.31%, 2023 −0.69%) 전패로 과최적 기각**. 월별로도 5/11 양(+), 최근 6개월 중 5개월 음수.
- **시스템 교훈**: 분해→복합 루프는 작동한다(3라운드에 음수→게이트 통과).
  실패한 것은 **검증 게이트의 위치** — 조합을 찾은 구간에서 채점하면 게이트 통과도 못 믿는다.

보고서: `2026-07-28_final_report.md`(+ 자가완결 HTML), 라운드 노트 `round_notes/R0~R7`.

## 5. 이어서 할 일 (우선순위)

### ① OOS 게이트를 채점 파이프라인에 내장 — 즉시 착수 가능(엔진 무수정)
- 지금은 표본외 검증을 **사람이 수동으로** 돌린다(R7이 그랬다). 이걸 상시 규칙으로.
- 구현안: `claude_candidate_batch_eval` 에 `--oos-config` 추가 → 각 쌍을 설계 구간 + 판정 구간
  양쪽에서 평가하고, **두 구간 동시 통과만 gate_passed** 로 기록. 대시보드는 기존 화면 그대로
  두 run 을 비교로 볼 수 있음(동일 쌍 × 다른 기간 묶음 보기는 UX 후보).
- 예상 효과: R6 같은 과최적 후보가 **자동으로 기각**된다. 연구 속도 저하 없이 신뢰도 확보.

### ② b2 코어(09:05~10) 워크포워드 재탐색 — ① 위에서 하는 것이 효율적
- 근거: 표본외에서도 −0.09%로 **유일하게 덜 죽는 코어**(다른 조합은 −0.3~−0.7).
- 방식: 처음부터 설계/판정 구간을 나눠 진행. 축 후보 = 소형주 임계 재조정, 트레일 파라미터
  격자, 진입 미시구조(체결강도·누적초당수요, R1 한계엣지 상위) 복합.

### ③ GPT 로그인 후 공식 LLM 루프 재가동 — 사장님 클릭 → 그 다음 제가
- 로그인되면 provider=gpt_auth 로 루프 run 1회 스모크 → 생성 품질·게이트 동작 확인.
- 그때까지는 지금처럼 제가 후보를 직접 설계(무중단 진행 가능).

### 보류/게이트 필요
- 결함 D(청산사유 분기 태그) — 엔진 수정 필요.
- 날씨(레짐) 판단 — 조건식 규칙 문서에 **시장 수준 변수가 없어 직접 구현 불가**.
  대안: 종목 프록시(시가갭·전일비) / 절차 온·오프 / 엔진에 시장 변수 추가(사장님 게이트).
- 브랜치 원격 백업(1,200+ 커밋 로컬 전용) — 여전히 미결.

## 6. 재현·조작 명령

```bash
# 배치 1회 (설계 구간)
C:\Python\64\Python31313\python.exe -m ai_strategy_loop.scripts.claude_candidate_batch_eval \
  --pairs-json docs/research/tick_wide_open30/pairs_r6.json \
  --config-json docs/research/tick_wide_open30/config_r1.json \
  --run-id <새_run_id>
# 환경: STOM_ALLOW_MINIMAL_SETTING=1, PYTHONUTF8=1 · 반드시 Start-Process 로 분리 기동
# config: config_r1(설계 20250407-20260227) / config_r3a(2024) / config_r3b(2023)
```

- 후보 등록: `loop_strategies.db` INSERT-only, 이름 규약 `CLW30_*`. 등록 전 반드시
  `strategy_preflight.validate_strategy_code()` 통과 확인(원본 전략은 수정 금지, 사본 사용).
- 결과 확인: 대시보드 8770 → 백테스트 탭 → 진화 세대 결과 라이브러리에서 `clw30_*` run 선택.

## 7. 함정 (시간 아끼는 목록)

1. `index.lock` 잔여 — 커밋 실패 시 `.git/worktrees/STOM_V.wt-dev/index.lock` 삭제.
2. 콘솔이 cp949 — 파이썬 실행 시 `PYTHONUTF8=1` 없으면 한글 깨짐/인코딩 오류.
3. 대시보드 세션 TTL 짧음 — curl 로 POST 할 때 `curl -c cj.txt http://127.0.0.1:8770/` 로 먼저 부트스트랩.
4. 배치를 `cmd /c A && B` 형태로 Start-Process 하면 인자가 깨진다 — **.bat 파일**로 감싸서 실행.
5. 모니터 타임아웃 알림은 무해(이미 완주한 감시). 실제 상태는 로그 tail 로 확인.
6. 브라우저 확인 시 강력 새로고침(Ctrl+F5) 후 좌상단 build 해시가 최신인지 먼저 볼 것.
