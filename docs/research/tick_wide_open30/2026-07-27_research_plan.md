# Tick 초기 30분 광범위 조건식 개선 연구 — 장기 자율 연구 계획

- 시작일: 2026-07-27 · 브랜치: `research/tick-wide-open30-loop-20260727` (부모 `loop/process-research-pipeline`)
- 목표: **왜 수익을 못 내는가 / 왜 수익 내는 조건식을 못 만드는가**를 tick 초기 30분에서 집중 규명.
  성과가 나면 성과 보고, 안 나면 "개선 불가 원인 분석 보고서"가 최종 산출물.
- 부수 목표: 각 라운드를 대시보드 v5.13.0 로 관찰하며 프로세스·UX/UI 시각화 전수검사.

## 연구 아키텍처 (LLM 프로바이더 인증 부재 대응)

gpt_auth 토큰 만료·codex-proxy 키 접근 불가 → **Claude 분석주도 루프**로 진행:

```
Claude(생성·분석) → claude_candidate_batch_eval(warm 엔진 1회 로딩 · 공식 채점 · loop_runs.db 기록)
      ↑                                   ↓
   CSV/부검 분석  ←  대시보드 v5.13.0 (결과·부검·리포트 관찰 = 프로세스 전수검사)
```

- 엔진: warm 세션 1회 prepare 후 유지(사용자 요구와 일치) · 32엔진 · 전체 유니버스
- 채점: 공식 `_score_outcome` — 하드게이트(MDD·일평균거래) 동일
- 기록: loop_runs.db → 대시보드 진화 라이브러리·결과 분석·HTML 리포트 그대로 사용

## 고정 조건 (사용자 지시)

| 항목 | 값 |
|---|---|
| 타임프레임 | tick (min 은 tick 성과 후) |
| 장중 윈도우 | **090000–093000** (tick 데이터 실측 최대 = 09:30:00, 30분 전체) |
| 기간 | 20250407–20260227 (min DB 와 동일 구간 — 이후 min 확장 대비) |
| 배팅 | 종목당 500만 (bt_betting=5, GUI 정합) |
| 관심종목 == 1 | 유지 (제거 금지) |

## 시드 (검증된 광범위 tick 쌍)

`ResearchTest_Tick_B/S_090000_092800_Wide_20260419` — 18일 구간에서 4,450거래 실증.
- 매수: 관심종목==1 · 0<현재가≤50,000 · 90000≤시분초≤92800 · 0<등락율≤25 · 당일거래대금>100 · 상한가5호가이내 제외
- 매도: 수익률 ≥+3% / ≤−3% / 보유 300초 / 92800 강제청산

## 라운드 설계 (분석주도 — 매 라운드 재백테 게이트)

- **R0 · 출구 그리드 탐침(6쌍)**: 시드 복제(기준선) + 윈도우 확장(–92900/청산 92950) +
  출구 비대칭 4종(+5/−2, +2/−4, 시간청산 제거, ±1.5 타이트). 목적: ① 파이프라인 검증
  ② 비용 대비 엣지의 1차 지도(출구가 지배한다는 기존 실측[rr8] 재확인)
- **R1+**: R0 CSV 분해(왕복비용·분단위 온도·보유시간 구조·청산사유) → 다음 후보 설계.
  개선이 2라운드 연속 없으면 축을 바꾼다(진입 밴드·시가갭 방향·거래대금 구간).
- **판정 규칙**: 모든 결론은 공식 채점 행(loop_runs.db)과 CSV 실측으로만. 미발행 값 추정 금지.

## 실패 가설 후보 (검증 대상)

1. 왕복비용(0.61~1.21%) > 초반 30분 평균 엣지 — 비용 분해로 정량화
2. ±3% 대칭 출구가 초반 변동성 분포와 미스매치 — 출구 그리드로 검증
3. 5분 시간청산이 수익 꼬리를 절단 — 시간청산 제거 변형으로 검증
4. 진입이 너무 넓어 (관심종목 필터 후에도) 음(−) 기대 구간 포함 — 세그먼트 부검으로 확인
5. 30분 창 자체의 구조적 한계(갭 방향성 미구분) — 시가갭 조건 분기로 검증

## 산출물

- 라운드별: loop_runs.db run + 대시보드 관찰 기록 + 이 디렉토리의 라운드 노트
- 최종: 성과 보고 또는 **"수익 조건식 개선 불가 원인 분석"** 보고서(HTML 자가완결 + md, 커밋)
