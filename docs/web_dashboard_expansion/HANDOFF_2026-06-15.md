# STOM 대시보드 개선 — 작업 핸드오프 (2026-06-15)

> 단일 인계 문서. 다음 세션/담당자가 이 문서만 읽고 이어받을 수 있도록 상태·체크리스트·재개법·운영레퍼런스를 담는다.
> 워크트리: 개발 `C:\System_Trading\STOM\STOM_V.wt-webbt` · 라이브(8770) `C:\System_Trading\STOM\STOM_V.wt-dev` · 부모 브랜치 `lazycodex/tick-sparse-positive-generation-improvement-20260604`.

---

## 1. 상태 스냅샷
- **두 개 프로그램 완료(총 14 PR 머지, #61~#74)**, 전부 8770 라이브 반영.
- 라이브 번들(8770 기준): `app.js v=5d10d7ab` · `stom-ui.js v=f41f5701` · `styles.css ?v=20260614j`.
- 게이트 핀 베이스라인: 전체 `pytest tests/unit/ -q` = **7 failed(기존 백엔드/CLI/PyQt — 프론트와 무관, 손대지 말 것) + ~3229 passed + 2 skipped**. 매 PR 이 이 집합과 정확히 동일해야 한다(`.omc/plans/webbt-baseline-failures.txt`는 wt-webbt 로컬 핀, gitignore).

## 2. 완료 내역

### 프로그램 A — Phase14 빌드하네스/감사 정리 (PR #61~#67)
| PR | 내용 |
|----|------|
| #61/#62 | P0.5 esbuild-부재 skip 가드 · P0.5b content-hash CRLF 견고화 |
| #63 | P1 데드 RunComparePanel 제거 · CodeBlock→CvCodeBlock 충돌 수정 · test_no_duplicate_globals 신설 |
| #64 | P4-functional 백테 CSV · WFO/스윕 드릴다운 · 엔진 DEMO 라벨 · LWC 비대칭 캡션 |
| #65 | P2 LiveBacktestChart 기하 공유(_liveChartGeom) · 결정 동선 크로스링크 |
| #66 | P3 공유 tick/format 헬퍼 → format.ts(_priceTick/_hmsTimeLabel) |
| #67 | Design Pass 진화 섹션 헤더 시각 승격(.stom-section-label) |

### 프로그램 B — 스타일·구조 개선 (PR #68~#74, 계획: `.omc/plans/dashboard-style-structure-program-20260614.md`)
| PR | 단계 | 내용 |
|----|------|------|
| #68 | P1 | Wiki·AIContext → 연구실 탭 홈(가산) |
| #69 | P0 | 디자인 토큰 스캐폴딩(--space/--radius/--fs, 픽셀중립) |
| #70 | P2 | 진화 사이드바 Wiki/AIContext 중복 제거 + 발견 버튼(C1 4테스트 리타겟) |
| #71 | P3 | 진화 7섹션 접이식 <details>(기본 펼침, localStorage/aria) |
| #72 | P4 | PIPELINE 통합 → format.ts window.STOM_PIPELINE |
| #73 | P6 | 디자인시스템 랜딩 — 본문 ≥14px·토큰 적용·반응형·WCAG AA(양 테마) |
| #74 | P7 | 검증결산 공유 셸(VdtPromoteChecklist/Alerts/SummaryLines) + HoF 이연 |

## 3. 미결 체크리스트
- [ ] **(사용자) P6·P7 시각 재베이스라인 사인오프** — 픽셀 변경 단계. 8770에서 다크/라이트 토글하며 타이포(본문 14px)·대비·결정이력 탭(공유 체크리스트/경보/요약 + 고유 OOS-CI 표 + 4 하위탭)·반응형(1280/1600/2200) 확인. MVQA 전/후 PNG 는 세션 tmp 에 캡처됨(영구 보관 원하면 `docs/web_dashboard_expansion/screenshots/<phase>/`로 이동).
- [ ] **Track Z — esbuild transform→bundle 이관**(충돌 클래스 근본 해결). 트리거-조건부 이연: `docs/web_dashboard_expansion/COLLISION_TAX.md` 누적 **2/5**. 5 도달 또는 다음 리뷰-충돌 시 별도 RALPLAN 으로 착수. 현재는 `test_no_duplicate_globals`(강화됨)+format.ts 호스트로 억제.
- [ ] (선택) 연구위키/AI컨텍스트/autopsy 본문을 진짜 prose ≥14px 로 올릴지 결정(현재 참조/모노 pre 패널이라 12/10.5px 유지 — P6 예외목록 `PROG_P6_DENSE_DATA_EXCEPTIONS.md`).
- [ ] (선택) HoF(HallOfFamePanel↔_RpHallOfFame) 시각 통합 — field-diff 결과 발산형이라 Track Z(모듈 스코프) 이후가 안전.

## 4. 재개 방법 (다음 세션)
1. 이 문서 + 계획서(`.omc/plans/dashboard-style-structure-program-20260614.md`)를 읽는다.
2. 새 작업은 wt-webbt 에서 부모 브랜치 기준 새 브랜치 → **PR-머지 사이클**(아래 5)로.
3. 명령어 예:
   - 후속 단계 실행: `/oh-my-claudecode:ralph <구체 작업: 파일/함수/수용기준 명시>` (모호하면 게이트가 ralplan 으로 보냄)
   - 새 설계 합의: `/oh-my-claudecode:ralplan <작업 설명>`
   - 단발 수정: executor 에이전트 직접 위임
4. 8770 은 정적 서빙 — 머지 후 `git -C wt-dev pull` + 브라우저 새로고침이면 반영(서버 재시작 불필요).

## 5. PR 사이클(매 단계 필수)
edit `.jsx`/`styles.css`/`format.ts`(wt-webbt) → `cd ai_strategy_loop/dashboard/webui-build && npm run build` → `python -m pytest tests/unit/ -q`(핀 베이스라인 동일) → `python scripts/verify_nonrelease_sync.py`(exit0) → Playwright :8771 per-tab 0 pageerror(+픽셀 단계는 MVQA 전/후 PNG) → opus code-review(별도 패스) → PR(부모 브랜치) → 머지 → `git -C wt-dev pull` → 8770 확인.
- **빌드 산출물 커밋(M2)**: `bundle/app.js`+`manifest.json`(+`stom-ui.js` if format.ts 변경) 같은 PR에. `?v=`/manifest 수동 병합 금지 — 머지 후 항상 `npm run build`.

## 6. 운영 레퍼런스(불변/함정)
- **빌드 모델**: esbuild **transform-not-bundle** — 26개 .jsx 가 한 최상위 스코프 공유 → **같은 최상위 이름 2회 선언 시 "already declared" SyntaxError(전 앱 크래시)**. 자기-별칭(`const X=window.X`)도 정의측 `function X`와 충돌(P7 실사례). 가드: `tests/unit/dashboard/test_no_duplicate_globals.py`(자기-별칭 포함 카운트). 교차파일 공유 컴포넌트는 더 이른 ORDER 파일에 1회 정의 + `<window.X/>` 멤버표현식 소비.
- **공유 데이터/유틸 호스트**: `webui-build/src/format.ts` → `stom-ui.js` → `window.*`(pre-app 전역, no-TDZ, .jsx collision 면제). glossary.jsx 금지(후순위 ORDER → TDZ).
- **FROZEN 전역(개명 금지)**: `window.LabPage`/`ProPage`/`VerdictPanel`/`App` + stom-ui 노출(fmt*/STATUS_KR/isDemoSource/livePanelPending/_axisTicks/_priceTick/_hmsTimeLabel/STOM_PIPELINE). lab/pro/verdict.html 이 이름으로 마운트.
- **콘텐츠-해시 자동화**: build 가 app.js/stom-ui.js `?v=` 를 5 HTML+manifest 에 주입. **styles.css `?v=` 는 수동 핀**(현 `20260614j`) — styles.css 변경 시 5 HTML 동시 bump + `test_dashboard_validation_views.py::test_index_html_cache_bumped` 핀 갱신(`test_design_pass` 캐시테스트는 핀-불특정).
- **사acrosanct 테스트**: `test_p14_build_harness.py`(26 ORDER 마커 + content-hash), `test_no_duplicate_globals.py`, `test_dashboard_validation_views::test_index_html_cache_bumped`, `test_phase9_spa_tabs`(STOM_TABS==6).
- **픽셀 인프라 없음**: 자동 스냅샷-diff 부재 → 픽셀 게이트는 수동 MVQA(6탭+lab/pro/verdict × 다크/라이트 × 1280/1600/2200, 사람 사인오프).
- **건드리지 말 것**: wt-dev 의 동시 연구 작업(p5_template_hypothesis·tmap multiband·`.omo/evidence` 등), `backtest/graph/`(보호 결과데이터). wt-dev 동기화 시 0-byte 스테일 `index.lock`(GitKraken 폴러)은 제거 후 머지.
- **field-diff 게이트**: 통합/병합 전 "버리는 사본이 고유 내용 없음"을 field-diff 로 증명. (지난 사이클들에서 감사 전제가 자주 틀림 — 실측 우선.)

## 7. 핵심 문서 인덱스
- 계획서: `.omc/plans/dashboard-style-structure-program-20260614.md`(로컬, gitignore)
- 단계별: `docs/web_dashboard_expansion/PROG_P0~P7_*.md`, `PHASE14_*.md`
- P6 예외목록/대비: `PROG_P6_DENSE_DATA_EXCEPTIONS.md`, `PROG_P6_CONTRAST_MATRIX.md`
- P7 field-diff: `PROG_P7_FIELD_DIFF.md` · 충돌세: `COLLISION_TAX.md`
