# v5.4 시각·인사이트 강화 마스터플랜 (2026-07-20)

## 0. 목적 (사장님 지시 원문 요지)
조건식 AI 연구 터미널을 더 고도화·시각화하여 **최종 목적(수익 조건식 발굴)을 더 빠르고 쉽게**, 사람과 AI 모두에게 **직접 인사이트**를 주는 방향으로 개발한다.

## 1. 브랜치 체계 (사장님 지시)
```
loop/process-research-pipeline (96b036ac)
  └ feature/dashboard-v5.4-enhance          ← 통합 브랜치
      ├ v5.4-live        (라이브 탭 L1~L5)
      ├ v5.4-history     (히스토리 성능 H1)
      ├ v5.4-backtest    (백테스트 시각화 B1)
      └ v5.4-reports     (리포트 R1~R2)
```
각 페이지 브랜치에서 개발 → 통합 브랜치로 `--no-ff` merge → 최종 pipeline merge + `v5.4.x` 태그.
개발 서버 8771(wt-v5), 최종 반영 후 8770(wt-dev) 재기동.

## 2. 요구 원장 (누락 금지 — 완료 시 [x])

### 라이브 탭 (v5.4-live)
- [x] **L1** 4개 품질지표 차트(fitness~)의 제목 라인 어긋남 수정: 영어 제목 삭제, 차트 밴드를 "실시간 연구 상황판" 내부로 이동. 상황판 자체 고도화.
- [x] **L2** 스테이지 탭(1~4) 아래 내용 2열 → **4열×x행** 재배치(위 4카드와 정렬). **레이아웃 선택 버튼**(2열/4열 등 사람이 선택) 제공.
- [x] **L3** "조건식 발굴 거버넌스"를 더 분리·가시화. **매수/매도 조건식을 크게**, 이해하기 쉽게 스테이지 내용 개선.
- [x] **L4** 백테스트 스테이지: 4열 배치, "페이즈 상세" 빈 높이 문제 → 백테스트 엔진과 동일 높이. 백테스트 엔진 탭 시각화·구분 UX 개선. 백테스팅 그래프 4열×x행 재배치. 분석 결과를 BT 탭 수준으로 상세화 + 시각 요소(히트맵·산점도·회귀·다차원 회귀 등 퀀트 시각화) 연구 반영.
- [x] **L5** 채점·부검: "탐색 히트맵" 중복 기능 병합·개선. "엣지·상관·안정성 검증 분석"의 여러 차트를 BT 결과탭처럼 **개별 그래프 한눈에** 4열 재배치 + 배치 선택. **반복·성과 스테이지도 동일 맥락** 적용.

### 히스토리 탭 (v5.4-history)
- [x] **H1** 히스토리 탭 진입 시 크롬 전체가 느려짐 — 데이터 로딩/렌더 성능 근본 원인 진단·수정 (증상 재현 → 프로파일 → 근본 수정).

### 백테스트 탭 (v5.4-backtest)
- [x] **B1** 그래프 4열×x행 재배치. 분석 결과 상세화 + AI 분석에 도움·사람에게 인사이트를 주는 퀀트 시각화(히트맵, 산점도, 회귀, 다차원 회귀 등) 최대한 연구·추가.

### 리포트 탭 (v5.4-reports)
- [x] **R1** 리포트 탭 배치 깨짐 수정 (재현 → 근본 수정).
- [x] **R2** 연구 루프 사이클/백테스트 결과의 **표준 리포트 포맷** 확립(구조해석 보고서 스타일: 화려한 시각화 + 체계적 분석 HTML). 결과 리포트를 기본 연구문서와 **구분**하여 체계적으로 보여주는 결과 분석 시스템.

## 3. 안전 불변 (기존 계약 유지)
performance_proved=false · human export gate · 보호 DB 무쓰기 · 리포트 writer는 오프라인 스크립트만(GET/WS 무쓰기) · P4 SELECT-only.

## 4. 검증 게이트 (릴리스마다)
`node build-app.mjs` 번들 커밋 · css 핀 증가 · V4_DASH_VERSION 갱신 · 3해상도(3440/2560/1920) 실브라우저 · 포커스드 pytest · 화면 스크린샷 → 최종 사장님 검수.
