################################################################################
BUY: Min_B_Study_251229
################################################################################
# 분봉 기반 변수 정의
전일종가 = 현재가 / (1 + (등락율 / 100))  # 단위: 원
시가등락율 = ((분봉시가 - 전일종가) / 전일종가) * 100  # 단위: 퍼센트
시가대비등락율 = ((현재가 - 분봉시가) / 분봉시가) * 100  # 단위: 퍼센트
분당순매수금액 = (분당매수수량 - 분당매도수량) * 현재가 / 1_000_000  # 단위: 백만원
누적거래대금비율 = 당일거래대금 / (시가총액 * 회전율 / 100) * 100 if (시가총액 * 회전율 / 100) != 0 else 0


매수 = True


# 공통 선결 조건
if not (관심종목 == 1):
    매수 = False
elif not (0 < 현재가 <= 50000):
    매수 = False
elif not (1.0 < 등락율 <= 20.0):
    매수 = False
elif not (고저평균대비등락율 > 0):
    매수 = False
elif not (현재가 < VI아래5호가):
    매수 = False
elif 라운드피겨위5호가이내:
    매수 = False
elif not (분당거래대금 > 분당거래대금N(1) * 0.0):
    매수 = False
#elif not (RSI < 70):
#    매수 = False
elif 시분초 < 120000:
    if 시가총액 < 5000:
        if not (-1.0 <= 시가등락율 < 20.0):
            매수 = False
        elif not (0 <= 시가대비등락율 < 20.0):
            매수 = False
        elif not (5 < 분당순매수금액 < 10 * 100):
            매수 = False
        elif not (현재가 > (분봉고가 - (분봉고가 - 분봉저가) * 80 / 100)):
            매수 = False
#        elif not (누적거래대금비율 > 1):
#            매수 = False
        elif not (전일비 > 0):
            매수 = False
        elif not (전일동시간비 > 0):
            매수 = False
        elif not (회전율 > 0):
            매수 = False
        elif not (당일거래대금 > 1 * 100):
            매수 = False
#        elif not (당일거래대금N(1) * 1.0  < 당일거래대금):
#            매수 = False
#        elif not (당일거래대금각도(30) > 1 and 당일거래대금각도(30) < 30):
#            매수 = False
#        elif not (분당거래대금 / 분당거래대금평균(30) > 1.0):
#            매수 = False
        # elif not (분당매수수량 > 매도총잔량 * 10 / 100):
        #     매수 = False
#        elif not (매도총잔량 > 매수총잔량 * 0.1):
#            매수 = False
#         elif not (체결강도 >= 50 and 체결강도 <= 300):
#            매수 = False
#        elif not (이동평균(120) < 현재가):
#            매수 = False
#        elif not (이동평균(60) < 현재가 and 이동평균(120) < 이동평균(60)):
#            매수 = False
#        elif not (이동평균(60,30) <= 이동평균(120, 30) and 이동평균(120) < 이동평균(60)):
#            매수 = False
#        elif not (이동평균(300) < 이동평균(120)):
#            매수 = False

    else:
        매수 = False

else:
    매수 = False

#매수 = True


if 매수:
    self.Buy(종목코드, 종목명, 매수수량, 현재가, 매도호가1, 매수호가1, 데이터길이)

################################################################################
BUY: Min_B_Study_251227_Filtered2
################################################################################
# 최종 매수 조건식_filtered (일반 필터 반영)
# 생성 시각: 2026-01-09 22:02:02
# save_file_name: stock_bt_Min_B_Study_251227_Filtered_20260109220135
# 매수 조건식: Min_B_Study_251227_Filtered
# 매도 조건식: Min_S_Study_251227_Filtered
# 필터 수: 5
# 예상 개선금액: 59,315,963원
# runtime mapping: included (buy-time)

# === 적용된 필터 목록 ===
# 1. 매수매도총잔량 74,595 이상 제외: 개선 28,448,159원, 제외 28.7%
# 2. 매수고저평균대비등락율 1.15 미만 제외: 개선 23,832,235원, 제외 28.7%
# 3. 매수변동폭비율 18.36 이상 제외: 개선 13,369,394원, 제외 5.2%
# 4. 매수회전율 0.37~0.56 구간 제외: 개선 8,951,976원, 제외 10.2%
# 5. 매도잔량_매수잔량_비율 0.55 미만 제외: 개선 7,717,655원, 제외 5.0%

# === 필터 조합 단계 ===
# 1. 매수매도총잔량 74,595 이상 제외: +28,448,159원 → 누적 15,304,883원
# 2. 매수고저평균대비등락율 1.15 미만 제외: +15,364,111원 → 누적 30,668,994원
# 3. 매수변동폭비율 18.36 이상 제외: +6,595,432원 → 누적 37,264,426원
# 4. 매수회전율 0.37~0.56 구간 제외: +4,501,460원 → 누적 41,765,886원
# 5. 매도잔량_매수잔량_비율 0.55 미만 제외: +4,406,801원 → 누적 46,172,687원

# === 매수 조건식 (일반 필터 반영) ===
# 분봉 기반 변수 정의
전일종가 = 현재가 / (1 + (등락율 / 100))  # 단위: 원
시가등락율 = ((분봉시가 - 전일종가) / 전일종가) * 100  # 단위: 퍼센트
시가대비등락율 = ((현재가 - 분봉시가) / 분봉시가) * 100  # 단위: 퍼센트
분당순매수금액 = (분당매수수량 - 분당매도수량) * 현재가 / 1_000_000  # 단위: 백만원
누적거래대금비율 = 당일거래대금 / (시가총액 * 회전율 / 100) * 100 if (시가총액 * 회전율 / 100) != 0 else 0


매수 = True

# === 세그먼트 필터 (자동 생성) ===
# [2026-01-08] 세그먼트 필터 우선 적용
# 세그먼트 필터를 먼저 평가하여, 해당 세그먼트에서 제외할 거래를 먼저 필터링
# === 세그먼트 필터 당일 재매수 차단 (2026-01-09) ===
# 한 번 차단된 종목은 해당 날짜 동안 재매수 시도 자체를 차단
# 목적: '대체 매수' 현상 방지 (예측 vs 실제 괴리 해소)

# 날짜 추출 (index에서 YYYYMMDD 부분)
try:
    _오늘날짜 = int(str(self.index)[:8]) if len(str(self.index)) >= 8 else 0
except (AttributeError, ValueError):
    _오늘날짜 = 0

# 날짜 변경 감지 시 차단 목록 초기화
if _오늘날짜 != self._세그먼트차단_마지막날짜:
    self.세그먼트차단종목 = {}
    self._세그먼트차단_마지막날짜 = _오늘날짜

# 이미 오늘 차단된 종목인지 확인 (당일 재매수 차단)
_이미차단됨 = (종목코드, _오늘날짜) in self.세그먼트차단종목
if _이미차단됨:
    매수 = False  # 당일 재매수 차단: 이미 차단된 종목

# 아래 세그먼트 필터는 _이미차단됨 여부와 무관하게 평가됨
# (차단된 종목도 세그먼트 필터를 거치지만, 이미 매수=False이므로 결과에 영향 없음)

# === Segment filter runtime mapping (buy-time) ===
# used vars (segment filter): 시분초, 시가총액, 매수매도총잔량, 매수스프레드, 매수고저평균대비등락율, 매수
# 세그먼트, 전체, 제외, 정의되지, 않은, 구간
# 당일거래대금_5틱분봉평균_비율, 매수고가, 매수매수총잔량, 매수변동폭, 매수호가잔량비, 매수시가등락율
# 매수회전율, 현재가_고저범위_위치, 매수변동폭비율, 거래품질점수, 매수초당거래대금, 매수전일비
# 매수전일동시간비, 매수초당매수수량, 매수저가, 당일거래대금_전틱분봉_비율, 매수가, 매수체결강도
# 시간대
# Derived metrics for segment filter evaluation
try:
    초당매수수량 = 초당매수수량
except NameError:
    초당매수수량 = 0
매수초당매수수량 = 초당매수수량
try:
    매수초당거래대금 = 초당거래대금
except NameError:
    매수초당거래대금 = 0
매수스프레드 = ((매도호가1 - 매수호가1) / 매수호가1) * 100 if 매수호가1 > 0 else 0
매수호가잔량비 = (매수총잔량 / (매도총잔량 + 1e-6)) * 100
매수변동폭 = 고가 - 저가
매수변동폭비율 = ((고가 - 저가) / 저가) * 100 if 저가 > 0 else 0
현재가_고저범위_위치 = (현재가 - 저가) / (고가 - 저가 + 1e-6) * 100
try:
    _prev_trade = 당일거래대금N(1)
    당일거래대금_전틱분봉_비율 = 당일거래대금 / (_prev_trade + 1e-6) if _prev_trade > 0 else 0.0
except (NameError, TypeError):
    당일거래대금_전틱분봉_비율 = 0.0
try:
    _trade_avg = (당일거래대금 + 당일거래대금N(1) + 당일거래대금N(2) + 당일거래대금N(3) + 당일거래대금N(4)) / 5
    당일거래대금_5틱분봉평균_비율 = 당일거래대금 / (_trade_avg + 1e-6) if _trade_avg > 0 else 0.0
except (NameError, TypeError):
    당일거래대금_5틱분봉평균_비율 = 0.0

# Snapshot mappings for buy-time columns
매수매수총잔량 = 매수총잔량
매수매도총잔량 = 매도총잔량
매수전일비 = 전일비
매수전일동시간비 = 전일동시간비
매수체결강도 = 체결강도
매수등락율 = 등락율
매수시가등락율 = 시가등락율
매수회전율 = 회전율
매수고가 = 고가
매수저가 = 저가
매수고저평균대비등락율 = 고저평균대비등락율
매수가 = 현재가

# Trade quality score (approx, buy-time only)
거래품질점수 = 50
if 매수체결강도 >= 120: 거래품질점수 += 10
if 매수체결강도 >= 150: 거래품질점수 += 10
if 매수호가잔량비 >= 100: 거래품질점수 += 10
if 시가총액 >= 1000 and 시가총액 <= 10000: 거래품질점수 += 10
if 매수등락율 >= 25: 거래품질점수 -= 15
if 매수등락율 >= 30: 거래품질점수 -= 10
if 매수스프레드 >= 0.5: 거래품질점수 -= 10
거래품질점수 = max(0, min(100, 거래품질점수))

# 세그먼트 필터 조건식 (자동 생성)
# 기존 매수 조건식 패턴과 동일한 if-elif NOT 구조 사용
# 시간대 우선 → 시가총액 중첩 구조

# [T1_092900_095900] 시간대
if 시분초 >= 92900 and 시분초 < 95900:
    # [대형주] - 대형주_T1_092900_095900
    if 시가총액 >= 6432:
        if not ((매수매도총잔량 >= 9436.4) and (매수스프레드 < 0.3323) and (매수고저평균대비등락율 < 3.858)):
            매수 = False
    # [중형주] - 중형주_T1_092900_095900
    elif 시가총액 >= 2622 and 시가총액 < 6432:
        매수 = False  # 세그먼트 전체 제외
    # [소형주] - 소형주_T1_092900_095900
    elif 시가총액 >= 1350.5 and 시가총액 < 2622:
        매수 = False  # 세그먼트 전체 제외
    # [초소형주] - 초소형주_T1_092900_095900
    elif 시가총액 >= 0 and 시가총액 < 1350.5:
        매수 = False  # 세그먼트 전체 제외
    else:
        매수 = False  # 정의되지 않은 시가총액 구간

# [T2_095900_102900] 시간대
elif 시분초 >= 95900 and 시분초 < 102900:
    # [대형주] - 대형주_T2_095900_102900
    if 시가총액 >= 6432:
        if not ((당일거래대금_5틱분봉평균_비율 < 1.1847) and (시가총액 >= 7007) and (매수고가 >= 10706)):
            매수 = False
    # [중형주] - 중형주_T2_095900_102900
    elif 시가총액 >= 2622 and 시가총액 < 6432:
        if not ((매수매수총잔량 >= 4536.2) and (매수변동폭 < 3710) and (매수호가잔량비 < 166.174)):
            매수 = False
    # [소형주] - 소형주_T2_095900_102900
    elif 시가총액 >= 1350.5 and 시가총액 < 2622:
        if not ((매수고저평균대비등락율 < 6.864) and (매수고가 < 19912) and (매수시가등락율 >= 4.476)):
            매수 = False
    # [초소형주] - 초소형주_T2_095900_102900
    elif 시가총액 >= 0 and 시가총액 < 1350.5:
        매수 = False  # 세그먼트 전체 제외
    else:
        매수 = False  # 정의되지 않은 시가총액 구간

# [T3_102900_105900] 시간대
elif 시분초 >= 102900 and 시분초 < 105900:
    # [대형주] - 대형주_T3_102900_105900
    if 시가총액 >= 6432:
        if not ((매수회전율 < 31.8705) and (현재가_고저범위_위치 >= 51.4087) and (매수변동폭 < 1200)):
            매수 = False
    # [중형주] - 중형주_T3_102900_105900
    elif 시가총액 >= 2622 and 시가총액 < 6432:
        if not ((매수변동폭 >= 333) and (매수스프레드 < 0.3231) and (시가총액 >= 3135.6)):
            매수 = False
    # [소형주] - 소형주_T3_102900_105900
    elif 시가총액 >= 1350.5 and 시가총액 < 2622:
        if not ((매수호가잔량비 >= 16.61) and (매수회전율 >= 9.24) and (매수변동폭 < 550)):
            매수 = False
    # [초소형주] - 초소형주_T3_102900_105900
    elif 시가총액 >= 0 and 시가총액 < 1350.5:
        if not ((매수변동폭비율 < 29.7817) and (매수회전율 >= 16.852) and (거래품질점수 >= 70)):
            매수 = False
    else:
        매수 = False  # 정의되지 않은 시가총액 구간

# [T4_105900_112900] 시간대
elif 시분초 >= 105900 and 시분초 < 112900:
    # [대형주] - 대형주_T4_105900_112900
    if 시가총액 >= 6432:
        if not ((매수초당거래대금 < 32.884) and (현재가_고저범위_위치 >= 53.144) and (매수호가잔량비 >= 34.246)):
            매수 = False
    # [중형주] - 중형주_T4_105900_112900
    elif 시가총액 >= 2622 and 시가총액 < 6432:
        if not ((매수전일비 >= 53.462) and (시가총액 < 5473) and (매수전일동시간비 >= 1359.77)):
            매수 = False
    # [소형주] - 소형주_T4_105900_112900
    elif 시가총액 >= 1350.5 and 시가총액 < 2622:
        if not ((매수초당매수수량 < 1253.98) and (매수저가 < 16450) and (매수전일동시간비 >= 156.03)):
            매수 = False
    # [초소형주] - 초소형주_T4_105900_112900
    elif 시가총액 >= 0 and 시가총액 < 1350.5:
        매수 = False  # 세그먼트 전체 제외
    else:
        매수 = False  # 정의되지 않은 시가총액 구간

# [T5_112900_115900] 시간대
elif 시분초 >= 112900 and 시분초 < 115900:
    # [대형주] - 대형주_T5_112900_115900
    if 시가총액 >= 6432:
        if not ((매수고저평균대비등락율 < 3.99) and (매수변동폭 >= 782) and (당일거래대금_전틱분봉_비율 >= 1.0021)):
            매수 = False
    # [중형주] - 중형주_T5_112900_115900
    elif 시가총액 >= 2622 and 시가총액 < 6432:
        if not ((매수가 < 44743.6) and (매수체결강도 < 131.148) and (매수호가잔량비 < 42.506)):
            매수 = False
    # [소형주] - 소형주_T5_112900_115900
    elif 시가총액 >= 1350.5 and 시가총액 < 2622:
        if not ((매수시가등락율 < 17.85) and (매수체결강도 >= 90.15) and (매수스프레드 < 0.2243)):
            매수 = False
    # [초소형주] - 초소형주_T5_112900_115900
    elif 시가총액 >= 0 and 시가총액 < 1350.5:
        매수 = False  # 세그먼트 전체 제외
    else:
        매수 = False  # 정의되지 않은 시가총액 구간

else:
    매수 = False  # 정의되지 않은 시간대 제외

# 세그먼트 필터에서 차단된 경우, 차단 목록에 기록
# 다음 번 같은 종목의 매수 시도 시 즉시 차단됨
if not 매수 and (종목코드, _오늘날짜) not in self.세그먼트차단종목:
    try:
        self.세그먼트차단종목[(종목코드, _오늘날짜)] = 시분초
    except NameError:
        self.세그먼트차단종목[(종목코드, _오늘날짜)] = 0

# === 세그먼트 필터 통과 후 기존 조건 평가 ===
# 세그먼트 필터에서 매수=False인 경우, 아래 기존 조건은 추가 제외만 할 뿐 영향 없음

# 공통 선결 조건
if not (관심종목 == 1):
    매수 = False
elif not (0 < 현재가 <= 50000):
    매수 = False
elif not (1.0 < 등락율 <= 20.0):
    매수 = False
elif not (고저평균대비등락율 > 0):
    매수 = False
elif not (현재가 < VI아래5호가):
    매수 = False
elif 라운드피겨위5호가이내:
    매수 = False
elif not (분당거래대금 > 분당거래대금N(1) * 0.0):
    매수 = False
#elif not (RSI < 70):
#    매수 = False
elif 시분초 < 120000:
    if 시가총액 < 100000:
        if not (-1.0 <= 시가등락율 < 20.0):
            매수 = False
        elif not (0 <= 시가대비등락율 < 20.0):
            매수 = False
        elif not (5 < 분당순매수금액 < 10 * 100):
            매수 = False
        elif not (현재가 > (분봉고가 - (분봉고가 - 분봉저가) * 80 / 100)):
            매수 = False
#        elif not (누적거래대금비율 > 1):
#            매수 = False
        elif not (전일비 > 0):
            매수 = False
        elif not (전일동시간비 > 0):
            매수 = False
        elif not (회전율 > 0):
            매수 = False
        elif not (당일거래대금 > 1 * 100):
            매수 = False
#        elif not (당일거래대금N(1) * 1.0  < 당일거래대금):
#            매수 = False
#        elif not (당일거래대금각도(30) > 1 and 당일거래대금각도(30) < 30):
#            매수 = False
#        elif not (분당거래대금 / 분당거래대금평균(30) > 1.0):
#            매수 = False
        # elif not (분당매수수량 > 매도총잔량 * 10 / 100):
        #     매수 = False
#        elif not (매도총잔량 > 매수총잔량 * 0.1):
#            매수 = False
#         elif not (체결강도 >= 50 and 체결강도 <= 300):
#            매수 = False
#        elif not (이동평균(120) < 현재가):
#            매수 = False
#        elif not (이동평균(60) < 현재가 and 이동평균(120) < 이동평균(60)):
#            매수 = False
#        elif not (이동평균(60,30) <= 이동평균(120, 30) and 이동평균(120) < 이동평균(60)):
#            매수 = False
#        elif not (이동평균(300) < 이동평균(120)):
#            매수 = False

    else:
        매수 = False

else:
    매수 = False

#매수 = True



# === 일반 필터 (자동 생성) ===
# 각 필터는 '해당 조건이면 매수하지 않음(제외)'을 의미합니다.
# 여러 필터를 함께 쓴다는 것은: (필터1 통과) AND (필터2 통과) AND ... 입니다.

# === Segment filter runtime mapping (buy-time) ===
# used vars (segment filter): 매수매도총잔량, 매수고저평균대비등락율, 매수변동폭비율, 매수회전율, 매도잔량_매수잔량_비율
# Derived metrics for segment filter evaluation
매수변동폭비율 = ((고가 - 저가) / 저가) * 100 if 저가 > 0 else 0

# Snapshot mappings for buy-time columns
매수매수총잔량 = 매수총잔량
매수매도총잔량 = 매도총잔량
매수회전율 = 회전율
매수고저평균대비등락율 = 고저평균대비등락율
매수매도총잔량_매수매수총잔량_비율 = (매수매도총잔량 / 매수매수총잔량) if 매수매수총잔량 > 0 else 0
매도잔량_매수잔량_비율 = 매수매도총잔량_매수매수총잔량_비율

# [매수매도총잔량 74,595 이상 제외] 개선: 28,448,159원, 제외: 28.7%
if (매수매도총잔량 < 74595.39):
    매수 = False

# [매수고저평균대비등락율 1.15 미만 제외] 개선: 23,832,235원, 제외: 28.7%
if (매수고저평균대비등락율 >= 1.15):
    매수 = False

# [매수변동폭비율 18.36 이상 제외] 개선: 13,369,394원, 제외: 5.2%
if (매수변동폭비율 < 18.36):
    매수 = False

# [매수회전율 0.37~0.56 구간 제외] 개선: 8,951,976원, 제외: 10.2%
if ((매수회전율 < 0.37) or (매수회전율 >= 0.564)):
    매수 = False

# [매도잔량_매수잔량_비율 0.55 미만 제외] 개선: 7,717,655원, 제외: 5.0%
if (매도잔량_매수잔량_비율 >= 0.55):
    매수 = False

# === 일반 필터 반영 완료 ===

if 매수:
    self.Buy(종목코드, 종목명, 매수수량, 현재가, 매도호가1, 매수호가1, 데이터길이)

