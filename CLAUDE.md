# CLAUDE.md - STOM_Version_1U 프로젝트 AI 어시스턴트 지침

> 이 파일은 Claude Code 및 AI 어시스턴트가 이 프로젝트에서 작업할 때 따라야 하는 지침입니다.
>
> **프로젝트 총괄 문서 (1/3)**: 이 문서는 `README.md`, `AGENTS.md`와 함께 프로젝트를 총괄합니다.

---

## 프로젝트 개요

**STOM (System Trading Optimization Machine)** - 틱데이터 기반 초단타 시스템 트레이딩 도구

현재 브랜치 `STOM_Version_1U`는 V1 코드베이스에 V2의 37개 커밋(V2.00~V2.36) 변경사항을 **코드 이해 기반으로 재구현**하는 프로젝트입니다.

---

## 문서 체계 및 관리 책임

### 프로젝트 총괄 문서 (최상위)

| 문서 | 역할 | 관리 대상 |
|------|------|----------|
| `README.md` | 프로젝트 소개 및 총괄 | 전체 프로젝트, docs/README.md |
| `CLAUDE.md` | AI 작업 지침 및 규칙 | 전체 프로젝트, docs/README.md |
| `AGENTS.md` | 아키텍처 및 개발 가이드 | 전체 프로젝트, docs/README.md |

### 하위 문서 (docs/)

| 문서 | 역할 | 관리 주체 |
|------|------|----------|
| `docs/README.md` | docs 폴더 문서 총괄 | 상위 3개 문서가 관리 |
| `docs/dev_plan/*.md` | 개발 계획 문서 | docs/README.md |

---

## 필수 참조 문서

**반드시 아래 문서들을 이 순서대로 참조하십시오:**

### 1. 개발 계획서 (최우선)
```
docs/dev_plan/STOM_Version_1U_Development_Plan.md
```
- 이 프로젝트의 **모든 개발 작업의 근거**이자 **실행 명세서**
- 37단계 순차 개발 계획, V2 커밋 분석, ui_mainwindow.py 추론 방법론 포함
- **어떤 작업이든 이 문서를 먼저 확인한 후 진행**

### 2. 에이전트 가이드 (상세 참조)
```
AGENTS.md
```
- 프로젝트 아키텍처, 코드 패턴, 디렉토리 구조, 파일 역할 상세
- 개발 작업 프로토콜, 검증 체크리스트
- Git 참조 명령어, V2 커밋 해시 빠른 참조

### 3. 문서 허브 (문서 관리)
```
docs/README.md
```
- docs/ 폴더 내 모든 문서의 중앙 관리 허브
- 새 문서 추가 시 이 파일에 등록 필수
- **이 문서는 상위 3개 문서(README.md, CLAUDE.md, AGENTS.md)가 관리**

---

## 핵심 규칙

### 절대 금지 사항
1. **체리픽(cherry-pick) 사용 금지** - V2 커밋을 git cherry-pick으로 가져오지 말 것
2. **ui/ui_mainwindow.pyd 파일 생성/추가 금지** - 바이너리 파일은 이 브랜치에 존재해서는 안 됨
3. **순서 무시 금지** - V1U.00 → V1U.01 → ... 순서를 반드시 지킬 것
4. **V2 코드 맹목 복사 금지** - V1U의 현재 상태를 이해하고 해당 맥락에 맞게 적용

### 필수 준수 사항
1. **ui/ui_mainwindow.py는 소스코드(.py) 형태를 유지**할 것
2. V2 커밋의 diff를 **분석하고 이해한 뒤** 적용할 것
3. pyd 파일은 읽을 수 없으므로 **주변 파일 변경, 커밋 메시지, pyd 크기 변동으로 추론**
4. **V2 커밋 수(37개)와 동일한 수로 V1U 커밋 진행** (V1U.00 ~ V1U.36)
5. **각 단계별 체크 과정 필수** (아래 체크 프로세스 참조)
6. **커밋 메시지는 상세하게 작성** (아래 커밋 규칙 참조)

---

## 커밋 규칙 (상세)

### 버전 매칭

**V1U.XX는 반드시 V2.XX와 매치됩니다:**

| V2 커밋 | V1U 커밋 | 대응 관계 |
|---------|----------|----------|
| V2.00 (b021269) | V1U.00 | 키움증권 해외선물 추가 |
| V2.01 (9d065c0) | V1U.01 | 텔레그램봇 업그레이드 |
| V2.02 (69067bf) | V1U.02 | DataFrame → Dictionary 전환 |
| ... | ... | ... |
| V2.36 (ddfd9fb) | V1U.36 | MERGE값 합산 방법 변경 |

### 커밋 메시지 형식

```
STOM V1U.XX - <핵심 변경 요약>

## 대응 V2 커밋
- V2.XX (<commit_hash>)
- 원본 커밋 메시지: <V2 커밋 메시지>

## 변경 내역
1. <변경 사항 1>
   - 상세 설명
   - 영향 받는 파일: <파일 목록>

2. <변경 사항 2>
   - 상세 설명
   - 영향 받는 파일: <파일 목록>

## ui_mainwindow.py 변경 (해당 시)
- pyd 크기 변동: <이전 크기> → <현재 크기> (<변동량>)
- 추론 근거: <주변 파일 변경, 커밋 메시지 분석 내용>
- 적용 내용: <ui_mainwindow.py에 적용한 변경>

## 검증 체크리스트
- [x] import 문 검증 완료
- [x] 함수 시그니처 일치 확인
- [x] 변수명 일관성 확인
- [x] Queue 변수명 일치 확인
- [x] ui_mainwindow.py 메서드-외부함수 대응 확인
```

### 커밋 메시지 예시

```
STOM V1U.00 - 키움증권 해외선물 추가 및 인증 시스템 구현

## 대응 V2 커밋
- V2.00 (b021269)
- 원본 커밋 메시지: STOM V2.00

## 변경 내역
1. future/ 디렉토리 생성 및 해외선물 모듈 구현
   - future_kiwoom.py: 키움 해외선물 에이전트
   - future_manager.py: 해외선물 매니저 (32bit)
   - future_strategy_tick.py: 틱 전략연산
   - future_trader.py: 트레이더
   - 영향 받는 파일: future/*.py (12개 파일)

2. 인증 시스템 추가
   - 시리얼키 검증 로직 구현
   - 영향 받는 파일: ui/ui_mainwindow.py, utility/setting.py

3. 스톰 라이브 ZMQ 방식 전환
   - 기존 socket → ZMQ pubsub 방식
   - 영향 받는 파일: ui/ui_mainwindow.py

4. strftime/strptime 전면 수정
   - 날짜 포맷 일관성 확보
   - 영향 받는 파일: utility/static.py, backtester/*.py

## ui_mainwindow.py 변경
- pyd 크기 변동: 782,336 → 846,848 (+64,512)
- 추론 근거: 해외선물 UI 요소, 인증 시스템, ZMQ 라이브 코드 추가
- 적용 내용:
  - FutureQ 관련 Writer 처리 추가
  - 인증 검증 메서드 추가
  - ZmqServ/ZmqRecv 클래스 수정

## 검증 체크리스트
- [x] import 문 검증 완료
- [x] 함수 시그니처 일치 확인
- [x] 변수명 일관성 확인
- [x] Queue 변수명 일치 확인
- [x] ui_mainwindow.py 메서드-외부함수 대응 확인
```

---

## 각 단계별 체크 프로세스 (필수)

```
┌───────────────────────────────────────────────────────────────┐
│ 단계 1: V2 커밋 이해                                           │
│   └─ git show --stat <V2_commit_hash>                         │
│   └─ 커밋 메시지 분석, 변경 파일 목록 확인                       │
│   └─ pyd 크기 변동 여부 확인 (AGENTS.md 참조)                   │
├───────────────────────────────────────────────────────────────┤
│ 단계 2: V1U 적용                                               │
│   └─ V1U 현재 상태에 맞게 코드 수정                             │
│   └─ 맹목적 복사 금지, 현재 맥락에 맞게 적용                     │
│   └─ 주변 파일 먼저 수정 → ui_mainwindow.py 마지막              │
├───────────────────────────────────────────────────────────────┤
│ 단계 3: 검증 체크리스트 (모두 확인 후 커밋)                      │
│   ├─ [ ] import 문이 존재하는 파일을 참조하는가                  │
│   ├─ [ ] 함수 시그니처가 호출측/피호출측 일치하는가              │
│   ├─ [ ] 변수명 일관성 확인 (수익률 vs 수익율)                   │
│   ├─ [ ] Queue 변수명이 프로세스 간 일치하는가                   │
│   └─ [ ] ui_mainwindow.py 메서드가 외부 함수와 대응하는가        │
├───────────────────────────────────────────────────────────────┤
│ 단계 4: 커밋 (상세 메시지 작성)                                  │
│   └─ 위 "커밋 메시지 형식" 참조                                 │
│   └─ V1U.XX ↔ V2.XX 매칭 명시                                  │
│   └─ 변경 내역, ui_mainwindow.py 추론, 검증 체크리스트 포함      │
└───────────────────────────────────────────────────────────────┘
```

---

## 작업 시작 방법

### 새로운 단계를 시작할 때

1. `docs/dev_plan/STOM_Version_1U_Development_Plan.md`에서 해당 Step 확인
2. `AGENTS.md`에서 관련 아키텍처/패턴 참조
3. V2 커밋의 diff 분석:
   ```bash
   git show --stat <V2_commit_hash>
   git diff <이전V2커밋>..<현재V2커밋> -- <파일>
   ```
4. 코드 변경 적용 (주변 파일 → ui_mainwindow.py 순서)
5. 검증 체크리스트 확인
6. 상세 커밋 메시지 작성 후 커밋

### 현재 진행 상태 확인

```bash
git log --oneline STOM_Version_1U
```

---

## 기술 스택

| 항목 | 기술 |
|------|------|
| 언어 | Python 3.11 |
| GUI | PyQt5 |
| IPC | ZMQ, multiprocessing.Queue |
| DB | SQLite3 |
| 차트 | pyqtgraph |
| 거래소 API | 키움증권 OCX, 업비트 REST/WebSocket, 바이낸스 REST/WebSocket |
| 백테스트 | 자체 엔진 (멀티프로세스) |
| 최적화 | 그리드, 유전 알고리즘(GA), 옵튜나 |

---

## 코드 컨벤션

- 한글 변수명 사용 (의도된 설계, 유지할 것)
- `수익률` (O) vs `수익율` (X) - V2.01에서 수정됨
- MainWindow 메서드는 외부 모듈에 위임하는 패턴: `def XXX(self): xxx(self)`
- Queue 변수명: `windowQ`, `stockQ`, `coinQ`, `futureQ`, `testQ` 등
- 상수는 `utility/static.py`에 정의

---

## 참고

- 이 프로젝트는 파이퀀트 강좌 수강생 전용 소스코드입니다
- 상업적 이용 및 미수강자 공유가 금지되어 있습니다
- 상세한 아키텍처와 파일 설명은 `AGENTS.md`를 참조하십시오
