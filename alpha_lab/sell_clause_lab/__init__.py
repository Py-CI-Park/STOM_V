"""매도식 D1 — 챔피언 매도식 절-단위 ablation 하니스 (봉인 bd5bb3c4).

labels_v2 는 sha 차단 + 하드코딩 미러라 변형 매도식을 재채점하지 못한다
(봉인 §14 실사). 본 패키지는 그 발화 로직을 drop_clause 파라미터화 미러로
복제한다 — 원본(labels_v2·pilot_v2) 무수정, 상수·컨텍스트·비용 경로는 전부
원본에서 임포트 재사용(드리프트 최소화). 미러의 존재 증명은 §14-F9 원본재현
게이트(drop=None ↔ 은행 862,932건 전수 비트동일, kill-2 지위)가 담당한다.

모듈: harness(드롭 발화·라벨) · measure(일 루프·재현 게이트·ablation) ·
judge_s(등급·부트스트랩·FDR·판정·리포트).
"""
