# v5.0 리포트 복합 매수 적응판 v2 — 지연 계산 구조(선게이트 후계산, 엔진 부하 절감)
# C1 시그널 기반이되, 성능을 위해 두 가지가 C1보다 약간 엄격하다:
#  (1) 고저평균대비등락율>0을 점수가 아닌 하드 게이트로 승격
#  (2) 체결강도 >= 최소체결강도-20 선필터 추가(낮은 체결강도의 F20 단독 발화 일부 차단)
# 나머지 파생변수·플래그·점수 논리는 C1과 동일하다(시드/gen4 검증 지연계산 패턴).
매수 = False

if (90030 <= 시분초 < 92000 and 관심종목 == 1 and 1000 <= 현재가 <= 100000
        and -2 <= 등락율 <= 25 and 당일거래대금 >= 500 and 초당거래대금 > 10
        and 데이터길이 >= 31
        and 고저평균대비등락율 > 0 and 현재가 < VI가격 - VI호가단위 * 5):
    스프레드틱 = (매도호가1 - 매수호가1) / 호가단위 if 호가단위 != 0 else 99

    if 시가총액 < 5000:
        최소체결강도 = 150
        최소매수공격비 = 1.7
        최소누적공격비 = 1.3
        최소거래폭발 = 3.5
        최대스프레드 = 2.0
        최소점수 = 7
        최대위험 = 2
    elif 시가총액 < 20000:
        최소체결강도 = 135
        최소매수공격비 = 1.5
        최소누적공격비 = 1.2
        최소거래폭발 = 2.8
        최대스프레드 = 2.0
        최소점수 = 6
        최대위험 = 3
    else:
        최소체결강도 = 115
        최소매수공격비 = 1.2
        최소누적공격비 = 1.05
        최소거래폭발 = 1.8
        최대스프레드 = 3.0
        최소점수 = 5
        최대위험 = 3

    if 스프레드틱 <= 최대스프레드 and 체결강도 >= 최소체결강도 - 20:
        전일종가 = 현재가 / (1 + 등락율 / 100) if (1 + 등락율 / 100) != 0 else 현재가
        시가등락율 = ((시가 - 전일종가) / 전일종가) * 100 if 전일종가 != 0 else 0

        if 시가등락율 <= 16:
            시가대비등락율 = ((현재가 - 시가) / 시가) * 100 if 시가 != 0 else 0
            고저범위 = 고가 - 저가
            현재가위치 = (현재가 - 저가) / 고저범위 * 100 if 고저범위 > 0 else 50
            초당순매수금액 = (초당매수수량 - 초당매도수량) * 현재가 / 1_000_000
            매수공격비1 = 초당매수수량 / 초당매도수량 if 초당매도수량 > 0 else 0
            매수공격비5 = 누적초당매수수량(5) / 누적초당매도수량(5) if 누적초당매도수량(5) > 0 else 0
            매수공격비10 = 누적초당매수수량(10) / 누적초당매도수량(10) if 누적초당매도수량(10) > 0 else 0
            매도공격비5 = 누적초당매도수량(5) / 누적초당매수수량(5) if 누적초당매수수량(5) > 0 else 0
            거래폭발5 = 초당거래대금 / 초당거래대금평균(5, 1) if 초당거래대금평균(5, 1) > 0 else 0
            거래폭발10 = 초당거래대금 / 초당거래대금평균(10, 1) if 초당거래대금평균(10, 1) > 0 else 0
            거래폭발30 = 초당거래대금 / 초당거래대금평균(30, 1) if 초당거래대금평균(30, 1) > 0 else 0
            체결상승10 = 체결강도 / 체결강도평균(10, 1) if 체결강도평균(10, 1) > 0 else 1
            체결상승30 = 체결강도 / 체결강도평균(30, 1) if 체결강도평균(30, 1) > 0 else 1
            상단소화율 = (매도잔량1N(1) - 매도잔량1) / 매도잔량1N(1) * 100 if 매도잔량1N(1) > 0 else 0
            하단붕괴율 = (매수잔량1N(1) - 매수잔량1) / 매수잔량1N(1) * 100 if 매수잔량1N(1) > 0 else 0
            총호가매수비율 = 매수총잔량 / (매수총잔량 + 매도총잔량) if (매수총잔량 + 매도총잔량) > 0 else 0.5

            F01 = 90030 <= 시분초 < 90200 and 2 <= 시가등락율 <= 12 and 현재가 >= 시가 + 호가단위 * 2 and 체결강도 >= 최소체결강도 and 매수공격비1 >= 최소매수공격비 and 매수공격비5 >= 최소누적공격비 and 거래폭발5 >= 최소거래폭발 * 0.55
            F02 = 90030 <= 시분초 < 90200 and -1 <= 시가등락율 <= 6 and 시가대비등락율 >= 0.5 and 현재가 > 최고현재가(5, 1) and 매수공격비5 >= 최소누적공격비 and 체결강도 >= 최소체결강도 - 15
            F04 = 90030 <= 시분초 < 90500 and 누적초당매도수량(10) >= 누적초당매수수량(10) * 0.75 and 현재가 >= 시가 - 호가단위 * 2 and 현재가 > 최고현재가(10, 1) and 거래폭발10 >= 2 and not 호가하락압력(10, 0.35)
            F05 = 90200 <= 시분초 < 91000 and 현재가 > 최고현재가(30, 1) and 거래폭발30 >= 최소거래폭발 and 체결강도 >= 최소체결강도 - 5 and 매수공격비10 >= 최소누적공격비
            F06 = 90200 <= 시분초 < 91500 and 체결상승30 >= 1.1 and 체결강도 >= 최소체결강도 - 5 and 구간호가총잔량비율(30) >= 0.52 and 매수공격비10 >= 최소누적공격비
            F07 = 데이터길이 >= 61 and 90500 <= 시분초 < 92000 and 횡보감지(30, 0.5) and 현재가 > 최고현재가(30, 1) and 거래대금급증(30, 2.5) and 체결강도급등(30, 1.05)
            F10 = 데이터길이 >= 61 and 시가총액 >= 20000 and 90500 <= 시분초 < 92000 and 현재가 > 이동평균(60, 1) and 거래폭발30 >= 1.6 and 누적초당매수수량(30) >= 누적초당매도수량(30) * 1.05 and 체결강도 >= 110
            F20 = 90100 <= 시분초 < 92000 and 매수공격비1 >= 1.2 and 매수공격비5 >= 1.15 and 매수공격비10 >= 1.1 and 체결강도 >= 체결강도평균(10, 1) and 현재가 >= 현재가N(3) and 현재가 > 최고현재가(10, 1)

            위험점수 = 0
            if 90030 <= 시분초 < 90200 and 현재가 < 시가 - 호가단위 * 2:
                위험점수 += 3
            if 초당매도수량 > 초당매수수량 * 2:
                위험점수 += 2
            if 매도공격비5 >= 1.8:
                위험점수 += 2
            if 매수총잔량 < 매수총잔량N(1) * 0.65:
                위험점수 += 2
            if 라운드피겨위5호가이내 and 초당매도수량 > 초당매수수량:
                위험점수 += 1
            if 하단붕괴율 >= 35:
                위험점수 += 2

            복합점수 = 0
            if F01: 복합점수 += 4
            if F02: 복합점수 += 4
            if F04: 복합점수 += 4
            if F05: 복합점수 += 4
            if F06: 복합점수 += 3
            if F07: 복합점수 += 3
            if F10: 복합점수 += 3
            if F20: 복합점수 += 2
            if 현재가위치 >= 70: 복합점수 += 1
            if 고저평균대비등락율 >= 0: 복합점수 += 1
            if 초당순매수금액 >= 30: 복합점수 += 1
            if 상단소화율 >= 15: 복합점수 += 1
            if 총호가매수비율 >= 0.45: 복합점수 += 1
            if 체결상승10 >= 1.05: 복합점수 += 1

            전략플래그수 = 0
            if F01: 전략플래그수 += 1
            if F02: 전략플래그수 += 1
            if F04: 전략플래그수 += 1
            if F05: 전략플래그수 += 1
            if F06: 전략플래그수 += 1
            if F07: 전략플래그수 += 1
            if F10: 전략플래그수 += 1
            if F20: 전략플래그수 += 1

            if 위험점수 <= 최대위험 and 전략플래그수 >= 1 and 복합점수 >= 최소점수:
                매수 = True

if 매수:
    self.Buy()
