# Planner Stage 1 — STOM Dashboard V2/V3 selectable rollout

## Summary
Plan only. Worktree `C:/System_Trading/STOM/STOM_V.wt-dashboard-remodel`. Compare V2 at `http://127.0.0.1:8770/` and V3/remodel at `http://127.0.0.1:8776/`; preserve V2 while making V3 selectable; inventory routes/pages/sections/buttons/functions; identify V3 gaps; define implementation and verification to reach 100 percent without live order, broker login, account trading, hidden production export, or bypass of Human Approval Gate and Append-Only Audit.

Evidence inspected: root `AGENTS.md`, `ai_strategy_loop/AGENTS.md`, `ai_strategy_loop/dashboard/app.py`, V3 `frontend/remodel/index.html`, `frontend/remodel/src/app.js`, remodel docs `TAB_CHECKLIST.md`, `UI_IMPLEMENTATION_SPEC.md`, `DATA_CONTRACT.md`, `ARCHITECTURE.md`, capture summaries, and focused route/remodel tests.

Known facts: 8770 serves old V2 title `STOM AI · 조건식 자율 진화 대시보드` on `/`, `/ui/evolution`, `/ui/backtest`, `/ui/chart-replay`; `/ui/remodel/*` is 404. 8776 serves V3 title `STOM AI · 조건식 AI 연구 대시보드` on `/ui/evolution`, `/ui/evolution/process`, `/ui/backtest`, `/ui/chart-replay`, `/ui/remodel/condition`, `/ui/remodel/audit`, includes `/ui/remodel/src/app.js?v=20260628canonical`, excludes `/ui/bundle/app.js`, and uses no-cache headers. Current route code maps canonical dashboard routes to `_dashboard_remodel_index_response()` in this remodel worktree. Recent focused checks already passed before this planner pass: 28 route/remodel tests, py_compile, node check, git diff check.

## RALPLAN-DR summary
Principle 3: preserve stable V2 contracts before rollout speed. Principle 4: make V3 selection observable and reversible with route owner, mode, local-only boundary, Human Approval Gate, and Append-Only Audit visible. Principle 5: never cross trading/export boundaries; no live order, broker login, account trading, hidden final approval, DB cutover, or automatic production export.

Top decision drivers: 1 safety and branch policy, 2 V2 continuity on 8770, 3 evidence-backed 100 percent inventory and verification.

Option A recommended: V2 default on 8770, V3 explicit under `/ui/remodel/*` plus optional selector/query/session flag, 8776 remains preview. Pros: preserves V2, reversible, route-by-route promotion possible. Cons: needs dispatch and selector tests. Option B: separate-port preview only. Pros low risk; cons not selectable in main surface. Option C: immediate canonical V3 replacement. Pros fast; cons violates V2 preservation and is not recommended before explicit approval.

## In scope / out of scope
In scope: route/version plan, V2/V3 inventory, V3 gap list, future implementation sequence, verification plan, pre-mortem. Out of scope: product edits by this planner, formatters, project-wide tests, broker login, live order, account trading, operating DB writes, hidden export, V3K gate execution.

## File-level changes for future execution
`ai_strategy_loop/dashboard/app.py`: add explicit dashboard-version dispatch; V2 default for `/`, `/ui/`, `/ui/evolution`, `/ui/backtest`, `/ui/chart-replay`; V3 only by explicit selection or `/ui/remodel/*`; keep no-cache V3 shell and explicit allowlists. V2 frontend files: add V3 preview selector/link without removing old title, bundle, routes, or functions. V3 remodel files: keep cache-busted assets, deterministic reference/demo mode, live safe-read adapters, manual gates for `/bt/*` mutations and `/sim/ws`, separate Winner Export from Decision Audit. Tests: extend route parity, remodel static, baseline contract, and UI remodel tests for V2 default plus V3 selectable behavior. Visual gate: add V2/V3 comparison artifacts.

## Sequencing and dependencies
Phase 0 baseline: capture V2/V3 status, title, asset markers, cache headers, redirects, forbidden markers; DOM inventory V2 and V3; diff matched, missing, added, changed, unsafe items. Phase 1 selectable rollout: selection priority is `/ui/remodel/*`, explicit selector/query/session flag, preview launch, then V2 default; implement dispatch without broad catchall; add V2 to V3 and V3 to V2 navigation. Phase 2 parity closure: bind V3 pages to safe local APIs, keep fixtures labeled, fail closed, gate mutations, disable or explain unwired controls. Phase 3 verification: focused tests, syntax checks for touched files, browser captures, DOM/source safety scans, read-only API evidence, manual QA. Phase 4 promotion: only after 100 percent evidence and explicit human approval; rollback by route/config flag.

## 8 V3 page checklist
1 Condition AI `/ui/remodel/condition` and preview `/ui/evolution`: shell, backend URL, reconnect, REST/WS/run badges, route owner/boundary, top/nested tabs, LIVE/ARCHIVE, progress, settings, Start/Stop, live generation, active strategy, phase timeline/detail, criteria, glossary, config, engine, cost, charts, GUI parity, HoF, generation table, code/detail actions, Winner approval/export, autopsy, Strategy Inspector, analytics tiles. Buttons: theme, reconnect, URL, tabs, live/archive, settings, Start/Stop, approval, inspector, copy/detail. Gap: fixture panels and research-loop-only Start/Stop.
2 Process `/ui/remodel/process`: run selector, process menu, safety/governance, Generation to Backtest to Scoring to Autopsy to Repeat map, KPIs, logs, node/process catalogs, boundary contract, metadata, settings. Gap: static map needs safe state or fixture label.
3 History `/ui/remodel/history` or records: archive table, filters/sort/windowing, research records, ResultDetail, Compare, lineage search, run detail, strategy gist, best generation, approval-needed export status. Gap: exact V2 field/function diff and large archive windowing.
4 Lab `/ui/remodel/lab`: active runs, stalled warnings, batch queue, freeze verdict, run list, Edge Ratio, variable importance, correlation, combinations, holdout, wiki/context/process/glossary/visual quality, data quality, not-for-investment cue. Gap: read-only API binding, no auto-start.
5 Workbench `/ui/remodel/workbench`: candidate selector, HoF workbench, heatmap, candidate cards, equity/IC/risk charts, metrics, exposure, evidence notes, review queue, handoff, no approval/export authority. Gap: queue/handoff visual unless safely wired or disabled.
6 Decision Audit `/ui/remodel/audit` and verdict: append-only ledger, PROMOTE checklist, OOS CI, alerts, regime, revival, V6/M4, evidence links, Human Decision form, note validation, history hash, audit metadata, separated from final export. Gap: prove append-only persistence.
7 Backtest `/ui/remodel/backtest`: health/demo banner, API matrix, run panel, modes, buy/sell selectors, dates, optimize JSON, WFO, sweep/self.vars, active job/logs, condition editor, results, compare, portfolio, reports. Safe GET probes only on load; `/bt/run`, strategy save/delete, cancel, portfolio, `/bt/ws_job` manual-gated.
8 Chart Replay `/ui/remodel/chart-replay`: replay API/WS matrix, source/day/stocks/strategies, aggregation, presets, immediate replay, playback, chart modes/layout, indicators, replay charts, signal log, metadata, minimap, `/sim/ws` manual gate. Safe GET probes only; WS start/pause/resume/speed/seek/stop user-gated and never auto-opened.

## V2 route checklist
Preserve default V2 on `/`, `/ui/`, `/ui/evolution`, `/ui/evolution/process`, `/ui/evolution/records`, `/ui/evolution/lab`, `/ui/evolution/workbench`, `/ui/evolution/verdict`, `/ui/backtest`, `/ui/chart-replay`, aliases `/ui/process`, `/ui/records`, `/ui/history`, `/ui/lab`, `/ui/pro`, `/ui/verdict`, `/ui/simulation`. Assert old title and V2 assets by default, no V3 asset unless selected, existing V2 buttons/functions intact, `/ui/remodel/*` either current 404 or explicit V3 selection only.

## Current V3 gaps
Default preservation gap, since canonical routes in remodel worktree already serve V3. Main-server selectability gap, since 8770 has `/ui/remodel/*` as 404. Fixture/data gap from `window.STOM_DATA`. Button semantics gap for visual/manual controls. Audit persistence gap. Export boundary gap. Exact V2/V3 parity diff not yet complete. Observability artifact gap. Cache/version cross-contamination risk. Unknown-route catchall risk.

## Acceptance criteria
V2 default routes on 8770 keep old title/assets unless V3 selected. V3 explicit selection shows V3 title/assets and excludes V2 bundle. All listed V2 routes and 8 V3 pages have status/title/asset/cache/redirect assertions. 100 percent section/button/function inventory has no unexplained missing or unsafe item. No live order, broker login, account trading, hidden export; Human Approval Gate and Append-Only Audit visible and enforced; `/bt/*` mutations and `/sim/ws` manual-gated. Focused tests pass; browser has zero console/page errors; visual gate min page 95 and average 97 or documented waiver; source/DOM safety scans pass; API evidence is read-only/local.

## Verification
Future focused tests: `python -m pytest tests/unit/test_dashboard_route_parity.py tests/unit/test_dashboard_remodel_baseline_contract.py tests/unit/test_dashboard_remodel_static.py tests/unit/dashboard/test_dashboard_ui_remodel.py -q`. Syntax for touched files: `python -m py_compile ai_strategy_loop/dashboard/app.py`; `node --check ai_strategy_loop/dashboard/frontend/remodel/src/app.js`. Visual/API/browser: `python scripts/verify_dashboard_remodel_visual_gate.py --base-url http://127.0.0.1:8776 --min-page-score 95 --min-average-score 97`; extend with `--compare-base-url http://127.0.0.1:8776` from 8770. Required artifacts: 8 V3 screenshots/full pages, V2 screenshots, side-by-side contact sheet, route matrix, scorecard, source safety, DOM safety, modal coverage, read-only API evidence. Manual browser QA: open V2/V3 side by side, click all tabs/modals/nonmutating controls, verify no forbidden network calls.

## Expanded test plan
Unit: route dispatch, V2 default, V3 explicit, selector gate, unknown 404, V3 no-cache, source safety, no auto mutating endpoint, no `/sim/ws` on load. Integration: FastAPI route matrix, read-only dashboard probes, backtest safe GET and manual POST gates, replay safe GET and user-gated WS, append-only audit once wired, export request gate. E2E: Chromium 8 V3 pages plus V2 routes, DOM inventory, modal coverage, console/page-error zero, network forbidden scan, visual scoring, responsive/full-page scroll. Observability: route matrix JSON, API evidence JSON, source/DOM safety JSON, modal coverage JSON, scorecard/contact sheets, manual QA notes, rollback state.

## Risks and mitigations
Risk 1 V3 replaces V2 by accident: V2 default tests, explicit opt-in, rollback flag. Risk 2 fixture data mistaken as live: provenance cues, fixture labels, fail-closed errors, live read evidence. Risk 3 export/trading boundary crossed: no hidden `final_approval`, manual gates, source/DOM/network scans. Risk 4 broad catchall hides broken routes: explicit allowlists and unknown route 404. Risk 5 visual parity hides broken functions: pair visual gate with DOM inventory, network assertions, modal coverage, API contract tests.

## Pre-mortem
Scenario 1: 8770 shows V3 unexpectedly and users lose V2. Prevent with default-route tests and visible opt-in only. Scenario 2: approval/audit/backtest/replay button causes production side effect. Prevent with no hidden final approval, manual confirmation, safety scans, no mutating calls on load. Scenario 3: 100 percent claim rests on fixture screenshots. Prevent with V2/V3 DOM diff, live read-only evidence, empty/error/loading states, V2 screenshots, observability artifacts.

## Handoff
After approval, use executor for route selector/tests, V2 selector/link, V3 page data gaps, and visual comparison extension. Use architect before default-route promotion, critic if canonical replacement is proposed, team only for approved parallel execution, ultragoal only for a durable multi-checkpoint ledger.
