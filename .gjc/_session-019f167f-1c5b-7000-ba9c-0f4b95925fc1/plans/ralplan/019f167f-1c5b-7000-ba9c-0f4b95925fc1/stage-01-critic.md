**[ITERATE]**

**Routing**: REQUEST CHANGES before finalization.

**Justification**: The planner direction is strong and aligned with the architect WATCH/APPROVE review: Option B is a fair middle path, keeps V3 explicit under /ui/remodel/*, preserves V2 as default, and addresses the real gap that the prior 100/100 package measured route ownership, text presence, safety labels, and visual nonblankness rather than human task success. Referenced files were verified: artifacts/ultragoal-g007-final-evidence/final-report.md reports PASS/100 evidence, final-100-scorecard.json is dominated by contract/evidence categories, visual-gate/scorecard.json documents text/safety/pixel/RMSE/histogram scoring, v2-v3-compare/compare-scorecard.json proves V2 default bundle vs explicit V3 remodel bundle, safety-audit/safety-scorecard.json is 100, and ai_strategy_loop/dashboard/frontend/remodel/src/app.js contains the explicit route mapping, render dispatch, safety footer, UX sweep panels, and backtest/replay contract matrices. The implementation surface is feasible for the no-build static SPA described by docs/ARCHITECTURE.md.

The plan should not be finalized yet because its decisive new success mechanism, scripts/verify_dashboard_human_ux_rubric.py, is still underspecified. The acceptance criteria use numeric score targets, but the category-to-observation scoring method, scripted task paths, artifact schema, and V2 baseline/delta calculation are not concrete enough for executors to implement without guessing. This repeats the architect WATCH item: the human rubric must be scripted/falsifiable, V2/default and safety evidence must be protected through tranches, and backtest/chart replay need early storyboard/rubric proof. Direction is approvable; final plan needs one planner iteration to make testability hard and non-subjective.

**Summary**:
- **Clarity**: Mostly clear. The scope, Option B recommendation, route boundaries, page-by-page goals, and file list are understandable. The weak point is the new rubric: phrases such as obvious primary next action, without hunting, readable scan order, and workflow quality need measurable pass/fail definitions.
- **Verifiability**: Not yet sufficient. Existing route/safety/visual commands are concrete, but the proposed human rubric command has no scoring schema, task trace requirements, viewport assertions, DOM/test-id contract, screenshot annotation requirements, or V2 baseline calculation details.
- **Completeness**: Directionally complete for planning, but incomplete as an execution handoff. It covers all eight pages and safety/default constraints, yet omits a required Tranche 0 rubric/storyboard deliverable before large UI churn.
- **Big Picture**: Correct. The root problem is measurement/IA mismatch, not missing widgets. Option B fits the current static SPA and avoids an oversized design-system rebuild.
- **Principle/Option Consistency**: Mostly consistent. Task first, evidence second and V3 wins only by measured usability over V2 conflict with sequencing that puts human rubric harness and annotated artifacts after Tranches A-C. Measurement must come before and during page migrations, not after them.
- **Alternatives Depth**: Acceptable for short-mode. Option A, B, and C are fair, with Option C deferred rather than invalidated. No single-option forcing detected.
- **Risk/Verification Rigor**: Good safety/default risk awareness, but UX-risk rigor is still too subjective. The plan needs falsifiable task scripts, tranche evidence checkpoints, and explicit backtest/replay early proof.

**Representative implementation simulations**:
1. **Shared IA primitives in src/app.js / styles/theme.css**: Feasible. The current app has a central route map for condition, process, history, lab, workbench, audit, backtest, and chart-replay, a render dispatch, shared panel/chart/table helpers, grids, and safety footer. A task header/canvas/rail/drawer helper can be added without framework migration. However, the plan should specify durable selectors/test IDs for the new task frame so the human rubric and static tests can observe it.
2. **Human rubric harness**: Not executable without planner revision. Score V2/V3 screenshots, DOM markers, and scripted task paths is a goal, not a contract. An executor would have to invent scenario steps, score weights per observable, evidence JSON fields, failure examples, and how to compute the 15-point V2 delta. This is the main blocker to finalization.
3. **Backtest and chart replay redesign**: Architecturally feasible but high-risk. Existing app.js proves backtest/replay safety through contract matrices, inert reference/demo mode, manual-gated buttons, and no auto /sim/ws stream. The plan correctly says to move matrices to evidence drawers and make the workflows task-first, but it must require early storyboard/rubric proof before implementation so contract evidence is not hidden and replay/backtest do not regress into subjective looks-better acceptance.

**Concrete required fixes**:
1. Add a Tranche 0 before UI implementation: lock V2/V3 baseline captures, define the human UX rubric script contract, produce page storyboards for at least condition, backtest, and chart replay, and require architect/critic acceptance of the rubric before page churn.
2. Define scripts/verify_dashboard_human_ux_rubric.py as a falsifiable artifact, not just a command. Include scenario IDs, route URLs, viewport sizes, DOM/test-id selectors, first-fold bounding-box checks, keyboard/focus checks, network assertions, screenshot/contact-sheet annotation requirements, and a JSON schema with category scores, task results, V2 baseline, V3 score, delta, failures, and evidence paths.
3. Replace subjective acceptance language with thresholds. Examples: maximum same-weight first-fold panels, required task header fields, minimum main chart dimensions, required chart title/axis/unit/legend/value/threshold selectors, selected heatmap-cell narrative, maximum tab stops to primary action, no horizontal primary-task scroll, and explicit failure thresholds per category.
4. Move verification earlier and per tranche: after shared IA and after each page tranche, run only focused route/default preservation, safety/network, and human-rubric tranche checks; final 90/100 and 15-point delta must not be the first time usability is measured.
5. Add early backtest/chart replay proof: storyboard the workflows select strategy/data -> edit condition -> validate -> gated run/preview -> analyze result and choose source/date/symbol -> choose strategy -> preview bars/signals -> start manual replay -> investigate signal, then map each step to rubric observations and required safety assertions: no POST/WS on load, manual gate next to action, contract markers retained in accessible drawers.
6. Preserve existing safety/identity DOM contracts while using progressive disclosure: list which current required markers from route compare, safety audit, backtest contract matrix, and replay contract matrix must remain discoverable to tests after moving them into drawers/tabs.

Until those revisions are made, return to Planner for iteration rather than approving execution.
