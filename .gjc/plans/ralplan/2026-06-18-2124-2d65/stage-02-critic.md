**OKAY**

**Justification**: The revised plan is actionable. It resolves the prior branch-base blocker by making the stale `.omo/evidence/stom-reorg-20260618/branch-map.md` and `pr-restart-strategy.md` historical only, then requiring a fail-closed fetch plus local and remote SHA check for `7d7187f7` before any worktree creation. The inspected current code supports the planned shape: `research_api.py` already owns stable `/research_docs`, `/research_doc`, `/research_records`, and `/research_records/detail` routes; `research_records.py` is campaign-scoped with safe campaign-name rejection; `research-records-panel.jsx` and `research-wiki.jsx` are separate consumers and the wiki renders markdown inertly; `phase-detail.jsx` already owns `ProcessFlowPanel`, `ProcessFlowDiagram`, `FLOW_STEPS`, `current_step`, `step_timings`, `phase_started_at`, `gen_started_at`, and recent logs; `test_p3_consolidation.py` guards Evolution HoF and Research Pro HoF as separate field-diff surfaces. The revised governed-index contract has namespaced IDs, required fields, source authority, canonicality, `.omo` allowlist limits, stale-source rejection, process-local cache invalidation, and focused verification. Executors can proceed without guessing after explicit execution approval.

**Summary**:
- Clarity: Clear. Option A, exact boundaries, file targets, route preservation, worktree safety, and stop-on-SHA-mismatch behavior are stated. The only minor ambiguity is the phrase `/research_index` or equivalent; execution should prefer the named routes because verification and smoke steps name them.
- Verifiability: Adequate. Focused backend, frontend, wiki, phase/process-flow, duplicate-global, build, missing-import, and Track Z checks are listed. Manual smoke names the legacy routes plus the new index routes. Project-wide gates remain skipped as required.
- Completeness: Adequate for the approved first slice. All-record lookup, governed evidence lineage, duplicate handling by labels only, realtime process visualization, and speed work are bounded. Broad route naming audit and whole-dashboard consolidation are intentionally deferred.
- Big Picture: Fits the repository constraints. Dirty `wt-dev`, protected runtime paths, V3K gates, live broker paths, strategy export, dependency changes, commits, pushes, PRs, and branch mutation remain out of scope until later explicit approval.
- Principle/Option Consistency: Consistent. The chosen research-index-first option follows the stated drivers: correct base, high operator value, lower regression risk, and rollback locality. HoF divergence is preserved rather than collapsed.
- Alternatives Depth: Sufficient. Option B and C are represented fairly, with the strongest case for broader consolidation acknowledged but not selected because the existing code and tests favor a bounded governed index branch.
- Risk/Verification Rigor: Sufficient. Prior high and medium architecture defects are closed at the plan layer. Remaining risks are implementation hygiene items, not planning blockers.

**Representative implementation simulations**:
1. Backend governed index: a new `research_index.py` can build namespaced rows from `research_records.list_research_records`, whitelisted docs from `research_api`, selected update logs, and allowlisted registry JSON or source-inventory entries. `research_api.py` can include `/research_index` and `/research_index/detail` while preserving all legacy routes. Detail parsing can reject traversal, malformed namespaces, missing files, and disallowed stale rows. Cache keys can be computed from root plus included source path, `mtime_ns`, and size, and tests can use temporary roots to prove root isolation without touching protected paths.
2. Frontend lookup and wiki/records bridge: `research-records-panel.jsx` and `research-wiki.jsx` can consume metadata, show source and canonicality badges, lazy-load details, abort stale requests, debounce search, memoize filters, and cap initial rows while keeping markdown in `<pre>` and avoiding `dangerouslySetInnerHTML`. `test_no_duplicate_globals.py` gives a concrete constraint for any new helper or panel names.
3. Process flow and duplicate handling: `phase-detail.jsx` already exposes the realtime process data and public components. Extraction to `process-flow-diagram.jsx` is conditional and can preserve exported globals. HoF work is limited to labels; `chart-hall-of-fame.jsx`, `rp-heatmap.jsx`, and `test_p3_consolidation.py` make a merge clearly out of scope.

**Non-blocking execution notes**:
- Persist the future fetch and SHA preflight receipt before creating the clean worktree. Do not rely on stale `.omo` branch evidence.
- Treat `canonicality` and `source_authority` as closed constants and test exact values for representative rows.
- Prefer the concrete `/research_index` and `/research_index/detail` routes so verification and manual smoke remain literal. If an equivalent route is chosen, update acceptance and tests before implementation.
- Prefer `research-registry.json` over markdown scraping. If markdown is parsed, keep the parser table-scoped and deterministic.
- Add focused tests for registry entries that use `machine_name` and any included registry campaign rows that use `campaign_id`.
- Make speed claims observable by recording endpoint size or latency and by testing metadata-only responses, lazy details, cache invalidation, search debounce or row cap constants.

No source/product files were modified during this critique, and no project-wide build, test, lint, or format gates were run.
