**[ITERATE]**

**Justification**: The revised plan is broadly actionable and much stronger than the prior blocked version: it names served bundle regeneration, stale Process iframe contract repairs, scoped ResearchLabPanel fullscreen removal, duplicate-surface boundaries, and the current dashboard parity gaps. Repository inspection confirms the key contracts are real: `frontend/index.html` and the standalone HTML entries serve `/ui/bundle/app.js`, `/ui/bundle/stom-ui.js`, and `/ui/styles.css?v=...`; `webui-build/package.json` exposes `build` and `build:app`; `build-app.mjs` regenerates bundle hashes and HTML script pins; `app.jsx` still embeds the Process iframe; `phase-detail.jsx` owns native `ProcessFlowPanel` and `ProcessFlowDiagram` state, timing, and logs; `rl-panel.jsx` still contains ResearchLabPanel fullscreen state/action; `test_dashboard_validation_views.py` still asserts fullscreen text; `dashboard-inventory.jsx`, `visual-quality.jsx`, and Track Z process harness still require/report the iframe; `evolution-gui-parity-panel.jsx` still passes `columns={2}` while `bt-gui-parity.jsx` supports one-column mode.

The remaining blocker is narrow but material under this review: `styles.css` is explicitly in scope for heatmap sizing, Process animation, Lab controls, Home readability cards, and GUI parity sizing, but the concrete served-runtime verification only names `npm run build:app` or `npm run build` plus the existing P14 harness. Actual `build-app.mjs` updates `app.js` and `stom-ui.js` pins/manifest only; it does not update `/ui/styles.css?v=20260620g004` in `index.html`, `lab.html`, `pro.html`, `verdict.html`, or `STOM AI Dashboard.html`, and the observed P14 test only asserts app/stom bundle hash consistency. The plan says generic version pins, but executors still have to infer the stylesheet cache-bust requirement and there is no concrete verification step for it. Because the user explicitly requires no stale served-runtime gap and concrete verification, this must be made explicit before execution approval.

Representative implementation simulations:
1. Process remodel slice: remove the iframe from `app.jsx`, keep `ProcessFlowPanel state={state}`, update `dashboard-inventory.jsx`, `visual-quality.jsx`, and Track Z `iframePresent` assertions to native `ProcessFlowPanel`/`ProcessFlowDiagram`, live strip, timing grid, logs, and defaults. This path is clear and covered by named files/tests.
2. Lab fullscreen removal slice: remove `fullscreen` state, fixed shell style, and the fullscreen action from `rl-panel.jsx`; preserve `opsStrip`, validation/equity route contracts, and Backtest `BtResultArea` fullscreen; update `tests/unit/test_dashboard_validation_views.py`. This path is clear and scoped.
3. Served runtime/style slice: after changing `styles.css`, `npm run build:app` will refresh bundle pins but not the stylesheet query. Without an explicit CSS pin or verification step across served HTML entries, users can still receive stale visual behavior despite source and JS bundle changes. This path currently requires inference, so the plan is not fully actionable.

**Summary**:
- Clarity: Mostly clear; file-level ownership and sequencing are concrete, but stylesheet cache-bust handling is implicit despite `styles.css` being a required surface.
- Verifiability: Strong for JS bundle, Process iframe removal, Lab fullscreen removal, Track Z, validation views, and GUI parity; weak for CSS served-runtime freshness because no concrete check is listed.
- Completeness: Covers Process remodel/animation, research visualization strengthening, Lab fullscreen removal, Home readability, one-column GUI parity, no duplicate owners, and stale Process/Lab test contracts. Needs one served-style artifact revision to close the last stale-runtime gap.
- Big Picture: Fits the brownfield dashboard parity objective and keeps owner boundaries stable: Lab explores, History archives, Workbench/HOF analyze/promote, Home summarizes/links only.
- Principle/Option Consistency: Principles align with the chosen option. Runtime truth over source-only truth is correct, but it must also apply to stylesheet pins, not only JS bundles.
- Alternatives Depth: Acceptable. The plan fairly rejects unchanged source-only work, legacy iframe/fullscreen fallbacks, and global fullscreen removal; no further architecture alternative is required for this focused revision.
- Risk/Verification Rigor: Risk table covers the main user-visible risks, but the CSS cache/version pin risk remains under-specified and unverified.

Required revisions:
1. Add an explicit served-style acceptance criterion: when `ai_strategy_loop/dashboard/frontend/styles.css` changes, update `/ui/styles.css?v=...` or an equivalent cache-busting stylesheet pin in every served HTML entry that links it (`index.html`, `lab.html`, `pro.html`, `verdict.html`, and `STOM AI Dashboard.html`), in addition to regenerated bundle outputs.
2. Add a concrete verification step for that criterion: either extend `tests/unit/dashboard/test_p14_build_harness.py` or an equivalent source/static check to assert stylesheet pin consistency/freshness across those entries, or list an explicit post-build inspection that proves the CSS pin was updated after stylesheet edits.
3. Add a top-level acceptance statement that this execution targets 100% closure of the listed remaining dashboard parity asks and may not defer any of them as advisory-only work: bounded edge heatmap sizing, Process full remodel/animation, research visualization/function strengthening, Research Lab fullscreen removal, Condition AI Home readability, and Evolution GUI Parity one-column large graphs.
