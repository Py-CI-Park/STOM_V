# Pending Approval RALPLAN Revision: Remaining Condition AI Dashboard Parity Gaps

Status: **pending approval**. Revision after critic ITERATE in `.gjc/plans/ralplan/2026-06-19-proxy-oos/stage-117-critic.md`. Planning-only: no product source edits, tests, builds, formatters, protected runtime path mutations, commits, or pushes were performed.

## Top-Level Completion Commitment
Approved execution targets **100% closure** of every listed remaining Condition AI dashboard parity ask and may not defer any listed item as advisory-only work: bounded/unified edge time x market-cap heatmap sizing, full native Process tab remodel and stronger animation, strengthened research visualizations/functionality, Research Lab fullscreen removal, Condition AI Home readability/research-suite exposure, and Evolution GUI Parity one-column larger graphs.

## Critic Amendments Incorporated
1. **CSS served-runtime cache busting**: any change to `ai_strategy_loop/dashboard/frontend/styles.css` must update `/ui/styles.css?v=...` or an equivalent stylesheet cache-busting pin in every served HTML entry that links it: `index.html`, `lab.html`, `pro.html`, `verdict.html`, and `STOM AI Dashboard.html`.
2. **CSS freshness verification**: add concrete verification through `tests/unit/dashboard/test_p14_build_harness.py` or an equivalent source/static check proving stylesheet pin consistency/freshness across those five HTML entries. After approved implementation, post-build inspection must prove the stylesheet pin changed when `styles.css` changed.
3. **Architect amendments preserved**: served JS bundle regeneration/version pins remain required; stale Process iframe contracts remain in scope; Research Lab fullscreen removal remains scoped to `ResearchLabPanel`; `BtResultArea` fullscreen analysis remains unless separately requested.
4. **Prior constraints preserved**: no duplicate History/Compare/Workbench/Research Lab surfaces, additive `/bt/result` only if touched, render-only chart replay if touched, wt-dev, STOM 2U_C nonrelease, protected runtime paths untouched, pending approval.

## RALPLAN-DR Summary
### Principles
- Runtime truth covers both JS and CSS: source edits are incomplete until served bundle and stylesheet cache pins are refreshed.
- One owner per surface: Lab owns exploration/edge/variables/correlation/combos/validation; History owns ResultDetail/Compare/archive; Workbench owns deep analysis; HOF owns promotion candidates; Home links/summarizes only.
- Contract repair with behavior removal: removing iframe/fullscreen behavior requires inventory, visual-quality, harness, and validation tests to move with it.
- Process motion reflects state evidence, preserving `current_step`, `step_timings`, timers, and logs.
- 100% parity closure is required for the listed asks; none may be punted as advisory-only.

### Top Drivers
- Browser serves generated `/ui/bundle/app.js` and `/ui/styles.css?v=...`, not raw JSX/CSS source in isolation.
- Current standalone entries all link the same stylesheet pin: `index.html`, `lab.html`, `pro.html`, `verdict.html`, and `STOM AI Dashboard.html`.
- Existing contracts still assert Process iframe and Research Lab fullscreen; they must be updated when behavior changes.

### Options
| Option | Pros | Cons | Verdict |
|---|---|---|---|
| Amend stage 117 with CSS pin/freshness and 100% closure requirements | Closes critic gap while keeping focused architecture | Slightly broader verification | **Chosen** |
| Rely on JS build only | Simpler | Leaves stale CSS risk for heatmap/process/home/parity visuals | Rejected |
| Manual browser refresh advice only | No test work | Not verifiable and fails served-runtime contract | Rejected |

## File-Level Plan
- `analysis.jsx`: bound `_Heatmap`; derive canonical axes from `segments.time` and `segments.market_cap`; fill sparse cross cells honestly; preserve counts/tooltips/legend.
- `rl-panel.jsx`: remove Research Lab fullscreen state/style/button only; preserve `opsStrip`, validation tabs, and SPA workbench navigation.
- `rl-analysis.jsx`: strengthen variable importance, correlation, combo visuals with top-N, sorting/filtering, legends, selected-pair detail, low-sample warnings.
- `phase-detail.jsx`: native process graph/cards/timing/log/defaults and stronger animation with reduced-motion.
- `app.jsx`: remove Process iframe; add Home Research Suite cards; keep Home navigation/summary only.
- `evolution-gui-parity-panel.jsx`: use one-column large `BtGuiParitySection`, not `columns={2}`.
- `bt-gui-parity.jsx`: make one-column mode explicit and preserve Backtest usage.
- `styles.css`: bounded heatmap, process animation/reduced-motion, Lab controls, Home cards, GUI parity one-column styles.
- `index.html`, `lab.html`, `pro.html`, `verdict.html`, `STOM AI Dashboard.html`: update `/ui/styles.css?v=...` or equivalent cache-busting pin whenever `styles.css` changes; also preserve/update JS bundle pins generated by build.
- `dashboard-pages.jsx`, `ui-contract.jsx`, `dashboard-inventory.jsx`, `visual-quality.jsx`: update Lab/Home/HOF/Process product text and owner contracts; no new routes.
- Served outputs after approved source edits: `frontend/bundle/app.js`, changed `bundle/stom-ui.js` if any, HTML JS pins, and CSS stylesheet pins.
- Tests: update `tests/unit/dashboard/test_p14_build_harness.py` or equivalent static check for stylesheet pin freshness across the five HTML entries; update Track Z harness, validation views, UI remodel, Research Lab, Process timing, Research records, and Evolution GUI Parity tests as described in stage 117.

## Sequencing
1. **Runtime and cache-bust preflight**: before implementation, include JS bundle outputs and all stylesheet-linked HTML entries in scope.
2. **Lab and heatmap**: remove Lab fullscreen only; preserve Backtest fullscreen; bound/canonicalize heatmap; update Lab and validation tests.
3. **Research visualizations**: complete edge, feature importance, correlation, combo, and validation strengthening; no listed research item may remain advisory-only.
4. **Process tab**: remove app iframe; native process page; update `dashboard-inventory.jsx`, `visual-quality.jsx`, and Track Z harness; reduced-motion animation.
5. **Home**: Research Suite cards target existing routes/sections: Lab `/ui/evolution/lab`, History `/ui/evolution/records`, Workbench `/ui/evolution/workbench`, HOF existing Workbench/HOF section or overview anchor, Backtest `/ui/backtest`, Chart Replay `/ui/chart-replay`.
6. **Evolution GUI Parity**: one-column larger stacked charts.
7. **Served runtime generation and pins**: run `npm run build:app` or `npm run build`; update/verify JS bundle pins and CSS stylesheet pins in every served HTML entry.

## Acceptance Criteria
- **100% closure**: all listed remaining parity asks are implemented in the approved execution; none is deferred as advisory-only.
- Any `styles.css` edit updates `/ui/styles.css?v=...` or equivalent cache-busting stylesheet pin in `index.html`, `lab.html`, `pro.html`, `verdict.html`, and `STOM AI Dashboard.html`.
- `tests/unit/dashboard/test_p14_build_harness.py` or equivalent source/static check verifies CSS pin consistency/freshness across those entries; post-build inspection proves the stylesheet pin changed when stylesheet edits occurred.
- Served dashboard runtime reflects source changes via regenerated JS bundle/version pins and refreshed CSS pins.
- No process iframe in `app.jsx`; `/process_flow` is read-only reference only if retained.
- `dashboard-inventory.jsx`, `visual-quality.jsx`, and Track Z harness no longer require embedded iframe.
- ResearchLabPanel has no fullscreen toggle/style/action; `BtResultArea` fullscreen remains.
- `tests/unit/test_dashboard_validation_views.py` asserts Lab fullscreen absence while preserving ops/validation contracts.
- Edge/time x market-cap heatmap is bounded, uses canonical axes, fills sparse cells honestly, and shows counts/tooltips/legend.
- Home Research Suite is readable and navigation-only; no new HOF route and no duplicate owner surfaces.
- Evolution GUI Parity no longer passes `columns={2}`.
- Existing route keys remain stable; `/bt/result` remains additive only if touched; chart replay remains render-only if touched; protected paths untouched.

## Verification Plan
Planning ran no tests/builds/formatters/gates. Approved execution must run served artifact generation, not as a formatter/gate but as required runtime output:

```powershell
cd ai_strategy_loop/dashboard/webui-build
npm run build:app
```

Fallback if unavailable:

```powershell
cd ai_strategy_loop/dashboard/webui-build
npm run build
```

Required focused verification after approved implementation:

```powershell
pytest tests/unit/dashboard/test_p14_build_harness.py tests/unit/dashboard/test_dashboard_ui_remodel.py tests/unit/test_dashboard_research_lab_frontend.py tests/unit/dashboard/test_p11_process_flow.py tests/unit/test_process_timing.py tests/unit/dashboard/test_track_z_pr1_harness.py tests/unit/test_dashboard_validation_views.py tests/unit/dashboard/test_research_records_frontend.py tests/unit/dashboard/test_evolution_gui_parity.py -q
```

The P14 or equivalent check must include CSS pin freshness: collect stylesheet `?v=` from `index.html`, `lab.html`, `pro.html`, `verdict.html`, and `STOM AI Dashboard.html`, assert all served entries are updated consistently, and when `styles.css` changed in the implementation diff, assert the pin differs from the pre-change value.

## Risks and Mitigations
| Risk | Mitigation |
|---|---|
| CSS source changes not served | Mandatory stylesheet pin updates across all five HTML entries plus P14/static freshness check |
| JS source edits not served | Mandatory bundle generation and JS version-pin outputs |
| Old tests force iframe/fullscreen back | Update inventory, visual-quality, Track Z, validation tests |
| Lab fullscreen removal breaks Backtest | Scope removal to ResearchLabPanel only |
| Listed parity item deferred | Top-level 100% closure acceptance forbids advisory-only deferral |
| Protected paths mutate | No runtime DB/log/graph writes; tmp_path fixtures only |

## ADR
Decision: amend stage 117 to make CSS cache-busting first-class, require P14/equivalent stylesheet freshness verification across all served HTML entries, and explicitly require 100% closure of the listed dashboard parity asks. Consequence: execution must update served JS and CSS runtime artifacts together, eliminating stale visual behavior while preserving owner boundaries and prior architect-approved amendments.

## Handoff
Pending approval. Recommended execution after approval: Ultragoal ledger with bounded executor slices; architect/critic review after Process/Home/runtime-pins completion.
