**ITERATE**

**Justification**: The Planner hybrid-shell direction is correct and evidence-backed, and the Architect review is fair. However the Architect WATCH comments must be incorporated before pending approval. The current plan leaves executors to choose the remodel URL/deep-link scheme, React bootstrap/build entry, remodel export policy, and CSS scoping strategy. Those decisions determine whether `/ui/remodel/` remains route-owned, whether existing routes stay preserved, and whether the no-hidden-production-export contract is upheld. Proceeding to pending approval now would force implementation guessing at the highest-risk seam.

**Summary**:
- Clarity: Strong product scope, page map, and sequencing. Insufficient clarity on route namespace, bootstrap/build path, `final_approval` behavior in remodel, and CSS scope.
- Verifiability: Acceptance criteria are mostly testable and include route preservation, endpoint consumption, E2E, visual, observability, and manual QA. Needs concrete tests for remodel deep-link refresh, in-remodel navigation not escaping to `/ui/*`, build manifest/hash outputs, and export source/DOM guards.
- Completeness: Broad coverage of condition AI, backtest, replay, settings, audit, and safety. Missing pre-implementation adapter decisions block safe execution.
- Big Picture: Correctly treats the production React/FastAPI dashboard as the executable specification and rejects greenfield state-machine duplication. Existing routes and research-only constraints remain central.
- Principle/Option Consistency: Option A aligns with reuse/no-mock/route-preservation principles. Tension remains between `research-only` and reusing production `ApprovalDialog`/`final_approval`; the final plan must resolve it explicitly.
- Alternatives Depth: Planner alternatives are adequate and the Architect steelman fairly states the greenfield upside. Final plan should fold that steelman into the trade-off notes so the chosen hybrid path is not shallow consensus by assertion.
- Risk/Verification Rigor: Expanded test plan and pre-mortem are present and generally strong. They need the Architect WATCH risks promoted into non-negotiable gates, especially route namespace, build/bootstrap, safety export, and CSS bleed.

**Referenced artifacts and files verified**:
- Planner artifact read from `.gjc/_session-019f0846-48b0-7000-aba3-a901492312f0/plans/ralplan/019f0846-48b0-7000-aba3-a901492312f0/stage-01-planner.md`.
- Architect artifact read from `.gjc/_session-019f084d-488d-7000-a1a3-83362420c2cd/plans/ralplan/019f084d-488d-7000-a1a3-83362420c2cd/stage-01-architect.md`.
- Scorecard evidence verified in `docs/update_log/2026-06-27_dashboard_remodel_detailed_100_point_scorecard.md`: current remodel is about 55/100 existing-dashboard parity and 71/100 standalone completeness.
- Route/static evidence verified in `ai_strategy_loop/dashboard/app.py`: explicit `/ui/evolution`, `/ui/backtest`, `/ui/chart-replay` handlers exist before static mounts; `/ui/remodel` is mounted before `/ui`; backend `final_approval` calls `export_winner` to production `strategy.db` after human approval.
- Route-contract risk verified in `ai_strategy_loop/dashboard/frontend/ui-contract.jsx` and `app.jsx`: `dashboardPathFor` hard-codes `/ui/evolution`, `/ui/backtest`, `/ui/chart-replay`, and `syncBrowserRoute` uses `history.pushState`/`replaceState` to those paths.
- Remodel preview state verified in `frontend/remodel/index.html`, `frontend/remodel/src/app.js`, and `frontend/remodel/src/data.js`: no-build static scripts, dummy `STOM_DATA`, live bridge only for `/health`, `/status`, `/runs`, and `/ws`.
- Production behavior verified in `app.jsx`, `cards.jsx`, `bt-tab-root.jsx`, `bt-tab-run.jsx`, `bt-tab-analysis.jsx`, `sim-tab-root.jsx`, `backtest_api.py`, and `simulation_api.py`: mature backtest/replay components and API/WS routes exist and justify reuse.
- Existing remodel tests verified in `tests/unit/test_dashboard_remodel_static.py`: current safety gate asserts no `final_approval`, no live-order/broker/account controls, and static live-bridge markers.
- Build strategy verified in `ai_strategy_loop/dashboard/webui-build/package.json` and `build-app.mjs`: production dashboard uses esbuild bundle-only output, content-hashed `/ui/bundle/app.js` and `stom-ui.js`, plus manifest/hash post-build behavior. Remodel preview currently bypasses this.

**Representative implementation simulations**:
1. **Mount production `App` under `/ui/remodel/`**: `dashboardRouteFromLocation` parses the first path segment after `ui`; for `/ui/remodel/backtest`, the head is `remodel`, so it falls back, then `syncBrowserRoute` can replace/push `/ui/evolution` or `/ui/backtest`. Without a prefix-aware route adapter or hash/query scheme, clicking inside remodel can leave the remodel route. This validates the Architect route WATCH as a blocker.
2. **Replace static remodel backtest with `BacktestTab`**: the functional component exists and consumes `/bt/health`, `/bt/strategies`, `/bt/run`, `/bt/ws_job`, `/bt/result`, overlays, portfolio, report, and evo handoff. But `frontend/remodel/index.html` only loads `src/data.js` and `src/app.js`; the production build is generated through `webui-build` into `/ui/bundle`. An executor would have to invent whether to create a `RemodelApp` bundle, adapter entry, or raw script reuse. This must be specified.
3. **Reuse production approval/export UX in research-only remodel**: `ApprovalDialog` sends `final_approval`; `_do_final_approval` exports to production `strategy.db`; current remodel tests require `final_approval` to be absent. The plan says no hidden automatic production export and human approval, but not whether remodel disables export entirely until promotion or reuses the explicit modal. This is a safety-contract decision, not an implementation detail.
4. **Apply remodel tokens to reused production components**: production `styles.css` uses `--bg-0`, `--line-1`, `--ink-*`; remodel `theme.css` uses `--bg`, `--panel`, `--border`, `--text` and global `body`/button/input rules. A naive bridge can bleed into preserved `/ui/evolution`, `/ui/backtest`, and `/ui/chart-replay`. CSS scope/layer acceptance criteria are required.

**Required fixes before pending approval**:
1. Add a remodel route namespace addendum: choose either hash/query routing under `/ui/remodel/` or prefix-aware `dashboardPathFor`/`dashboardRouteFromLocation`; define common shell, condition subtabs, backtest, replay, settings, inspector, and modal URLs; require refresh/deep-link tests and click-navigation tests proving remodel does not escape to `/ui/backtest` or `/ui/chart-replay` while existing routes remain preserved.
2. Add a build/bootstrap addendum: specify the exact production build entry and output for remodel, whether a new `RemodelApp` composes production child components or `App` is reused via adapter, how manifest/content hashes are updated, and that raw JSX/runtime-Babel or a second parallel renderer is not introduced.
3. Add a safety export addendum: explicitly decide `final_approval` in `/ui/remodel/` for this replacement phase. Either disable/export-hide it until final promotion, or reuse it only with the existing explicit human modal plus visible audit separation. Add source and DOM assertions for no live order, broker login, account trading, or hidden automatic production export.
4. Add CSS scoping criteria: token mapping must be namespaced or layered so remodel styling cannot globally regress the preserved routes; screenshot controls for `/ui/evolution`, `/ui/backtest`, `/ui/chart-replay`, and `/ui/remodel/` must be required each pass.
5. Expand the pre-mortem and test plan with the four WATCH risks above. Keep the existing backtest/replay E2E gates, but add build/harness/manifest checks and direct remodel deep-link refresh coverage.

**Verdict**: ITERATE. The plan should not proceed to pending approval until a planner revision incorporates the Architect WATCH comments as explicit execution and verification requirements.
